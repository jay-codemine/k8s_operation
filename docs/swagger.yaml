definitions:
  internal_app_controllers_api_v1.activateRequest:
    properties:
      license:
        type: string
    required:
    - license
    type: object
  intstr.IntOrString:
    properties:
      intVal:
        type: integer
      strVal:
        type: string
      type:
        $ref: '#/definitions/intstr.Type'
    type: object
  intstr.Type:
    enum:
    - 0
    - 1
    format: int64
    type: integer
    x-enum-comments:
      Int: The IntOrString holds an int.
      String: The IntOrString holds a string.
    x-enum-descriptions:
    - The IntOrString holds an int.
    - The IntOrString holds a string.
    x-enum-varnames:
    - Int
    - String
  k8s_io_api_core_v1.ResourceClaim:
    properties:
      name:
        description: |-
          Name must match the name of one entry in pod.spec.resourceClaims of
          the Pod where this field is used. It makes that resource available
          inside a container.
        type: string
      request:
        description: |-
          Request is the name chosen for a request in the referenced claim.
          If empty, everything from the claim is made available, otherwise
          only the result of this request.

          +optional
        type: string
    type: object
  k8soperation_internal_app_models.AppStoreCreateRequest:
    properties:
      category:
        type: string
      chart_url:
        type: string
      description:
        type: string
      display_name:
        type: string
      doc_url:
        type: string
      featured:
        type: integer
      icon:
        type: string
      min_k8s:
        type: string
      name:
        type: string
      namespace:
        type: string
      provider:
        type: string
      sort_order:
        type: integer
      status:
        type: integer
      tags:
        type: string
      values_yaml:
        type: string
      version:
        type: string
    required:
    - category
    - name
    - version
    type: object
  k8soperation_internal_app_models.AppStoreInstallRequest:
    properties:
      app_id:
        type: integer
      cluster_id:
        type: integer
      namespace:
        type: string
      release_name:
        type: string
      values:
        type: string
    required:
    - app_id
    - cluster_id
    - namespace
    - release_name
    type: object
  k8soperation_internal_app_models.AppStoreUpdateRequest:
    properties:
      category:
        type: string
      chart_url:
        type: string
      description:
        type: string
      display_name:
        type: string
      doc_url:
        type: string
      featured:
        type: integer
      icon:
        type: string
      id:
        type: integer
      min_k8s:
        type: string
      name:
        type: string
      namespace:
        type: string
      provider:
        type: string
      sort_order:
        type: integer
      status:
        type: integer
      tags:
        type: string
      values_yaml:
        type: string
      version:
        type: string
    required:
    - id
    type: object
  k8soperation_internal_app_models.ApprovalLevel:
    properties:
      label:
        type: string
      level:
        type: integer
      role:
        type: string
      user_ids:
        items:
          type: integer
        type: array
    type: object
  k8soperation_internal_app_models.AuditLog:
    properties:
      action:
        type: string
      action_display:
        type: string
      cluster_id:
        type: integer
      cluster_name:
        type: string
      created_at:
        type: integer
      detail:
        type: string
      duration_ms:
        type: integer
      error_message:
        type: string
      extra:
        type: string
      id:
        type: integer
      module:
        type: string
      namespace:
        type: string
      pipeline_id:
        type: integer
      pipeline_name:
        type: string
      project_id:
        type: integer
      project_name:
        type: string
      request_body:
        type: string
      request_method:
        type: string
      request_uri:
        type: string
      response_code:
        type: integer
      response_message:
        type: string
      status:
        type: string
      target_id:
        type: string
      target_name:
        type: string
      target_type:
        type: string
      user_agent:
        type: string
      user_id:
        type: integer
      user_ip:
        type: string
      username:
        type: string
    type: object
  k8soperation_internal_app_models.AuditRetentionUpdateReq:
    properties:
      is_permanent:
        type: boolean
      retention_days:
        maximum: 3650
        minimum: 0
        type: integer
    type: object
  k8soperation_internal_app_models.CicdEnvResourceRule:
    properties:
      approval_role:
        type: string
      cpu_limit_max:
        type: string
      cpu_request_min:
        type: string
      created_at:
        type: integer
      description:
        type: string
      env:
        type: string
      id:
        type: integer
      memory_limit_max:
        type: string
      memory_request_min:
        type: string
      modified_at:
        type: integer
      replicas_max:
        type: integer
      replicas_min:
        type: integer
      require_approval:
        type: boolean
      service_type:
        type: string
    type: object
  k8soperation_internal_app_models.CicdResourceTemplate:
    properties:
      cpu_limit:
        type: string
      cpu_request:
        type: string
      created_at:
        type: integer
      description:
        type: string
      env:
        type: string
      hpa_cpu_target:
        type: integer
      hpa_enabled:
        type: boolean
      hpa_max_replicas:
        type: integer
      hpa_min_replicas:
        type: integer
      id:
        type: integer
      is_default:
        type: boolean
      memory_limit:
        type: string
      memory_request:
        type: string
      modified_at:
        type: integer
      name:
        type: string
      replicas_default:
        type: integer
      replicas_max:
        type: integer
      replicas_min:
        type: integer
      service_type:
        type: string
      sort_order:
        type: integer
    type: object
  k8soperation_internal_app_models.EnvVar:
    properties:
      name:
        type: string
      value:
        type: string
    type: object
  k8soperation_internal_app_models.GitOpsConfig:
    properties:
      argo_app_name:
        type: string
      argo_project:
        type: string
      auto_sync:
        type: boolean
      build_context:
        type: string
      dockerfile_path:
        type: string
      git_manifest_repo:
        type: string
      image_registry:
        type: string
      image_repo:
        type: string
      manifest_path:
        type: string
      prune_resource:
        type: boolean
      target_revision:
        type: string
      workflow_namespace:
        type: string
      workflow_template:
        type: string
    type: object
  k8soperation_internal_app_models.PlatformSettingsResponse:
    properties:
      about:
        $ref: '#/definitions/k8soperation_internal_domain_settings.AboutSettings'
      alert:
        $ref: '#/definitions/k8soperation_internal_domain_settings.AlertSettings'
      basic:
        $ref: '#/definitions/k8soperation_internal_domain_settings.BasicSettings'
      notification:
        $ref: '#/definitions/k8soperation_internal_domain_settings.NotificationSettings'
      security:
        $ref: '#/definitions/k8soperation_internal_domain_settings.SecuritySettings'
    type: object
  k8soperation_internal_app_models.ResourceConfig:
    properties:
      hpa:
        $ref: '#/definitions/k8soperation_internal_domain_cicd.HPAConfig'
      replicas:
        type: integer
      resources:
        $ref: '#/definitions/k8soperation_internal_domain_cicd.ResourceRequests'
      strategy:
        type: string
    type: object
  k8soperation_internal_app_models.ResourceValidationResult:
    properties:
      approval_role:
        type: string
      errors:
        items:
          type: string
        type: array
      need_approval:
        type: boolean
      risk_level:
        $ref: '#/definitions/k8soperation_internal_domain_cicd.RiskLevel'
      suggestion:
        type: string
      valid:
        type: boolean
      warnings:
        items:
          type: string
        type: array
    type: object
  k8soperation_internal_app_requests.ApprovalActionRequest:
    properties:
      action:
        description: approve/reject
        type: string
      id:
        type: integer
      reason:
        description: 审批意见
        type: string
    type: object
  k8soperation_internal_app_requests.ApprovalBatchActionRequest:
    properties:
      action:
        description: approve/reject
        type: string
      ids:
        items:
          type: integer
        type: array
      reason:
        description: 审批意见
        type: string
    type: object
  k8soperation_internal_app_requests.ApprovalCreateRequest:
    properties:
      env_name:
        type: string
      image:
        type: string
      image_digest:
        type: string
      pipeline_id:
        type: integer
      pipeline_run_id:
        type: integer
      request_reason:
        type: string
    type: object
  k8soperation_internal_app_requests.ApprovalDeleteRequest:
    properties:
      id:
        type: integer
    type: object
  k8soperation_internal_app_requests.ApprovalUpdateRequest:
    properties:
      env_name:
        description: 目标环境
        type: string
      id:
        type: integer
      image:
        description: 镜像地址
        type: string
      image_digest:
        description: 镜像摘要
        type: string
      request_reason:
        description: 申请原因
        type: string
    type: object
  k8soperation_internal_app_requests.AuthForgotPasswordRequest:
    properties:
      confirm:
        type: string
      new_password:
        type: string
      username:
        type: string
    type: object
  k8soperation_internal_app_requests.AuthLoginRequest:
    properties:
      password:
        type: string
      username:
        type: string
    type: object
  k8soperation_internal_app_requests.AuthRegisterRequest:
    properties:
      password:
        type: string
      password_confirm:
        type: string
      username:
        type: string
    type: object
  k8soperation_internal_app_requests.BasicAuthFields:
    properties:
      password:
        type: string
      username:
        type: string
    type: object
  k8soperation_internal_app_requests.BatchClusterPermissionRequest:
    properties:
      access_level:
        description: none/read/write/admin
        type: string
      can_create:
        description: deprecated
        type: boolean
      can_delete:
        description: deprecated
        type: boolean
      can_exec:
        description: deprecated
        type: boolean
      can_update:
        description: deprecated
        type: boolean
      can_view:
        description: deprecated
        type: boolean
      cluster_ids:
        items:
          type: integer
        type: array
      role_type:
        type: string
      user_id:
        type: integer
    type: object
  k8soperation_internal_app_requests.CicdBuildCallbackRequest:
    properties:
      build_id:
        description: Jenkins 构建ID
        type: integer
      image_digest:
        description: 镜像摘要（可选）
        type: string
      image_repo:
        description: 构建出的镜像Repo
        type: string
      image_tag:
        description: 构建出的镜像Tag
        type: string
      message:
        description: 构建消息
        type: string
      status:
        description: SUCCESS / FAILURE / ABORTED
        type: string
    type: object
  k8soperation_internal_app_requests.CicdReleaseBatchCancelRequest:
    properties:
      ids:
        items:
          type: integer
        type: array
    type: object
  k8soperation_internal_app_requests.CicdReleaseBatchRetryRequest:
    properties:
      ids:
        items:
          type: integer
        type: array
    type: object
  k8soperation_internal_app_requests.CicdReleaseBatchRollbackRequest:
    properties:
      ids:
        items:
          type: integer
        type: array
    type: object
  k8soperation_internal_app_requests.CicdReleaseCreateRequest:
    properties:
      app_name:
        type: string
      cluster_ids:
        items:
          type: integer
        type: array
      concurrency:
        type: integer
      container_name:
        type: string
      env:
        description: 镜像晋级链追踪（由 promote 服务填充，手动创建可不传）
        type: string
      image_digest:
        type: string
      image_repo:
        type: string
      image_tag:
        type: string
      message:
        description: 可选：发布说明/备注
        type: string
      namespace:
        type: string
      pipeline_id:
        description: 模板化发布：传入 pipeline_id 后自动继承流水线的部署配置，以下字段均可省略
        type: integer
      request_id:
        description: 可选，幂等校验用
        type: string
      source_env:
        description: 晋级来源环境
        type: string
      source_run_id:
        description: 构建镜像的流水线运行ID
        type: integer
      strategy:
        type: string
      timeout_sec:
        type: integer
      workload_kind:
        type: string
      workload_name:
        type: string
    type: object
  k8soperation_internal_app_requests.CicdReleaseIDRequest:
    properties:
      id:
        type: integer
    type: object
  k8soperation_internal_app_requests.CicdReleaseUpdateRequest:
    properties:
      app_name:
        type: string
      concurrency:
        type: integer
      container_name:
        type: string
      id:
        type: integer
      image_repo:
        type: string
      image_tag:
        type: string
      message:
        type: string
      namespace:
        type: string
      strategy:
        type: string
      timeout_sec:
        type: integer
      workload_kind:
        type: string
      workload_name:
        type: string
    type: object
  k8soperation_internal_app_requests.ClusterPermissionCreateRequest:
    properties:
      access_level:
        description: none/read/write/admin
        type: string
      can_create:
        description: deprecated
        type: boolean
      can_delete:
        description: deprecated
        type: boolean
      can_exec:
        description: deprecated
        type: boolean
      can_update:
        description: deprecated
        type: boolean
      can_view:
        description: deprecated
        type: boolean
      cluster_id:
        type: integer
      expire_at:
        description: 过期时间，0=永不过期
        type: integer
      namespaces:
        description: 可访问的命名空间（空=全部）
        items:
          type: string
        type: array
      role_type:
        type: string
      user_id:
        type: integer
    type: object
  k8soperation_internal_app_requests.ClusterPermissionUpdateRequest:
    properties:
      access_level:
        description: none/read/write/admin
        type: string
      can_create:
        description: deprecated
        type: boolean
      can_delete:
        description: deprecated
        type: boolean
      can_exec:
        description: deprecated
        type: boolean
      can_update:
        description: deprecated
        type: boolean
      can_view:
        description: deprecated
        type: boolean
      expire_at:
        type: integer
      id:
        type: integer
      namespaces:
        items:
          type: string
        type: array
      role_type:
        type: string
    type: object
  k8soperation_internal_app_requests.CommonIdRequest:
    properties:
      id:
        type: integer
    type: object
  k8soperation_internal_app_requests.CreatedResource:
    properties:
      index:
        type: integer
      kind:
        type: string
      message:
        type: string
      name:
        type: string
      namespace:
        type: string
    type: object
  k8soperation_internal_app_requests.DataSourceRef:
    properties:
      apiGroup:
        description: '"snapshot.storage.k8s.io" 等'
        type: string
      kind:
        description: PersistentVolumeClaim / VolumeSnapshot
        type: string
      name:
        type: string
    type: object
  k8soperation_internal_app_requests.DockerFields:
    properties:
      auth:
        type: string
      email:
        type: string
      password:
        type: string
      server:
        type: string
      username:
        type: string
    type: object
  k8soperation_internal_app_requests.EnvVarDef:
    properties:
      name:
        type: string
      value:
        type: string
    type: object
  k8soperation_internal_app_requests.EnvVarKV:
    properties:
      name:
        type: string
      value:
        type: string
    type: object
  k8soperation_internal_app_requests.EnvironmentCreateRequest:
    properties:
      approval_levels:
        description: 多级审批配置
        items:
          $ref: '#/definitions/k8soperation_internal_app_models.ApprovalLevel'
        type: array
      approval_user_ids:
        description: 审批人员ID列表（兼容旧接口）
        items:
          type: integer
        type: array
      auto_rollback_on_fail:
        description: 部署失败时自动回滚
        type: boolean
      cluster_id:
        description: 关联集群ID
        type: integer
      color:
        description: 环境颜色标识
        type: string
      description:
        description: 描述
        type: string
      display_name:
        description: 显示名称
        type: string
      name:
        description: 环境名称(dev/staging/prod)
        type: string
      namespace:
        description: 默认命名空间
        type: string
      require_approval:
        description: 是否需要审批
        type: boolean
      sort_order:
        description: 排序
        type: integer
    type: object
  k8soperation_internal_app_requests.EnvironmentIDRequest:
    properties:
      id:
        type: integer
    type: object
  k8soperation_internal_app_requests.EnvironmentUpdateRequest:
    properties:
      approval_levels:
        description: 多级审批配置
        items:
          $ref: '#/definitions/k8soperation_internal_app_models.ApprovalLevel'
        type: array
      approval_user_ids:
        items:
          type: integer
        type: array
      auto_rollback_on_fail:
        type: boolean
      cluster_id:
        type: integer
      color:
        type: string
      description:
        type: string
      display_name:
        type: string
      id:
        type: integer
      name:
        type: string
      namespace:
        type: string
      require_approval:
        type: boolean
      sort_order:
        type: integer
    type: object
  k8soperation_internal_app_requests.EnvironmentVariable:
    properties:
      name:
        description: 环境变量名
        type: string
      value:
        description: 环境变量值
        type: string
    type: object
  k8soperation_internal_app_requests.FailedResource:
    properties:
      error:
        type: string
      index:
        type: integer
      kind:
        type: string
      message:
        type: string
      name:
        type: string
    type: object
  k8soperation_internal_app_requests.GitBranchesRequest:
    properties:
      credential_id:
        description: 可选：Jenkins凭证ID
        type: string
      password:
        description: 可选：私有仓库认证密码或访问令牌(Token)
        type: string
      repo_url:
        type: string
      username:
        description: 可选：私有仓库认证用户名
        type: string
    type: object
  k8soperation_internal_app_requests.GitValidateRequest:
    properties:
      credential_id:
        description: 可选：Jenkins凭证ID
        type: string
      password:
        description: 可选：私有仓库认证密码或访问令牌(Token)
        type: string
      repo_url:
        type: string
      username:
        description: 可选：私有仓库认证用户名
        type: string
    type: object
  k8soperation_internal_app_requests.HealthCheckDetail:
    properties:
      command:
        description: Type=Command 时使用
        type: string
      failure_threshold:
        type: integer
      http_header:
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.HttpHeader'
        type: array
      initial_delay_seconds:
        type: integer
      path:
        type: string
      period_seconds:
        type: integer
      port:
        type: integer
      protocol:
        type: string
      success_threshold:
        type: integer
      timeout_seconds:
        type: integer
      type:
        description: HTTP|TCP|Command
        type: string
    type: object
  k8soperation_internal_app_requests.HttpHeader:
    properties:
      name:
        type: string
      value:
        type: string
    type: object
  k8soperation_internal_app_requests.IngressPathRule:
    properties:
      path:
        description: 访问路径（如 /、/api、/api/(.*)）
        type: string
      path_type:
        description: 'pathType: Exact / Prefix / ImplementationSpecific'
        type: string
      service_name:
        description: 后端 Service 名称
        type: string
      service_port:
        description: 后端 Service 端口（支持命名端口，故用字符串；可传 "80" 或 "http"）
        type: string
    type: object
  k8soperation_internal_app_requests.IngressRule:
    properties:
      host:
        type: string
      paths:
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.IngressPathRule'
        type: array
    type: object
  k8soperation_internal_app_requests.IngressTLS:
    properties:
      hosts:
        items:
          type: string
        type: array
      secret_name:
        type: string
    type: object
  k8soperation_internal_app_requests.K8sClusterBatchDeleteRequest:
    properties:
      ids:
        items:
          type: integer
        type: array
    type: object
  k8soperation_internal_app_requests.K8sClusterCreateRequest:
    properties:
      cluster_name:
        type: string
      cluster_version:
        type: string
      kube_config:
        type: string
    type: object
  k8soperation_internal_app_requests.K8sClusterDeleteRequest:
    properties:
      force:
        description: 可选：是否强制删除
        type: boolean
      id:
        type: integer
    type: object
  k8soperation_internal_app_requests.K8sClusterInitRequest:
    properties:
      id:
        type: integer
    type: object
  k8soperation_internal_app_requests.K8sClusterUpdateRequest:
    properties:
      cluster_name:
        type: string
      cluster_version:
        type: string
      id:
        type: integer
      kube_config:
        description: ✅ 可选（不填就不更新）
        type: string
    type: object
  k8soperation_internal_app_requests.KVDef:
    properties:
      key:
        type: string
      value:
        type: string
    type: object
  k8soperation_internal_app_requests.KubeAppConfigCreateRequest:
    properties:
      app_name:
        type: string
      cluster_id:
        type: integer
      enable_metrics:
        type: boolean
      env:
        description: 只校验 env 存在，不校验内部字段
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.EnvVarKV'
        type: array
      image:
        type: string
      namespace:
        type: string
      replicas:
        description: 可选
        type: integer
      strategy:
        type: string
    type: object
  k8soperation_internal_app_requests.KubeAppConfigUpdateRequest:
    properties:
      app_name:
        type: string
      cluster_id:
        type: integer
      enable_metrics:
        type: boolean
      env:
        description: 可选
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.EnvVarKV'
        type: array
      image:
        type: string
      namespace:
        type: string
      replicas:
        type: integer
      strategy:
        type: string
    type: object
  k8soperation_internal_app_requests.KubeApplyYamlClusterRequest:
    properties:
      name:
        type: string
      yaml:
        type: string
    type: object
  k8soperation_internal_app_requests.KubeApplyYamlRequest:
    properties:
      name:
        type: string
      namespace:
        type: string
      yaml:
        type: string
    type: object
  k8soperation_internal_app_requests.KubeConfigMapApplyYamlRequest:
    properties:
      yaml:
        description: YAML 字符串，包含 namespace 和 name
        type: string
    type: object
  k8soperation_internal_app_requests.KubeConfigMapCreateRequest:
    properties:
      annotations:
        type: string
      binaryData:
        additionalProperties:
          items:
            format: int32
            type: integer
          type: array
        type: object
      data:
        description: ConfigMap 的数据部分：文本型与二进制型
        type: string
      labels:
        type: string
      name:
        type: string
      namespace:
        type: string
    type: object
  k8soperation_internal_app_requests.KubeConfigMapUpdateDataRequest:
    properties:
      binaryData:
        additionalProperties:
          items:
            format: int32
            type: integer
          type: array
        description: 二选一
        type: object
      data:
        additionalProperties:
          type: string
        description: 二选一
        type: object
      name:
        type: string
      namespace:
        type: string
    type: object
  k8soperation_internal_app_requests.KubeCronJobCreateRequest:
    properties:
      active_deadline_seconds:
        type: integer
      affinity:
        $ref: '#/definitions/v1.Affinity'
      backoff_limit:
        type: integer
      completions:
        type: integer
      concurrency_policy:
        description: Allow|Forbid|Replace
        type: string
      container_command:
        items:
          type: string
        type: array
      container_command_args:
        items:
          type: string
        type: array
      container_image:
        type: string
      container_name:
        description: === Pod 模板 ===
        type: string
      containers:
        items:
          $ref: '#/definitions/v1.Container'
        type: array
      cpu_requirement:
        type: string
      description:
        description: 描述，可选
        type: string
      failed_jobs_history_limit:
        type: integer
      image_pull_secrets:
        items:
          $ref: '#/definitions/v1.LocalObjectReference'
        type: array
      init_containers:
        items:
          $ref: '#/definitions/v1.Container'
        type: array
      is_liveness_enable:
        type: boolean
      is_readiness_enable:
        description: === 健康探针 ===
        type: boolean
      labels:
        description: 标签，可选
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.Label'
        type: array
      liveness_probe:
        $ref: '#/definitions/k8soperation_internal_app_requests.HealthCheckDetail'
      memory_requirement:
        type: string
      name:
        description: === 基本信息 ===
        type: string
      namespace:
        description: 命名空间
        type: string
      node_selector:
        additionalProperties:
          type: string
        type: object
      parallelism:
        description: === JobSpec 级字段 ===
        type: integer
      port_mappings:
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.PortMapping'
        type: array
      readiness_probe:
        $ref: '#/definitions/k8soperation_internal_app_requests.HealthCheckDetail'
      restart_policy:
        description: === PodSpec 配置 ===
        type: string
      run_as_privileged:
        type: boolean
      schedule:
        description: === 调度配置 ===
        type: string
      service_account:
        type: string
      starting_deadline_seconds:
        type: integer
      successful_jobs_history_limit:
        type: integer
      suspend:
        description: 是否暂停任务
        type: boolean
      time_zone:
        description: 时区
        type: string
      tolerations:
        items:
          $ref: '#/definitions/v1.Toleration'
        type: array
      ttl_seconds_after_finished:
        type: integer
      variables:
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.EnvironmentVariable'
        type: array
      volumes:
        items:
          $ref: '#/definitions/v1.Volume'
        type: array
    type: object
  k8soperation_internal_app_requests.KubeCronJobDeleteRequest:
    properties:
      cascade:
        description: 是否级联清理：通常删除 CronJob 时是否一并清理其历史 Job
        type: boolean
      force:
        type: boolean
      grace_period_seconds:
        type: integer
      name:
        type: string
      namespace:
        type: string
      timeout_seconds:
        type: integer
      wait:
        type: boolean
    type: object
  k8soperation_internal_app_requests.KubeCronJobSuspendRequest:
    properties:
      name:
        type: string
      namespace:
        type: string
      suspend:
        description: true=暂停，false=恢复
        type: boolean
    type: object
  k8soperation_internal_app_requests.KubeCronJobTriggerRequest:
    properties:
      name:
        type: string
      namespace:
        type: string
    type: object
  k8soperation_internal_app_requests.KubeDaemonSetCreateRequest:
    properties:
      affinity:
        $ref: '#/definitions/v1.Affinity'
      container_command:
        description: 启动命令（可选）
        type: string
      container_command_args:
        description: 启动参数（可选）
        type: string
      container_image:
        description: 容器
        type: string
      cpu_requirement:
        description: 请求 CPU（可选）
        type: string
      description:
        description: 描述（可选）
        type: string
      host_network:
        type: boolean
      image_pull_secret:
        description: 单个镜像拉取密钥名（可选，兼容你原有风格）
        type: string
      image_pull_secrets:
        description: 多个镜像密钥
        items:
          $ref: '#/definitions/v1.LocalObjectReference'
        type: array
      is_create_service:
        description: 可选：是否同时创建 Service（如需要暴露端口）
        type: boolean
      is_liveness_enable:
        type: boolean
      is_readiness_enable:
        description: 探针
        type: boolean
      labels:
        description: 资源标签（会与系统关键标签合并）
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.Label'
        type: array
      liveness_probe:
        $ref: '#/definitions/k8soperation_internal_app_requests.HealthCheckDetail'
      max_unavailable:
        type: string
      memory_requirement:
        description: 请求内存（可选）
        type: string
      min_ready_seconds:
        type: integer
      name:
        description: 基本
        type: string
      namespace:
        description: 命名空间
        type: string
      node_selector:
        additionalProperties:
          type: string
        description: DaemonSet 常用：节点/污点/亲和/网络/SA/卷
        type: object
      port_mappings:
        description: 端口映射
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.PortMapping'
        type: array
      readiness_probe:
        $ref: '#/definitions/k8soperation_internal_app_requests.HealthCheckDetail'
      revision_history_limit:
        type: integer
      run_as_privileged:
        description: 是否特权
        type: boolean
      service_account:
        type: string
      service_name:
        type: string
      service_type:
        type: string
      tolerations:
        items:
          $ref: '#/definitions/v1.Toleration'
        type: array
      variables:
        description: 环境变量
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.EnvironmentVariable'
        type: array
      volumes:
        items:
          $ref: '#/definitions/v1.Volume'
        type: array
    type: object
  k8soperation_internal_app_requests.KubeDaemonSetRollbackRequest:
    properties:
      name:
        type: string
      namespace:
        type: string
      revision_name:
        description: |-
          指定回滚到的历史版本（ControllerRevision）
          建议用 revision_name（更直观），如果你喜欢数字 revision 也可以再加一个 int64 字段做二选一
        type: string
    type: object
  k8soperation_internal_app_requests.KubeDaemonSetUpdateImageRequest:
    properties:
      container:
        description: 目标容器名称
        type: string
      image:
        description: 新镜像地址，例如 nginx:1.27
        type: string
      name:
        type: string
      namespace:
        type: string
    type: object
  k8soperation_internal_app_requests.KubeDeploymentCreateRequest:
    properties:
      affinity:
        allOf:
        - $ref: '#/definitions/v1.Affinity'
        description: 亲和性配置（原生K8s格式，高级用户）
      container_command:
        description: 容器启动命令（可选）
        type: string
      container_command_args:
        description: 容器启动参数（可选）
        type: string
      container_image:
        description: 容器镜像
        type: string
      cpu_requirement:
        description: CPU 需求（可选）
        type: string
      description:
        description: 描述信息（可选）
        type: string
      image_pull_secret:
        description: 镜像拉取密钥（可选）
        type: string
      is_create_service:
        description: 是否同时创建 Service
        type: boolean
      is_liveness_enable:
        description: 是否启用 Liveness 探针
        type: boolean
      is_readiness_enable:
        description: 是否启用 Readiness 探针
        type: boolean
      labels:
        description: 标签
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.Label'
        type: array
      liveness_probe:
        allOf:
        - $ref: '#/definitions/k8soperation_internal_app_requests.HealthCheckDetail'
        description: Liveness 探针配置
      memory_requirement:
        description: 内存需求（可选）
        type: string
      name:
        description: Deployment 名称
        type: string
      namespace:
        description: 命名空间
        type: string
      node_affinity_rules:
        description: 高级调度规则（前端友好格式）
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.NodeAffinityRule'
        type: array
      node_selector:
        additionalProperties:
          type: string
        description: 节点选择器（custom 模式使用）
        type: object
      port_mappings:
        description: 容器端口映射
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.PortMapping'
        type: array
      probes:
        allOf:
        - $ref: '#/definitions/k8soperation_internal_app_requests.ProbeConfig'
        description: 探针配置（liveness/readiness/startup）
      readiness_probe:
        allOf:
        - $ref: '#/definitions/k8soperation_internal_app_requests.HealthCheckDetail'
        description: Readiness 探针配置
      replicas:
        description: 副本数
        type: integer
      resources:
        allOf:
        - $ref: '#/definitions/k8soperation_internal_app_requests.ResourceRequirements'
        description: 资源配置和探针配置（Rancher/Kuboard 风格）
      revision_history_limit:
        description: 保留的历史版本数量（可选，默认10）
        type: integer
      run_as_privileged:
        description: 是否以特权模式运行
        type: boolean
      scheduling_policy:
        allOf:
        - $ref: '#/definitions/k8soperation_internal_app_requests.SchedulingPolicy'
        description: 调度规则配置
      service_name:
        description: Service 名称
        type: string
      service_type:
        description: Service 类型
        type: string
      tolerations:
        description: 容忍配置
        items:
          $ref: '#/definitions/v1.Toleration'
        type: array
      topology_spread_configs:
        description: 拓扑分布约束（简化格式）
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.TopologySpread'
        type: array
      variables:
        description: 环境变量
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.EnvironmentVariable'
        type: array
    type: object
  k8soperation_internal_app_requests.KubeDeploymentPauseResumeRequest:
    properties:
      name:
        type: string
      namespace:
        type: string
    type: object
  k8soperation_internal_app_requests.KubeDeploymentRestartRequest:
    properties:
      name:
        type: string
      namespace:
        type: string
    type: object
  k8soperation_internal_app_requests.KubeDeploymentRollbackRequest:
    properties:
      name:
        type: string
      namespace:
        type: string
      replica_set:
        description: 指定回滚的 ReplicaSet
        type: string
    type: object
  k8soperation_internal_app_requests.KubeDeploymentRollingUpdateRequest:
    properties:
      max_surge:
        description: 最大超出副本数，如 "1" 或 "25%"
        type: string
      max_unavailable:
        description: 最大不可用副本数
        type: string
      min_ready_seconds:
        description: Pod 就绪后最少等待秒数
        type: integer
      name:
        type: string
      namespace:
        type: string
      progress_deadline_seconds:
        description: 进度截止时间
        type: integer
      revision_history_limit:
        description: 历史版本保留数
        type: integer
    type: object
  k8soperation_internal_app_requests.KubeDeploymentScaleRequest:
    properties:
      name:
        type: string
      namespace:
        type: string
      scale_num:
        description: 副本数量
        type: integer
    type: object
  k8soperation_internal_app_requests.KubeDeploymentUpdateImageRequest:
    properties:
      container:
        description: 目标容器名称
        type: string
      image:
        description: 新镜像地址，例如 nginx:1.27
        type: string
      name:
        type: string
      namespace:
        type: string
    type: object
  k8soperation_internal_app_requests.KubeDeploymentUpdateRequest:
    properties:
      content:
        description: 更新内容（一般是 YAML/JSON）
        items:
          type: integer
        type: array
      name:
        description: Deployment 名称
        type: string
      namespace:
        description: 命名空间
        type: string
    type: object
  k8soperation_internal_app_requests.KubeDeploymentUpdateResourcesRequest:
    properties:
      container:
        description: 容器名称
        type: string
      cpu_limit:
        description: CPU Limit (如 500m)
        type: string
      cpu_request:
        description: CPU Request (如 100m)
        type: string
      memory_limit:
        description: Memory Limit (如 512Mi)
        type: string
      memory_request:
        description: Memory Request (如 128Mi)
        type: string
      name:
        type: string
      namespace:
        type: string
    type: object
  k8soperation_internal_app_requests.KubeEventListRequest:
    properties:
      continue:
        type: string
      kind:
        description: Deployment/StatefulSet/DaemonSet/Pod/Node...
        type: string
      limit:
        description: 默认 50
        type: integer
      name:
        type: string
      namespace:
        description: 为空=全局
        type: string
      reason:
        type: string
      since_seconds:
        type: integer
      type:
        description: Normal | Warning
        type: string
    type: object
  k8soperation_internal_app_requests.KubeHPABatchItem:
    properties:
      max_replicas:
        type: integer
      min_replicas:
        type: integer
      name:
        type: string
      namespace:
        type: string
    type: object
  k8soperation_internal_app_requests.KubeHPABatchScaleRequest:
    properties:
      items:
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeHPABatchItem'
        type: array
    type: object
  k8soperation_internal_app_requests.KubeHPACreateRequest:
    properties:
      annotations:
        additionalProperties:
          type: string
        type: object
      cpu_target_util:
        description: 目标 CPU 利用率 % (1-100)
        type: integer
      labels:
        additionalProperties:
          type: string
        type: object
      max_replicas:
        description: 最大副本数 > min
        type: integer
      mem_target_util:
        description: 目标内存利用率 %
        type: integer
      min_replicas:
        description: 最小副本数 >=0
        type: integer
      name:
        type: string
      namespace:
        type: string
      scale_down_stab_seconds:
        description: 缩容稳定窗口（秒）
        type: integer
      scale_up_stab_seconds:
        description: 扩容稳定窗口（秒）
        type: integer
      target_api_version:
        description: 默认 apps/v1
        type: string
      target_kind:
        description: Deployment/StatefulSet
        type: string
      target_name:
        description: 目标负载名称
        type: string
    type: object
  k8soperation_internal_app_requests.KubeIngressCreateRequest:
    properties:
      annotations:
        description: 可选注解（如 rewrite、限流等）
        type: string
      ingress_class_name:
        description: 例如："nginx"、"traefik"；可为空（走默认 class）
        type: string
      labels:
        description: 可选标签
        type: string
      name:
        type: string
      namespace:
        type: string
      rules:
        description: 规则：支持多个 host
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.IngressRule'
        type: array
      tls:
        description: 可选 TLS 列表
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.IngressTLS'
        type: array
    type: object
  k8soperation_internal_app_requests.KubeJobCreateRequest:
    properties:
      active_deadline_seconds:
        description: 最长期限
        type: integer
      affinity:
        $ref: '#/definitions/v1.Affinity'
      backoff_limit:
        description: 失败重试次数
        type: integer
      completions:
        description: 需完成数
        type: integer
      container_command:
        type: string
      container_command_args:
        type: string
      container_image:
        description: 镜像（必填）
        type: string
      container_name:
        description: 容器
        type: string
      cpu_requirement:
        type: string
      description:
        description: 描述（可选）
        type: string
      image:
        type: string
      image_pull_secrets:
        items:
          $ref: '#/definitions/v1.LocalObjectReference'
        type: array
      init_containers:
        items:
          $ref: '#/definitions/v1.Container'
        type: array
      is_liveness_enable:
        type: boolean
      is_readiness_enable:
        description: 探针（Job 也可配置，长跑/有健康需求时有用）
        type: boolean
      labels:
        description: 资源标签（会与系统关键标签合并）
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.Label'
        type: array
      liveness_probe:
        $ref: '#/definitions/k8soperation_internal_app_requests.HealthCheckDetail'
      memory_requirement:
        type: string
      name:
        description: 基本
        type: string
      namespace:
        description: 命名空间
        type: string
      node_selector:
        additionalProperties:
          type: string
        type: object
      parallelism:
        description: JobSpec
        type: integer
      pod_labels: {}
      port_mappings:
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.PortMapping'
        type: array
      readiness_probe:
        $ref: '#/definitions/k8soperation_internal_app_requests.HealthCheckDetail'
      restart_policy:
        description: PodSpec 相关
        type: string
      run_as_privileged:
        type: boolean
      service_account:
        type: string
      set_explicit_selector:
        description: 若你想显式设置 selector（一般不建议，留空即可）
        type: boolean
      suspend:
        description: 创建即挂起（可选）
        type: boolean
      tolerations:
        items:
          $ref: '#/definitions/v1.Toleration'
        type: array
      ttl_seconds_after_finished:
        type: integer
      variables:
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.EnvironmentVariable'
        type: array
      volumes:
        items:
          $ref: '#/definitions/v1.Volume'
        type: array
    type: object
  k8soperation_internal_app_requests.KubeJobRestartRequest:
    properties:
      name:
        type: string
      namespace:
        type: string
    type: object
  k8soperation_internal_app_requests.KubeJobSuspendRequest:
    properties:
      name:
        type: string
      namespace:
        type: string
      suspend:
        description: true=挂起，false=恢复
        type: boolean
    type: object
  k8soperation_internal_app_requests.KubeJobUpdateImageRequest:
    properties:
      container:
        type: string
      image:
        type: string
      name:
        type: string
      namespace:
        type: string
      touch:
        description: 可选：是否打一个 annotation 强制模板变化（方便触发 controller 识别到变更）
        type: boolean
    type: object
  k8soperation_internal_app_requests.KubeMultiResourceApplyYamlRequest:
    properties:
      yaml:
        description: 多个 YAML 文档，用 --- 分隔
        type: string
    type: object
  k8soperation_internal_app_requests.KubeNamespaceCreateRequest:
    properties:
      annotations:
        type: object
      description:
        description: 描述信息（可选） -> 写入 annotation
        type: string
      labels:
        description: Labels（可选）
        type: object
      name:
        description: Namespace 名称（必填）
        type: string
      quota_cpu:
        type: string
      quota_memory:
        type: string
      quota_pods:
        type: string
    type: object
  k8soperation_internal_app_requests.KubeNamespaceLabelPatchRequest:
    properties:
      add:
        additionalProperties:
          type: string
        description: 添加/更新的标签
        type: object
      name:
        type: string
      remove:
        description: 要删除的标签 key
        items:
          type: string
        type: array
    type: object
  k8soperation_internal_app_requests.KubeNamespaceUpdateRequest:
    properties:
      content:
        description: 更新内容（一般是 Namespace 的 metadata JSON）
        items:
          type: integer
        type: array
      name:
        description: Namespace 名称（必填）
        type: string
    type: object
  k8soperation_internal_app_requests.KubeNodeCordonRequest:
    properties:
      nodeName:
        type: string
      unschedulable:
        type: boolean
    type: object
  k8soperation_internal_app_requests.KubeNodeDrainRequest:
    properties:
      deleteEmptyDir:
        description: 是否删除使用 emptyDir 的 Pod（等价于 kubectl drain 的 --delete-emptydir-data）
        type: boolean
      dryRun:
        description: 仅打印将要执行的动作，而不真正执行（DryRun 模式）
        type: boolean
      force:
        description: 是否强制驱逐（等价于 --force），比如没有 PDB / 本地存储等场景
        type: boolean
      gracePeriodSeconds:
        description: |-
          Pod 的优雅退出时间，单位秒：
            -1 = 使用 Pod 自己的 terminationGracePeriodSeconds
             0 = 立刻删除（不推荐）
           > 0 = 指定宽限期
        type: integer
      ignoreDaemonSets:
        default: true
        description: 是否忽略 DaemonSet Pod（等价于 --ignore-daemonsets）
        type: boolean
      nodeName:
        description: 要 drain 的节点名（必填）
        type: string
      podSelector:
        description: |-
          仅匹配特定 Label 的 Pod，格式与 labelSelector 相同：
            例如： "app=myapp,env=prod"
        type: string
      timeoutSeconds:
        description: 整个 drain 操作的超时时间（秒），0 表示不超时（可交给 HTTP ctx 控制）
        type: integer
    type: object
  k8soperation_internal_app_requests.KubeNodeLabelPatchRequest:
    properties:
      add:
        type: string
      name:
        type: string
      remove:
        items:
          type: string
        type: array
    type: object
  k8soperation_internal_app_requests.KubeNodeTaintPatchRequest:
    properties:
      add:
        description: 按 key+effect 覆盖/新增
        items:
          $ref: '#/definitions/v1.Taint'
        type: array
      name:
        type: string
      removeKeys:
        description: 按 key 删除（同 effect 一并移除）
        items:
          type: string
        type: array
    type: object
  k8soperation_internal_app_requests.KubePVApplyYamlRequest:
    properties:
      name:
        type: string
      yaml:
        type: string
    type: object
  k8soperation_internal_app_requests.KubePVCCreateRequest:
    properties:
      accessMode:
        description: 单个访问模式，前端发送
        type: string
      accessModes:
        description: 兼容旧版
        items:
          $ref: '#/definitions/v1.PersistentVolumeAccessMode'
        type: array
      annotations:
        type: string
      dataSource:
        allOf:
        - $ref: '#/definitions/k8soperation_internal_app_requests.DataSourceRef'
        description: 可选：从已有 PVC/快照恢复（具备能力时再放开）
      labels:
        type: string
      name:
        type: string
      namespace:
        description: 表单模式字段
        type: string
      selector:
        additionalProperties:
          type: string
        type: object
      selectorMatchLabels:
        description: 选择器：用于绑定满足标签/表达式的 PV（可选）
        type: string
      storage:
        description: e.g. "10Gi"
        type: string
      storageClassName:
        description: 可为空
        type: string
      volumeMode:
        allOf:
        - $ref: '#/definitions/v1.PersistentVolumeMode'
        description: Filesystem/Block
      yamlContent:
        description: |-
          支持两种模式：表单模式和 YAML 模式
          1. 如果有 YamlContent，则使用 YAML 创建
          2. 否则使用表单字段创建
        type: string
    type: object
  k8soperation_internal_app_requests.KubePVCResizeRequest:
    properties:
      name:
        type: string
      namespace:
        type: string
      storage:
        description: e.g. "20Gi"
        type: string
    type: object
  k8soperation_internal_app_requests.KubePVCreateFromYamlRequest:
    properties:
      yaml:
        type: string
    type: object
  k8soperation_internal_app_requests.KubePVCreateRequest:
    properties:
      accessModes:
        items:
          $ref: '#/definitions/v1.PersistentVolumeAccessMode'
        type: array
      annotations:
        type: string
      capacity:
        description: e.g. 10Gi
        type: string
      hostPath:
        description: 常见后端定义
        type: string
      labels:
        type: string
      name:
        type: string
      nfs:
        allOf:
        - $ref: '#/definitions/k8soperation_internal_app_requests.NFSVolumeConfig'
        description: 若使用 NFS，填写 Server / Path
      reclaimPolicy:
        description: Delete / Retain
        type: string
      storageClassName:
        type: string
      volumeMode:
        $ref: '#/definitions/v1.PersistentVolumeMode'
    type: object
  k8soperation_internal_app_requests.KubePVExpandRequest:
    properties:
      name:
        type: string
      newCapacity:
        description: '例如: 20Gi'
        type: string
    type: object
  k8soperation_internal_app_requests.KubePodApplyYamlRequest:
    properties:
      name:
        type: string
      namespace:
        type: string
      yaml:
        type: string
    type: object
  k8soperation_internal_app_requests.KubePodCreateRequest:
    properties:
      containerName:
        type: string
      image:
        type: string
      labels:
        additionalProperties:
          type: string
        type: object
      name:
        type: string
      namespace:
        type: string
    type: object
  k8soperation_internal_app_requests.KubePodEvictRequest:
    properties:
      namespace:
        type: string
      podName:
        type: string
    type: object
  k8soperation_internal_app_requests.KubePodLabelPatchRequest:
    properties:
      add:
        additionalProperties:
          type: string
        description: 添加/更新的标签
        type: object
      name:
        type: string
      namespace:
        type: string
      remove:
        description: 要删除的标签 key
        items:
          type: string
        type: array
    type: object
  k8soperation_internal_app_requests.KubePodUpdateRequest:
    properties:
      content:
        description: 原始 Pod JSON
        type: object
      name:
        type: string
      namespace:
        type: string
    type: object
  k8soperation_internal_app_requests.KubeSecretCreateRequest:
    properties:
      annotations:
        type: string
      basic:
        $ref: '#/definitions/k8soperation_internal_app_requests.BasicAuthFields'
      data:
        description: 通用键值（Opaque 或直接键值对）
        type: string
      docker:
        $ref: '#/definitions/k8soperation_internal_app_requests.DockerFields'
      labels:
        type: string
      mode:
        description: 推荐传 mode（更语义化），兼容 type（原生 k8s 类型字符串）
        type: string
      name:
        type: string
      namespace:
        type: string
      ssh:
        $ref: '#/definitions/k8soperation_internal_app_requests.SSHAuthFields'
      string_data:
        type: string
      tls:
        allOf:
        - $ref: '#/definitions/k8soperation_internal_app_requests.TLSFields'
        description: 专用结构体
      type:
        type: string
    type: object
  k8soperation_internal_app_requests.KubeSecretDecodeRequest:
    properties:
      data:
        additionalProperties:
          type: string
        description: '批量解码，例如 {"username": "YWRtaW4=", "password": "MTIzNDU2"}'
        type: object
      value:
        description: 单个字符串解码，例如 "YWRtaW4="
        type: string
    type: object
  k8soperation_internal_app_requests.KubeServiceCreateRequest:
    properties:
      annotations:
        type: string
      cluster_ip:
        description: 可选项
        type: string
      external_traffic_policy:
        description: Local / Cluster（LB/NodePort 常用）
        type: string
      name:
        type: string
      namespace:
        description: 命名空间 & 名称
        type: string
      ports:
        description: 端口规则
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.ServicePortRule'
        type: array
      selector:
        type: string
      selector_labels:
        description: 选择器：建议用 label 列表；也支持传 map（Swagger 里以字符串展示）
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.Label'
        type: array
      session_affinity:
        description: None / ClientIP
        type: string
      type:
        description: |-
          Service 类型：ClusterIP / NodePort / LoadBalancer / Headless
          Headless 时 cluster_ip 建议传 "None"
        type: string
    type: object
  k8soperation_internal_app_requests.KubeStatefulSetCreateRequest:
    properties:
      container_command:
        description: 容器启动命令
        type: string
      container_command_args:
        description: 容器启动参数
        type: string
      container_image:
        description: 主容器镜像
        type: string
      cpu_requirement:
        description: CPU 需求（可选）
        type: string
      description:
        description: 描述信息（可选）
        type: string
      image_pull_secret:
        description: 镜像/启动相关（可选）
        type: string
      is_create_service:
        description: Service 相关（StatefulSet 必须绑定一个服务名，通常为 Headless Service）
        type: boolean
      is_liveness_enable:
        description: 是否启用 Liveness
        type: boolean
      is_readiness_enable:
        description: 是否启用 Readiness
        type: boolean
      labels:
        description: 标签
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.Label'
        type: array
      liveness_probe:
        allOf:
        - $ref: '#/definitions/k8soperation_internal_app_requests.HealthCheckDetail'
        description: Liveness 配置
      memory_requirement:
        description: 资源/标签/环境变量/端口
        type: string
      name:
        description: 基本信息
        type: string
      namespace:
        description: 命名空间
        type: string
      port_mappings:
        description: 端口映射
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.PortMapping'
        type: array
      readiness_probe:
        allOf:
        - $ref: '#/definitions/k8soperation_internal_app_requests.HealthCheckDetail'
        description: Readiness 配置
      replicas:
        description: 副本数
        type: integer
      run_as_privileged:
        description: 探针/特权
        type: boolean
      service_name:
        description: 必填：绑定的 Service 名（Headless 推荐）
        type: string
      service_type:
        description: Service 类型（ClusterIP/NodePort/...；Headless 时忽略）
        type: string
      update_partition:
        description: 滚动更新策略（可选）
        type: integer
      variables:
        description: 环境变量
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.EnvironmentVariable'
        type: array
      volume_claim_templates:
        description: 每个 Pod 的独立 PVC（StatefulSet 常用）
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.VolumeClaimTemplate'
        type: array
    type: object
  k8soperation_internal_app_requests.KubeStatefulSetRestartRequest:
    properties:
      name:
        type: string
      namespace:
        type: string
    type: object
  k8soperation_internal_app_requests.KubeStatefulSetRollbackRequest:
    properties:
      name:
        type: string
      namespace:
        type: string
      revision_name:
        description: 指定回滚到的历史版本（ControllerRevision）
        type: string
    type: object
  k8soperation_internal_app_requests.KubeStatefulSetScaleRequest:
    properties:
      name:
        type: string
      namespace:
        type: string
      scale_num:
        description: 副本数量
        type: integer
    type: object
  k8soperation_internal_app_requests.KubeStatefulSetUpdateImageRequest:
    properties:
      container:
        description: 目标容器名称
        type: string
      image:
        description: 新镜像地址，例如 mysql:8.0
        type: string
      name:
        type: string
      namespace:
        type: string
    type: object
  k8soperation_internal_app_requests.KubeStorageClassCreateRequest:
    properties:
      allowVolumeExpansion:
        type: boolean
      mountOptions:
        items:
          type: string
        type: array
      name:
        type: string
      parameters:
        type: string
      provisioner:
        type: string
      reclaimPolicy:
        description: Delete / Retain
        type: string
      volumeBindingMode:
        description: Immediate / WaitForFirstConsumer
        type: string
    type: object
  k8soperation_internal_app_requests.Label:
    properties:
      key:
        type: string
      value:
        type: string
    type: object
  k8soperation_internal_app_requests.MultiResourceCreateResult:
    properties:
      created:
        description: 成功创建的资源
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.CreatedResource'
        type: array
      failed:
        description: 创建失败的资源
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.FailedResource'
        type: array
      total:
        description: 总资源数
        type: integer
    type: object
  k8soperation_internal_app_requests.MultiResourceParsedResult:
    properties:
      errors:
        items:
          type: string
        type: array
      resources:
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.ParsedResource'
        type: array
      total:
        type: integer
    type: object
  k8soperation_internal_app_requests.NFSVolumeConfig:
    properties:
      path:
        type: string
      readOnly:
        type: boolean
      server:
        type: string
    type: object
  k8soperation_internal_app_requests.NodeAffinityRule:
    properties:
      key:
        description: 节点标签键
        type: string
      operator:
        description: '操作符: In, NotIn, Exists, DoesNotExist, Gt, Lt'
        type: string
      required:
        description: true=硬性要求, false=软性偏好
        type: boolean
      values:
        description: 值（逗号分隔）
        type: string
      weight:
        description: 软性偏好权重 (1-100)
        type: integer
    type: object
  k8soperation_internal_app_requests.ParsedResource:
    properties:
      apiVersion:
        description: API 版本
        type: string
      content:
        description: 原始 YAML 内容
        type: string
      dependsOn:
        description: 依赖的资源
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.ResourceRef'
        type: array
      index:
        description: 在原始 YAML 中的索引
        type: integer
      kind:
        description: 资源类型 (Deployment, Service, ConfigMap 等)
        type: string
      name:
        description: 资源名称
        type: string
      namespace:
        description: 命名空间
        type: string
      order:
        description: 创建顺序 (数值越小优先级越高)
        type: integer
      warnings:
        description: 警告信息
        items:
          type: string
        type: array
    type: object
  k8soperation_internal_app_requests.PatchPodImageRequest:
    properties:
      container:
        type: string
      name:
        type: string
      namespace:
        type: string
      new_image:
        type: string
    type: object
  k8soperation_internal_app_requests.PipelineBatchCreateRequest:
    properties:
      pipelines:
        description: 流水线列表
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.PipelineBatchItem'
        type: array
      skip_existing:
        description: 跳过已存在的（按名称判重），否则报错
        type: boolean
    type: object
  k8soperation_internal_app_requests.PipelineBatchItem:
    properties:
      auto_deploy:
        description: 自动部署配置（可选）
        type: boolean
      deploy_env:
        type: string
      description:
        description: 可选：描述
        type: string
      enable_artifact_upload:
        type: boolean
      enable_build_cache:
        description: 镜像构建缓存，未传时默认开启
        type: boolean
      enable_deploy_silence:
        description: 发布联动告警静默
        type: boolean
      enable_sonar:
        type: boolean
      env_vars:
        description: 可选：环境变量
        items:
          $ref: '#/definitions/k8soperation_internal_app_models.EnvVar'
        type: array
      git_branch:
        description: 可选：分支，留空用配置默认值
        type: string
      git_repo:
        description: 必填：Git 仓库地址
        type: string
      language_type:
        description: 推荐：go/java/frontend/python/custom
        type: string
      name:
        description: 必填：流水线名称
        type: string
      require_approval:
        type: boolean
      silence_buffer_minutes:
        type: integer
      silence_severities:
        type: string
      target_cluster_id:
        type: integer
      target_container:
        type: string
      target_namespace:
        type: string
      target_workload_kind:
        type: string
      target_workload_name:
        type: string
    type: object
  k8soperation_internal_app_requests.PipelineBatchRunRequest:
    properties:
      ids:
        description: 流水线ID列表
        items:
          type: integer
        type: array
    type: object
  k8soperation_internal_app_requests.PipelineBatchStopRequest:
    properties:
      ids:
        description: 流水线ID列表
        items:
          type: integer
        type: array
    type: object
  k8soperation_internal_app_requests.PipelineCallbackRequest:
    properties:
      build_number:
        description: 构建号
        type: integer
      duration:
        description: 额外信息
        type: integer
      image:
        description: 构建产物 - 支持 image 或 image_url
        type: string
      image_digest:
        description: 镜像 digest (e.g., sha256:xxx)
        type: string
      image_url:
        description: 构建产出的镜像地址（兼容旧字段）
        type: string
      job_name:
        description: 必须字段
        type: string
      message:
        description: 错误或补充信息
        type: string
      pipeline_id:
        description: 平台关联字段
        type: integer
      request_id:
        description: 请求ID（用于日志追踪）
        type: string
      run_id:
        description: 运行记录ID（精确匹配，避免 build_number 重复问题）
        type: integer
      status:
        description: SUCCESS / FAILURE / ABORTED
        type: string
    type: object
  k8soperation_internal_app_requests.PipelineCreateRequest:
    properties:
      auto_deploy:
        description: 部署配置
        type: boolean
      deploy_config:
        additionalProperties: {}
        type: object
      deploy_env:
        description: 部署环境
        type: string
      deploy_mode:
        description: GitOps 部署模式配置
        type: string
      description:
        type: string
      enable_artifact_upload:
        description: 是否启用制品上传
        type: boolean
      enable_build_cache:
        description: 是否启用镜像构建缓存（Kaniko --cache）。用指针以区分"未传"与"显式关闭"，未传时默认开启
        type: boolean
      enable_deploy_silence:
        description: 发布联动告警静默
        type: boolean
      enable_sonar:
        description: 是否启用 SonarQube
        type: boolean
      env_vars:
        items:
          $ref: '#/definitions/k8soperation_internal_app_models.EnvVar'
        type: array
      environment_id:
        description: 关联环境ID（>0 时命名空间/集群/审批以环境为准）
        type: integer
      git_branch:
        type: string
      git_repo:
        type: string
      gitops_config:
        allOf:
        - $ref: '#/definitions/k8soperation_internal_app_models.GitOpsConfig'
        description: GitOps 配置（DeployMode=gitops 时必填）
      jenkins_job:
        type: string
      jenkins_url:
        type: string
      language_type:
        description: go/java/frontend/python/custom
        type: string
      name:
        type: string
      require_approval:
        description: 是否需要审批
        type: boolean
      silence_buffer_minutes:
        description: 静默缓冲时间(分钟)
        type: integer
      silence_severities:
        description: 静默的告警级别
        type: string
      target_cluster_id:
        description: 目标集群ID
        type: integer
      target_container:
        description: 容器名称
        type: string
      target_namespace:
        description: 目标命名空间
        type: string
      target_workload_kind:
        description: 工作负载类型
        type: string
      target_workload_name:
        description: 工作负载名称
        type: string
    type: object
  k8soperation_internal_app_requests.PipelineIDRequest:
    properties:
      id:
        type: integer
    type: object
  k8soperation_internal_app_requests.PipelinePromoteRequest:
    properties:
      image_digest:
        description: 显式镜像digest
        type: string
      image_repo:
        description: 显式镜像仓库
        type: string
      image_tag:
        description: 显式镜像tag
        type: string
      pipeline_id:
        description: 流水线ID
        type: integer
      reason:
        description: 晋级说明
        type: string
      source_release_id:
        description: 已有发布单ID（从其它环境晋级）
        type: integer
      source_run_id:
        description: 镜像来源（三选一，优先级：显式镜像 > source_release_id > source_run_id）
        type: integer
      target_env:
        description: 目标环境(dev/test/staging/prod)
        type: string
    type: object
  k8soperation_internal_app_requests.PipelineRunRequest:
    properties:
      auto_deploy:
        description: 运行时部署配置（可覆盖流水线默认配置）
        type: boolean
      branch:
        description: 可选：覆盖默认分支
        type: string
      deploy_env:
        description: 目标环境：dev/test/staging/prod
        type: string
      env_vars:
        additionalProperties:
          type: string
        description: 可选：覆盖环境变量
        type: object
      force:
        description: 强制运行：自动清理旧的失败/运行中构建
        type: boolean
      id:
        type: integer
      replicas:
        description: 目标副本数
        type: integer
      strategy:
        description: 发布策略（发布中心透传，记录发布意图到 deploy_config，可选）
        type: string
      target_cluster_id:
        description: 目标集群ID
        type: integer
      target_container:
        description: 容器名称
        type: string
      target_namespace:
        description: 目标命名空间
        type: string
      target_workload_kind:
        description: 工作负载类型
        type: string
      target_workload_name:
        description: 工作负载名称
        type: string
    type: object
  k8soperation_internal_app_requests.PipelineStopRequest:
    properties:
      build_number:
        description: 可选：指定构建号，不传则停止最新的
        type: integer
      id:
        type: integer
    type: object
  k8soperation_internal_app_requests.PipelineTargetDeleteRequest:
    properties:
      id:
        type: integer
    type: object
  k8soperation_internal_app_requests.PipelineTargetItem:
    properties:
      auto_deploy:
        description: 是否自动部署
        type: boolean
      cluster_id:
        description: 目标集群ID
        type: integer
      container:
        description: 容器名称
        type: string
      env:
        description: 环境标识(dev/test/staging/prod)
        type: string
      namespace:
        description: 目标命名空间
        type: string
      promote_from:
        description: 上游来源环境
        type: string
      require_approval:
        description: 是否需要审批
        type: boolean
      sort_order:
        description: 顺序
        type: integer
      workload_kind:
        description: 工作负载类型
        type: string
      workload_name:
        description: 工作负载名称
        type: string
    type: object
  k8soperation_internal_app_requests.PipelineTargetSaveRequest:
    properties:
      pipeline_id:
        type: integer
      targets:
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.PipelineTargetItem'
        type: array
    type: object
  k8soperation_internal_app_requests.PipelineUpdateRequest:
    properties:
      auto_deploy:
        description: 部署配置
        type: boolean
      deploy_config:
        additionalProperties: {}
        type: object
      deploy_env:
        description: 部署环境
        type: string
      description:
        type: string
      enable_artifact_upload:
        description: 是否启用制品上传
        type: boolean
      enable_build_cache:
        description: 是否启用镜像构建缓存（Kaniko --cache）
        type: boolean
      enable_deploy_silence:
        description: 发布联动告警静默
        type: boolean
      enable_sonar:
        description: 是否启用 SonarQube
        type: boolean
      env_vars:
        items:
          $ref: '#/definitions/k8soperation_internal_app_models.EnvVar'
        type: array
      environment_id:
        description: 关联环境ID（>0 时命名空间/集群/审批以环境为准，=0 解除绑定）
        type: integer
      git_branch:
        type: string
      git_repo:
        type: string
      id:
        type: integer
      jenkins_job:
        type: string
      jenkins_url:
        type: string
      language_type:
        description: go/java/frontend/python/custom
        type: string
      name:
        type: string
      require_approval:
        description: 是否需要审批
        type: boolean
      silence_buffer_minutes:
        description: 静默缓冲时间(分钟)
        type: integer
      silence_severities:
        description: 静默的告警级别
        type: string
      status:
        type: string
      target_cluster_id:
        description: 目标集群ID
        type: integer
      target_container:
        description: 容器名称
        type: string
      target_namespace:
        description: 目标命名空间
        type: string
      target_workload_kind:
        description: 工作负载类型
        type: string
      target_workload_name:
        description: 工作负载名称
        type: string
    type: object
  k8soperation_internal_app_requests.PortDef:
    properties:
      name:
        type: string
      node_port:
        description: NodePort Service 的宿主机端口（30000-32767），为 0 时自动分配
        type: integer
      port:
        type: integer
      protocol:
        description: TCP/UDP，默认 TCP
        type: string
      target_port:
        description: Service 目标端口（映射到 Pod 的容器端口），为 0 时默认等于 Port
        type: integer
    type: object
  k8soperation_internal_app_requests.PortMapping:
    properties:
      port:
        type: integer
      protocol:
        description: TCP/UDP/SCTP
        type: string
      target_port:
        type: integer
    type: object
  k8soperation_internal_app_requests.ProbeConfig:
    properties:
      enable_liveness:
        description: Liveness Probe - 存活探针
        type: boolean
      enable_readiness:
        description: Readiness Probe - 就绪探针
        type: boolean
      enable_startup:
        description: Startup Probe - 启动探针
        type: boolean
      liveness_probe:
        $ref: '#/definitions/k8soperation_internal_app_requests.HealthCheckDetail'
      readiness_probe:
        $ref: '#/definitions/k8soperation_internal_app_requests.HealthCheckDetail'
      startup_probe:
        $ref: '#/definitions/k8soperation_internal_app_requests.HealthCheckDetail'
    type: object
  k8soperation_internal_app_requests.QuickOnboardRequest:
    properties:
      app_name:
        description: '--- 基础信息 ---'
        type: string
      auto_deploy:
        description: '--- 可选：是否立即部署 ---'
        type: boolean
      cluster_id:
        description: '--- 集群 ---'
        type: integer
      configmap_data:
        description: '--- ConfigMap ---'
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.KVDef'
        type: array
      configmap_mount_path:
        description: 如 /etc/config
        type: string
      container_name:
        type: string
      cpu_lim:
        description: 如 "500m"
        type: string
      cpu_req:
        description: '--- 资源配置 ---'
        type: string
      cron_concurrency_policy:
        description: Allow/Forbid/Replace
        type: string
      cron_schedule:
        description: '--- CronJob 专属 ---'
        type: string
      env_vars:
        description: '--- 环境变量 ---'
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.EnvVarDef'
        type: array
      git_branch:
        type: string
      git_repo:
        description: '--- 可选：Git 仓库（用于创建流水线）---'
        type: string
      image:
        description: '--- 镜像 ---'
        type: string
      job_backoff_limit:
        description: 重试次数
        type: integer
      job_completions:
        description: '--- Job 专属 ---'
        type: integer
      job_parallelism:
        description: 并行度
        type: integer
      job_ttl_seconds_after_finished:
        description: 完成后保留时间
        type: integer
      mem_lim:
        description: 如 "256Mi"
        type: string
      mem_req:
        description: 如 "128Mi"
        type: string
      namespace:
        type: string
      ports:
        description: '--- 端口 ---'
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.PortDef'
        type: array
      pvc_access_mode:
        description: ReadWriteOnce/ReadWriteMany/ReadOnlyMany
        type: string
      pvc_mount_path:
        description: 如 /data
        type: string
      pvc_name:
        description: '--- PVC 存储 ---'
        type: string
      pvc_size:
        description: 如 "10Gi"
        type: string
      pvc_storage_class:
        description: storage class 名称
        type: string
      replicas:
        description: '--- 副本数（Deployment/StatefulSet 有效）---'
        type: integer
      secret_data:
        description: '--- Secret ---'
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.KVDef'
        type: array
      secret_mount_path:
        description: 如 /etc/secrets
        type: string
      service_ports:
        description: 可选，默认用 ports
        items:
          $ref: '#/definitions/k8soperation_internal_app_requests.PortDef'
        type: array
      service_type:
        description: '--- Service 配置 ---'
        type: string
      workload_kind:
        description: Deployment|StatefulSet|DaemonSet|CronJob|Job
        type: string
      workload_name:
        description: 可选，默认=app_name
        type: string
    type: object
  k8soperation_internal_app_requests.ResourceRef:
    properties:
      fieldPath:
        description: 引用的字段路径
        type: string
      kind:
        type: string
      name:
        type: string
      namespace:
        type: string
    type: object
  k8soperation_internal_app_requests.ResourceRequirements:
    properties:
      cpu_limit:
        description: CPU 限制
        type: string
      cpu_request:
        description: CPU 请求，如 100m, 0.5, 1
        type: string
      memory_limit:
        description: 内存限制
        type: string
      memory_request:
        description: 内存请求，如 64Mi, 128Mi, 1Gi
        type: string
    type: object
  k8soperation_internal_app_requests.ResourceValidateRequest:
    properties:
      config:
        allOf:
        - $ref: '#/definitions/k8soperation_internal_app_models.ResourceConfig'
        description: 资源配置
      env:
        description: dev/test/staging/prod
        type: string
      service_type:
        description: java/go/node/python
        type: string
    required:
    - config
    - env
    - service_type
    type: object
  k8soperation_internal_app_requests.RoleCreateRequest:
    properties:
      color:
        type: string
      description:
        type: string
      display_name:
        type: string
      icon:
        type: string
      name:
        type: string
      role_type:
        type: string
      scope_cicd:
        description: none/read/write/admin
        type: string
      scope_cluster:
        description: none/read/write/admin
        type: string
      scope_platform:
        description: none/read/write/admin
        type: string
    type: object
  k8soperation_internal_app_requests.RolePermissionsUpdateRequest:
    properties:
      permission_ids:
        items:
          type: integer
        type: array
      role_id:
        type: integer
    type: object
  k8soperation_internal_app_requests.RoleUpdateRequest:
    properties:
      color:
        type: string
      description:
        type: string
      display_name:
        type: string
      icon:
        type: string
      id:
        type: integer
      scope_cicd:
        type: string
      scope_cluster:
        type: string
      scope_platform:
        description: none/read/write/admin
        type: string
      sort_order:
        type: integer
    type: object
  k8soperation_internal_app_requests.SSHAuthFields:
    properties:
      known_hosts:
        type: string
      private_key:
        type: string
    type: object
  k8soperation_internal_app_requests.SchedulingPolicy:
    enum:
    - default
    - spread
    - pack
    - custom
    type: string
    x-enum-varnames:
    - SchedulingPolicyDefault
    - SchedulingPolicySpread
    - SchedulingPolicyPack
    - SchedulingPolicyCustom
  k8soperation_internal_app_requests.ServicePortRule:
    properties:
      app_protocol:
        description: App 协议（可选）
        type: string
      name:
        description: 端口名，可选
        type: string
      node_port:
        description: NodePort（仅在 NodePort/LoadBalancer 类型下生效），可选
        type: integer
      port:
        description: Service 暴露端口（1~65535）
        type: integer
      protocol:
        description: 协议：TCP/UDP/SCTP
        type: string
      target_port:
        description: 指向 Pod 的容器端口；为兼容命名端口，这里用字符串（Swagger 友好）
        type: string
    type: object
  k8soperation_internal_app_requests.StageApproveRequest:
    properties:
      action:
        description: approve/reject
        type: string
      comment:
        description: 审批意见
        type: string
      stage_id:
        type: integer
    type: object
  k8soperation_internal_app_requests.StageCallbackRequest:
    properties:
      build_number:
        description: 构建号
        type: integer
      job_name:
        description: Jenkins Job 名称
        type: string
      pipeline_id:
        description: 流水线ID
        type: integer
      run_id:
        description: 运行记录ID（优先精确匹配，避免 build_number 重用/多流水线共用 Job 定位到旧记录）
        type: integer
      stage_type:
        description: '阶段类型: checkout/dependencies/compile/test/lint/build/push/approval/deploy'
        type: string
      status:
        description: '阶段状态: running/success/failed/waiting'
        type: string
    type: object
  k8soperation_internal_app_requests.StageDeployRequest:
    properties:
      cluster_id:
        description: 可覆盖默认集群
        type: integer
      container:
        description: 可覆盖默认容器
        type: string
      image:
        description: 可覆盖默认镜像
        type: string
      namespace:
        description: 可覆盖默认命名空间
        type: string
      stage_id:
        type: integer
      workload_kind:
        description: 可覆盖默认工作负载类型
        type: string
      workload_name:
        description: 可覆盖默认工作负载名称
        type: string
    type: object
  k8soperation_internal_app_requests.TLSFields:
    properties:
      cert:
        description: -> tls.crt
        type: string
      key:
        description: -> tls.key
        type: string
    type: object
  k8soperation_internal_app_requests.TemplateCreateRequest:
    properties:
      default_env_vars:
        items:
          additionalProperties: {}
          type: object
        type: array
      deploy_config:
        additionalProperties: {}
        type: object
      description:
        type: string
      jenkins_template:
        type: string
      name:
        type: string
      stages:
        items:
          additionalProperties: {}
          type: object
        type: array
      type:
        type: string
    type: object
  k8soperation_internal_app_requests.TemplateIDRequest:
    properties:
      id:
        type: integer
    type: object
  k8soperation_internal_app_requests.TemplateUpdateRequest:
    properties:
      default_env_vars:
        items:
          additionalProperties: {}
          type: object
        type: array
      deploy_config:
        additionalProperties: {}
        type: object
      description:
        type: string
      id:
        type: integer
      jenkins_template:
        type: string
      name:
        type: string
      stages:
        items:
          additionalProperties: {}
          type: object
        type: array
      type:
        type: string
    type: object
  k8soperation_internal_app_requests.TopologySpread:
    properties:
      label_selector:
        description: 标签选择器 (key=value)
        type: string
      max_skew:
        description: 最大偏差值
        type: integer
      topology_key:
        description: 拓扑域键，如 kubernetes.io/hostname, topology.kubernetes.io/zone
        type: string
      when_unsatisfiable:
        description: DoNotSchedule 或 ScheduleAnyway
        type: string
    type: object
  k8soperation_internal_app_requests.UserCreateRequest:
    properties:
      password:
        type: string
      password_confirm:
        type: string
      tenant_id:
        type: integer
      username:
        type: string
    type: object
  k8soperation_internal_app_requests.UserRoleAssignRequest:
    properties:
      role_ids:
        items:
          type: integer
        type: array
      user_id:
        type: integer
    type: object
  k8soperation_internal_app_requests.UserUpdateRequest:
    properties:
      id:
        type: integer
      password:
        type: string
      role:
        type: string
      status:
        type: integer
      tenant_id:
        type: integer
      username:
        type: string
    type: object
  k8soperation_internal_app_requests.VolumeClaimTemplate:
    properties:
      access_mode:
        description: 例如 ReadWriteOnce
        type: string
      mount_path:
        description: 容器内挂载路径
        type: string
      name:
        description: PVC 名称（与 volumeMounts 对应）
        type: string
      storage_class:
        description: 存储类
        type: string
      storage_size:
        description: 例如 "5Gi"
        type: string
    type: object
  k8soperation_internal_app_requests.YamlApplyRequest:
    properties:
      yaml:
        description: YAML 内容
        type: string
    required:
    - yaml
    type: object
  k8soperation_internal_app_requests.YamlCreateRequest:
    properties:
      yaml:
        description: YAML 内容
        type: string
    required:
    - yaml
    type: object
  k8soperation_internal_app_services.AIChatRequest:
    properties:
      conversation_id:
        description: 可选，0 = 新建会话
        type: integer
      message:
        type: string
      model_id:
        description: 可选，指定模型
        type: string
      provider_id:
        description: 可选，指定 AI 提供商
        type: string
    type: object
  k8soperation_internal_app_services.AIChatResponse:
    properties:
      approval_id:
        type: integer
      context_round:
        description: 当前对话轮次（本次对话是第几轮）
        type: integer
      conversation_id:
        type: integer
      history_count:
        description: 本次请求携带的历史消息数（不含本次）
        type: integer
      need_approval:
        type: boolean
      pending_tools:
        description: 等待审批的工具
        items:
          $ref: '#/definitions/k8soperation_internal_app_services.PendingToolInfo'
        type: array
      reply:
        type: string
      tools_called:
        description: 本次对话调用了哪些工具
        items:
          type: string
        type: array
    type: object
  k8soperation_internal_app_services.CleanupPolicyRequest:
    properties:
      cron_expression:
        type: string
      description:
        type: string
      enabled:
        type: boolean
      id:
        type: integer
      keep_days:
        type: integer
      keep_last_count:
        type: integer
      name:
        type: string
      registry_id:
        type: integer
      repository_pattern:
        type: string
      tag_pattern:
        type: string
    required:
    - name
    - registry_id
    type: object
  k8soperation_internal_app_services.LDAPConfigResponse:
    properties:
      attr_email:
        type: string
      attr_phone:
        type: string
      attr_username:
        type: string
      auto_create:
        type: boolean
      base_dn:
        type: string
      bind_dn:
        type: string
      enabled:
        type: boolean
      group_attr:
        type: string
      group_base_dn:
        type: string
      group_filter:
        type: string
      group_role_mapping:
        items:
          $ref: '#/definitions/k8soperation_pkg_setting.LDAPGroupRoleMapping'
        type: array
      host:
        type: string
      local_fallback:
        type: boolean
      port:
        type: integer
      sync_on_login:
        type: boolean
      use_tls:
        type: boolean
      user_filter:
        type: string
    type: object
  k8soperation_internal_app_services.LDAPSyncResult:
    properties:
      errors:
        items:
          type: string
        type: array
      failed:
        type: integer
      synced:
        type: integer
      total:
        type: integer
    type: object
  k8soperation_internal_app_services.PendingToolInfo:
    properties:
      approval_id:
        type: integer
      cluster_id:
        type: integer
      namespace:
        type: string
      resource_name:
        type: string
      risk_level:
        type: string
      summary:
        type: string
      tool_args:
        additionalProperties: true
        description: 完整参数
        type: object
      tool_name:
        type: string
    type: object
  k8soperation_internal_app_services.RegistryCreateRequest:
    properties:
      access_key_id:
        type: string
      access_key_secret:
        type: string
      description:
        type: string
      insecure:
        type: boolean
      is_default:
        type: boolean
      name:
        maxLength: 100
        minLength: 1
        type: string
      password:
        type: string
      region:
        type: string
      type:
        enum:
        - docker
        - harbor
        - gcr
        - ecr
        - acr
        - quay
        type: string
      url:
        type: string
      username:
        type: string
    required:
    - name
    - type
    - url
    type: object
  k8soperation_internal_app_services.RegistryUpdateRequest:
    properties:
      access_key_id:
        type: string
      access_key_secret:
        type: string
      description:
        type: string
      id:
        type: integer
      insecure:
        type: boolean
      is_default:
        type: boolean
      name:
        maxLength: 100
        minLength: 1
        type: string
      password:
        type: string
      region:
        type: string
      type:
        enum:
        - docker
        - harbor
        - gcr
        - ecr
        - acr
        - quay
        type: string
      url:
        type: string
      username:
        type: string
    required:
    - id
    - name
    - type
    - url
    type: object
  k8soperation_internal_domain_audit.ActionSummary:
    properties:
      action:
        type: string
      count:
        type: integer
    type: object
  k8soperation_internal_domain_audit.AuditLog:
    properties:
      action:
        type: string
      action_display:
        type: string
      cluster_id:
        type: integer
      cluster_name:
        type: string
      created_at:
        type: integer
      detail:
        type: string
      duration_ms:
        type: integer
      error_message:
        type: string
      extra:
        type: string
      id:
        type: integer
      module:
        type: string
      namespace:
        type: string
      pipeline_id:
        type: integer
      pipeline_name:
        type: string
      project_id:
        type: integer
      project_name:
        type: string
      request_body:
        type: string
      request_method:
        type: string
      request_uri:
        type: string
      response_code:
        type: integer
      response_message:
        type: string
      status:
        type: string
      target_id:
        type: string
      target_name:
        type: string
      target_type:
        type: string
      user_agent:
        type: string
      user_id:
        type: integer
      user_ip:
        type: string
      username:
        type: string
    type: object
  k8soperation_internal_domain_audit.AuditLogListResponse:
    properties:
      list:
        items:
          $ref: '#/definitions/k8soperation_internal_domain_audit.AuditLog'
        type: array
      page:
        type: integer
      page_size:
        type: integer
      statistics:
        $ref: '#/definitions/k8soperation_internal_domain_audit.AuditStatistics'
      total:
        type: integer
    type: object
  k8soperation_internal_domain_audit.AuditRetentionPolicy:
    properties:
      is_permanent:
        type: boolean
      retention_days:
        type: integer
    type: object
  k8soperation_internal_domain_audit.AuditStatistics:
    properties:
      action_summary:
        items:
          $ref: '#/definitions/k8soperation_internal_domain_audit.ActionSummary'
        type: array
      hourly_counts:
        items:
          $ref: '#/definitions/k8soperation_internal_domain_audit.HourlyCount'
        type: array
      success_rate:
        type: number
      top_modules:
        items:
          $ref: '#/definitions/k8soperation_internal_domain_audit.TopModuleStat'
        type: array
      top_users:
        items:
          $ref: '#/definitions/k8soperation_internal_domain_audit.TopUserStat'
        type: array
      total_all:
        type: integer
      total_today:
        type: integer
      total_week:
        type: integer
    type: object
  k8soperation_internal_domain_audit.HourlyCount:
    properties:
      count:
        type: integer
      hour:
        type: integer
    type: object
  k8soperation_internal_domain_audit.TopModuleStat:
    properties:
      count:
        type: integer
      module:
        type: string
    type: object
  k8soperation_internal_domain_audit.TopUserStat:
    properties:
      count:
        type: integer
      username:
        type: string
    type: object
  k8soperation_internal_domain_cicd.HPAConfig:
    properties:
      cpu_target:
        type: integer
      enabled:
        type: boolean
      max_replicas:
        type: integer
      min_replicas:
        type: integer
    type: object
  k8soperation_internal_domain_cicd.ResourceRequests:
    properties:
      limits:
        $ref: '#/definitions/k8soperation_internal_domain_cicd.ResourceValues'
      requests:
        $ref: '#/definitions/k8soperation_internal_domain_cicd.ResourceValues'
    type: object
  k8soperation_internal_domain_cicd.ResourceValues:
    properties:
      cpu:
        type: string
      memory:
        type: string
    type: object
  k8soperation_internal_domain_cicd.RiskLevel:
    enum:
    - low
    - medium
    - high
    type: string
    x-enum-varnames:
    - RiskLevelLow
    - RiskLevelMedium
    - RiskLevelHigh
  k8soperation_internal_domain_settings.AboutSettings:
    properties:
      build_date:
        type: string
      db_type:
        type: string
      go_version:
        type: string
      k8s_support:
        type: string
      version:
        type: string
      vue_version:
        type: string
    type: object
  k8soperation_internal_domain_settings.AlertSettings:
    properties:
      alert_silence:
        type: integer
      cpu_threshold:
        type: integer
      disk_threshold:
        type: integer
      mem_threshold:
        type: integer
    type: object
  k8soperation_internal_domain_settings.BasicSettings:
    properties:
      default_cluster:
        type: string
      default_page:
        type: string
      language:
        type: string
      timezone:
        type: string
    type: object
  k8soperation_internal_domain_settings.NotificationSettings:
    properties:
      dingtalk_webhook:
        type: string
      enable_dingtalk:
        type: boolean
      enable_email:
        type: boolean
      enable_webhook:
        type: boolean
      smtp_server:
        type: string
      webhook_url:
        type: string
    type: object
  k8soperation_internal_domain_settings.PlatformSettingsResponse:
    properties:
      about:
        $ref: '#/definitions/k8soperation_internal_domain_settings.AboutSettings'
      alert:
        $ref: '#/definitions/k8soperation_internal_domain_settings.AlertSettings'
      basic:
        $ref: '#/definitions/k8soperation_internal_domain_settings.BasicSettings'
      notification:
        $ref: '#/definitions/k8soperation_internal_domain_settings.NotificationSettings'
      security:
        $ref: '#/definitions/k8soperation_internal_domain_settings.SecuritySettings'
    type: object
  k8soperation_internal_domain_settings.SecuritySettings:
    properties:
      audit_retention:
        type: integer
      enable_2fa:
        type: boolean
      password_policy:
        type: string
      session_timeout:
        type: integer
    type: object
  k8soperation_pkg_app_response.Response:
    type: object
  k8soperation_pkg_app_response.SuccessResponse:
    description: 通用成功响应
    properties:
      code:
        example: 0
        type: integer
      data: {}
      msg:
        example: OK
        type: string
    type: object
  k8soperation_pkg_license.Status:
    properties:
      edition:
        description: 版本
        type: string
      expire_at:
        description: 到期时间（unix 秒）
        type: integer
      issued_at:
        description: 签发时间（unix 秒）
        type: integer
      licensed:
        description: 是否已授权且有效
        type: boolean
      licensee:
        description: 被授权方
        type: string
      machine_id:
        description: 本机机器码（用于向供应商申请 License）
        type: string
      reason:
        description: 未授权原因（未激活/已过期/机器码不匹配等）
        type: string
    type: object
  k8soperation_pkg_setting.LDAPGroupRoleMapping:
    properties:
      cicdaccess:
        description: '发布域权限级别: none/read/write/admin'
        type: string
      clusterAccess:
        description: '集群域权限级别: none/read/write/admin'
        type: string
      ldapgroup:
        description: LDAP 组名（cn 值）
        type: string
      platformRole:
        description: 平台角色标识
        type: string
    type: object
  resource.Quantity:
    properties:
      Format:
        enum:
        - DecimalExponent
        - BinarySI
        - DecimalSI
        type: string
        x-enum-comments:
          BinarySI: e.g., 12Mi (12 * 2^20)
          DecimalExponent: e.g., 12e6
          DecimalSI: e.g., 12M  (12 * 10^6)
        x-enum-descriptions:
        - e.g., 12e6
        - e.g., 12Mi (12 * 2^20)
        - e.g., 12M  (12 * 10^6)
        x-enum-varnames:
        - DecimalExponent
        - BinarySI
        - DecimalSI
    type: object
  v1.AWSElasticBlockStoreVolumeSource:
    properties:
      fsType:
        description: |-
          fsType is the filesystem type of the volume that you want to mount.
          Tip: Ensure that the filesystem type is supported by the host operating system.
          Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
          More info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore
          TODO: how do we prevent errors in the filesystem from compromising the machine
          +optional
        type: string
      partition:
        description: |-
          partition is the partition in the volume that you want to mount.
          If omitted, the default is to mount by volume name.
          Examples: For volume /dev/sda1, you specify the partition as "1".
          Similarly, the volume partition for /dev/sda is "0" (or you can leave the property empty).
          +optional
        type: integer
      readOnly:
        description: |-
          readOnly value true will force the readOnly setting in VolumeMounts.
          More info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore
          +optional
        type: boolean
      volumeID:
        description: |-
          volumeID is unique ID of the persistent disk resource in AWS (Amazon EBS volume).
          More info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore
        type: string
    type: object
  v1.Affinity:
    properties:
      nodeAffinity:
        allOf:
        - $ref: '#/definitions/v1.NodeAffinity'
        description: |-
          Describes node affinity scheduling rules for the pod.
          +optional
      podAffinity:
        allOf:
        - $ref: '#/definitions/v1.PodAffinity'
        description: |-
          Describes pod affinity scheduling rules (e.g. co-locate this pod in the same node, zone, etc. as some other pod(s)).
          +optional
      podAntiAffinity:
        allOf:
        - $ref: '#/definitions/v1.PodAntiAffinity'
        description: |-
          Describes pod anti-affinity scheduling rules (e.g. avoid putting this pod in the same node, zone, etc. as some other pod(s)).
          +optional
    type: object
  v1.AppArmorProfile:
    properties:
      localhostProfile:
        description: |-
          localhostProfile indicates a profile loaded on the node that should be used.
          The profile must be preconfigured on the node to work.
          Must match the loaded name of the profile.
          Must be set if and only if type is "Localhost".
          +optional
        type: string
      type:
        allOf:
        - $ref: '#/definitions/v1.AppArmorProfileType'
        description: |-
          type indicates which kind of AppArmor profile will be applied.
          Valid options are:
            Localhost - a profile pre-loaded on the node.
            RuntimeDefault - the container runtime's default profile.
            Unconfined - no AppArmor enforcement.
          +unionDiscriminator
    type: object
  v1.AppArmorProfileType:
    enum:
    - Unconfined
    - RuntimeDefault
    - Localhost
    type: string
    x-enum-varnames:
    - AppArmorProfileTypeUnconfined
    - AppArmorProfileTypeRuntimeDefault
    - AppArmorProfileTypeLocalhost
  v1.AzureDataDiskCachingMode:
    enum:
    - None
    - ReadOnly
    - ReadWrite
    type: string
    x-enum-varnames:
    - AzureDataDiskCachingNone
    - AzureDataDiskCachingReadOnly
    - AzureDataDiskCachingReadWrite
  v1.AzureDataDiskKind:
    enum:
    - Shared
    - Dedicated
    - Managed
    type: string
    x-enum-varnames:
    - AzureSharedBlobDisk
    - AzureDedicatedBlobDisk
    - AzureManagedDisk
  v1.AzureDiskVolumeSource:
    properties:
      cachingMode:
        allOf:
        - $ref: '#/definitions/v1.AzureDataDiskCachingMode'
        description: |-
          cachingMode is the Host Caching mode: None, Read Only, Read Write.
          +optional
          +default=ref(AzureDataDiskCachingReadWrite)
      diskName:
        description: diskName is the Name of the data disk in the blob storage
        type: string
      diskURI:
        description: diskURI is the URI of data disk in the blob storage
        type: string
      fsType:
        description: |-
          fsType is Filesystem type to mount.
          Must be a filesystem type supported by the host operating system.
          Ex. "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
          +optional
          +default="ext4"
        type: string
      kind:
        allOf:
        - $ref: '#/definitions/v1.AzureDataDiskKind'
        description: |-
          kind expected values are Shared: multiple blob disks per storage account  Dedicated: single blob disk per storage account  Managed: azure managed data disk (only in managed availability set). defaults to shared
          +default=ref(AzureSharedBlobDisk)
      readOnly:
        description: |-
          readOnly Defaults to false (read/write). ReadOnly here will force
          the ReadOnly setting in VolumeMounts.
          +optional
          +default=false
        type: boolean
    type: object
  v1.AzureFileVolumeSource:
    properties:
      readOnly:
        description: |-
          readOnly defaults to false (read/write). ReadOnly here will force
          the ReadOnly setting in VolumeMounts.
          +optional
        type: boolean
      secretName:
        description: secretName is the  name of secret that contains Azure Storage
          Account Name and Key
        type: string
      shareName:
        description: shareName is the azure share Name
        type: string
    type: object
  v1.CSIVolumeSource:
    properties:
      driver:
        description: |-
          driver is the name of the CSI driver that handles this volume.
          Consult with your admin for the correct name as registered in the cluster.
        type: string
      fsType:
        description: |-
          fsType to mount. Ex. "ext4", "xfs", "ntfs".
          If not provided, the empty value is passed to the associated CSI driver
          which will determine the default filesystem to apply.
          +optional
        type: string
      nodePublishSecretRef:
        allOf:
        - $ref: '#/definitions/v1.LocalObjectReference'
        description: |-
          nodePublishSecretRef is a reference to the secret object containing
          sensitive information to pass to the CSI driver to complete the CSI
          NodePublishVolume and NodeUnpublishVolume calls.
          This field is optional, and  may be empty if no secret is required. If the
          secret object contains more than one secret, all secret references are passed.
          +optional
      readOnly:
        description: |-
          readOnly specifies a read-only configuration for the volume.
          Defaults to false (read/write).
          +optional
        type: boolean
      volumeAttributes:
        additionalProperties:
          type: string
        description: |-
          volumeAttributes stores driver-specific properties that are passed to the CSI
          driver. Consult your driver's documentation for supported values.
          +optional
        type: object
    type: object
  v1.Capabilities:
    properties:
      add:
        description: |-
          Added capabilities
          +optional
          +listType=atomic
        items:
          type: string
        type: array
      drop:
        description: |-
          Removed capabilities
          +optional
          +listType=atomic
        items:
          type: string
        type: array
    type: object
  v1.CephFSVolumeSource:
    properties:
      monitors:
        description: |-
          monitors is Required: Monitors is a collection of Ceph monitors
          More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it
          +listType=atomic
        items:
          type: string
        type: array
      path:
        description: |-
          path is Optional: Used as the mounted root, rather than the full Ceph tree, default is /
          +optional
        type: string
      readOnly:
        description: |-
          readOnly is Optional: Defaults to false (read/write). ReadOnly here will force
          the ReadOnly setting in VolumeMounts.
          More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it
          +optional
        type: boolean
      secretFile:
        description: |-
          secretFile is Optional: SecretFile is the path to key ring for User, default is /etc/ceph/user.secret
          More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it
          +optional
        type: string
      secretRef:
        allOf:
        - $ref: '#/definitions/v1.LocalObjectReference'
        description: |-
          secretRef is Optional: SecretRef is reference to the authentication secret for User, default is empty.
          More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it
          +optional
      user:
        description: |-
          user is optional: User is the rados user name, default is admin
          More info: https://examples.k8s.io/volumes/cephfs/README.md#how-to-use-it
          +optional
        type: string
    type: object
  v1.CinderVolumeSource:
    properties:
      fsType:
        description: |-
          fsType is the filesystem type to mount.
          Must be a filesystem type supported by the host operating system.
          Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
          More info: https://examples.k8s.io/mysql-cinder-pd/README.md
          +optional
        type: string
      readOnly:
        description: |-
          readOnly defaults to false (read/write). ReadOnly here will force
          the ReadOnly setting in VolumeMounts.
          More info: https://examples.k8s.io/mysql-cinder-pd/README.md
          +optional
        type: boolean
      secretRef:
        allOf:
        - $ref: '#/definitions/v1.LocalObjectReference'
        description: |-
          secretRef is optional: points to a secret object containing parameters used to connect
          to OpenStack.
          +optional
      volumeID:
        description: |-
          volumeID used to identify the volume in cinder.
          More info: https://examples.k8s.io/mysql-cinder-pd/README.md
        type: string
    type: object
  v1.ClusterTrustBundleProjection:
    properties:
      labelSelector:
        allOf:
        - $ref: '#/definitions/v1.LabelSelector'
        description: |-
          Select all ClusterTrustBundles that match this label selector.  Only has
          effect if signerName is set.  Mutually-exclusive with name.  If unset,
          interpreted as "match nothing".  If set but empty, interpreted as "match
          everything".
          +optional
      name:
        description: |-
          Select a single ClusterTrustBundle by object name.  Mutually-exclusive
          with signerName and labelSelector.
          +optional
        type: string
      optional:
        description: |-
          If true, don't block pod startup if the referenced ClusterTrustBundle(s)
          aren't available.  If using name, then the named ClusterTrustBundle is
          allowed not to exist.  If using signerName, then the combination of
          signerName and labelSelector is allowed to match zero
          ClusterTrustBundles.
          +optional
        type: boolean
      path:
        description: Relative path from the volume root to write the bundle.
        type: string
      signerName:
        description: |-
          Select all ClusterTrustBundles that match this signer name.
          Mutually-exclusive with name.  The contents of all selected
          ClusterTrustBundles will be unified and deduplicated.
          +optional
        type: string
    type: object
  v1.ConfigMapEnvSource:
    properties:
      name:
        description: |-
          Name of the referent.
          This field is effectively required, but due to backwards compatibility is
          allowed to be empty. Instances of this type with an empty value here are
          almost certainly wrong.
          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
          +optional
          +default=""
          +kubebuilder:default=""
          TODO: Drop `kubebuilder:default` when controller-gen doesn't need it https://github.com/kubernetes-sigs/kubebuilder/issues/3896.
        type: string
      optional:
        description: |-
          Specify whether the ConfigMap must be defined
          +optional
        type: boolean
    type: object
  v1.ConfigMapKeySelector:
    properties:
      key:
        description: The key to select.
        type: string
      name:
        description: |-
          Name of the referent.
          This field is effectively required, but due to backwards compatibility is
          allowed to be empty. Instances of this type with an empty value here are
          almost certainly wrong.
          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
          +optional
          +default=""
          +kubebuilder:default=""
          TODO: Drop `kubebuilder:default` when controller-gen doesn't need it https://github.com/kubernetes-sigs/kubebuilder/issues/3896.
        type: string
      optional:
        description: |-
          Specify whether the ConfigMap or its key must be defined
          +optional
        type: boolean
    type: object
  v1.ConfigMapProjection:
    properties:
      items:
        description: |-
          items if unspecified, each key-value pair in the Data field of the referenced
          ConfigMap will be projected into the volume as a file whose name is the
          key and content is the value. If specified, the listed keys will be
          projected into the specified paths, and unlisted keys will not be
          present. If a key is specified which is not present in the ConfigMap,
          the volume setup will error unless it is marked optional. Paths must be
          relative and may not contain the '..' path or start with '..'.
          +optional
          +listType=atomic
        items:
          $ref: '#/definitions/v1.KeyToPath'
        type: array
      name:
        description: |-
          Name of the referent.
          This field is effectively required, but due to backwards compatibility is
          allowed to be empty. Instances of this type with an empty value here are
          almost certainly wrong.
          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
          +optional
          +default=""
          +kubebuilder:default=""
          TODO: Drop `kubebuilder:default` when controller-gen doesn't need it https://github.com/kubernetes-sigs/kubebuilder/issues/3896.
        type: string
      optional:
        description: |-
          optional specify whether the ConfigMap or its keys must be defined
          +optional
        type: boolean
    type: object
  v1.ConfigMapVolumeSource:
    properties:
      defaultMode:
        description: |-
          defaultMode is optional: mode bits used to set permissions on created files by default.
          Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511.
          YAML accepts both octal and decimal values, JSON requires decimal values for mode bits.
          Defaults to 0644.
          Directories within the path are not affected by this setting.
          This might be in conflict with other options that affect the file
          mode, like fsGroup, and the result can be other mode bits set.
          +optional
        type: integer
      items:
        description: |-
          items if unspecified, each key-value pair in the Data field of the referenced
          ConfigMap will be projected into the volume as a file whose name is the
          key and content is the value. If specified, the listed keys will be
          projected into the specified paths, and unlisted keys will not be
          present. If a key is specified which is not present in the ConfigMap,
          the volume setup will error unless it is marked optional. Paths must be
          relative and may not contain the '..' path or start with '..'.
          +optional
          +listType=atomic
        items:
          $ref: '#/definitions/v1.KeyToPath'
        type: array
      name:
        description: |-
          Name of the referent.
          This field is effectively required, but due to backwards compatibility is
          allowed to be empty. Instances of this type with an empty value here are
          almost certainly wrong.
          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
          +optional
          +default=""
          +kubebuilder:default=""
          TODO: Drop `kubebuilder:default` when controller-gen doesn't need it https://github.com/kubernetes-sigs/kubebuilder/issues/3896.
        type: string
      optional:
        description: |-
          optional specify whether the ConfigMap or its keys must be defined
          +optional
        type: boolean
    type: object
  v1.Container:
    properties:
      args:
        description: |-
          Arguments to the entrypoint.
          The container image's CMD is used if this is not provided.
          Variable references $(VAR_NAME) are expanded using the container's environment. If a variable
          cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced
          to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. "$$(VAR_NAME)" will
          produce the string literal "$(VAR_NAME)". Escaped references will never be expanded, regardless
          of whether the variable exists or not. Cannot be updated.
          More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell
          +optional
          +listType=atomic
        items:
          type: string
        type: array
      command:
        description: |-
          Entrypoint array. Not executed within a shell.
          The container image's ENTRYPOINT is used if this is not provided.
          Variable references $(VAR_NAME) are expanded using the container's environment. If a variable
          cannot be resolved, the reference in the input string will be unchanged. Double $$ are reduced
          to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e. "$$(VAR_NAME)" will
          produce the string literal "$(VAR_NAME)". Escaped references will never be expanded, regardless
          of whether the variable exists or not. Cannot be updated.
          More info: https://kubernetes.io/docs/tasks/inject-data-application/define-command-argument-container/#running-a-command-in-a-shell
          +optional
          +listType=atomic
        items:
          type: string
        type: array
      env:
        description: |-
          List of environment variables to set in the container.
          Cannot be updated.
          +optional
          +patchMergeKey=name
          +patchStrategy=merge
          +listType=map
          +listMapKey=name
        items:
          $ref: '#/definitions/v1.EnvVar'
        type: array
      envFrom:
        description: |-
          List of sources to populate environment variables in the container.
          The keys defined within a source may consist of any printable ASCII characters except '='.
          When a key exists in multiple
          sources, the value associated with the last source will take precedence.
          Values defined by an Env with a duplicate key will take precedence.
          Cannot be updated.
          +optional
          +listType=atomic
        items:
          $ref: '#/definitions/v1.EnvFromSource'
        type: array
      image:
        description: |-
          Container image name.
          More info: https://kubernetes.io/docs/concepts/containers/images
          This field is optional to allow higher level config management to default or override
          container images in workload controllers like Deployments and StatefulSets.
          +optional
        type: string
      imagePullPolicy:
        allOf:
        - $ref: '#/definitions/v1.PullPolicy'
        description: |-
          Image pull policy.
          One of Always, Never, IfNotPresent.
          Defaults to Always if :latest tag is specified, or IfNotPresent otherwise.
          Cannot be updated.
          More info: https://kubernetes.io/docs/concepts/containers/images#updating-images
          +optional
      lifecycle:
        allOf:
        - $ref: '#/definitions/v1.Lifecycle'
        description: |-
          Actions that the management system should take in response to container lifecycle events.
          Cannot be updated.
          +optional
      livenessProbe:
        allOf:
        - $ref: '#/definitions/v1.Probe'
        description: |-
          Periodic probe of container liveness.
          Container will be restarted if the probe fails.
          Cannot be updated.
          More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes
          +optional
      name:
        description: |-
          Name of the container specified as a DNS_LABEL.
          Each container in a pod must have a unique name (DNS_LABEL).
          Cannot be updated.
        type: string
      ports:
        description: |-
          List of ports to expose from the container. Not specifying a port here
          DOES NOT prevent that port from being exposed. Any port which is
          listening on the default "0.0.0.0" address inside a container will be
          accessible from the network.
          Modifying this array with strategic merge patch may corrupt the data.
          For more information See https://github.com/kubernetes/kubernetes/issues/108255.
          Cannot be updated.
          +optional
          +patchMergeKey=containerPort
          +patchStrategy=merge
          +listType=map
          +listMapKey=containerPort
          +listMapKey=protocol
        items:
          $ref: '#/definitions/v1.ContainerPort'
        type: array
      readinessProbe:
        allOf:
        - $ref: '#/definitions/v1.Probe'
        description: |-
          Periodic probe of container service readiness.
          Container will be removed from service endpoints if the probe fails.
          Cannot be updated.
          More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes
          +optional
      resizePolicy:
        description: |-
          Resources resize policy for the container.
          +featureGate=InPlacePodVerticalScaling
          +optional
          +listType=atomic
        items:
          $ref: '#/definitions/v1.ContainerResizePolicy'
        type: array
      resources:
        allOf:
        - $ref: '#/definitions/v1.ResourceRequirements'
        description: |-
          Compute Resources required by this container.
          Cannot be updated.
          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
          +optional
      restartPolicy:
        allOf:
        - $ref: '#/definitions/v1.ContainerRestartPolicy'
        description: |-
          RestartPolicy defines the restart behavior of individual containers in a pod.
          This overrides the pod-level restart policy. When this field is not specified,
          the restart behavior is defined by the Pod's restart policy and the container type.
          Additionally, setting the RestartPolicy as "Always" for the init container will
          have the following effect:
          this init container will be continually restarted on
          exit until all regular containers have terminated. Once all regular
          containers have completed, all init containers with restartPolicy "Always"
          will be shut down. This lifecycle differs from normal init containers and
          is often referred to as a "sidecar" container. Although this init
          container still starts in the init container sequence, it does not wait
          for the container to complete before proceeding to the next init
          container. Instead, the next init container starts immediately after this
          init container is started, or after any startupProbe has successfully
          completed.
          +featureGate=SidecarContainers
          +optional
      restartPolicyRules:
        description: |-
          Represents a list of rules to be checked to determine if the
          container should be restarted on exit. The rules are evaluated in
          order. Once a rule matches a container exit condition, the remaining
          rules are ignored. If no rule matches the container exit condition,
          the Container-level restart policy determines the whether the container
          is restarted or not. Constraints on the rules:
          - At most 20 rules are allowed.
          - Rules can have the same action.
          - Identical rules are not forbidden in validations.
          When rules are specified, container MUST set RestartPolicy explicitly
          even it if matches the Pod's RestartPolicy.
          +featureGate=ContainerRestartRules
          +optional
          +listType=atomic
        items:
          $ref: '#/definitions/v1.ContainerRestartRule'
        type: array
      securityContext:
        allOf:
        - $ref: '#/definitions/v1.SecurityContext'
        description: |-
          SecurityContext defines the security options the container should be run with.
          If set, the fields of SecurityContext override the equivalent fields of PodSecurityContext.
          More info: https://kubernetes.io/docs/tasks/configure-pod-container/security-context/
          +optional
      startupProbe:
        allOf:
        - $ref: '#/definitions/v1.Probe'
        description: |-
          StartupProbe indicates that the Pod has successfully initialized.
          If specified, no other probes are executed until this completes successfully.
          If this probe fails, the Pod will be restarted, just as if the livenessProbe failed.
          This can be used to provide different probe parameters at the beginning of a Pod's lifecycle,
          when it might take a long time to load data or warm a cache, than during steady-state operation.
          This cannot be updated.
          More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes
          +optional
      stdin:
        description: |-
          Whether this container should allocate a buffer for stdin in the container runtime. If this
          is not set, reads from stdin in the container will always result in EOF.
          Default is false.
          +optional
        type: boolean
      stdinOnce:
        description: |-
          Whether the container runtime should close the stdin channel after it has been opened by
          a single attach. When stdin is true the stdin stream will remain open across multiple attach
          sessions. If stdinOnce is set to true, stdin is opened on container start, is empty until the
          first client attaches to stdin, and then remains open and accepts data until the client disconnects,
          at which time stdin is closed and remains closed until the container is restarted. If this
          flag is false, a container processes that reads from stdin will never receive an EOF.
          Default is false
          +optional
        type: boolean
      terminationMessagePath:
        description: |-
          Optional: Path at which the file to which the container's termination message
          will be written is mounted into the container's filesystem.
          Message written is intended to be brief final status, such as an assertion failure message.
          Will be truncated by the node if greater than 4096 bytes. The total message length across
          all containers will be limited to 12kb.
          Defaults to /dev/termination-log.
          Cannot be updated.
          +optional
        type: string
      terminationMessagePolicy:
        allOf:
        - $ref: '#/definitions/v1.TerminationMessagePolicy'
        description: |-
          Indicate how the termination message should be populated. File will use the contents of
          terminationMessagePath to populate the container status message on both success and failure.
          FallbackToLogsOnError will use the last chunk of container log output if the termination
          message file is empty and the container exited with an error.
          The log output is limited to 2048 bytes or 80 lines, whichever is smaller.
          Defaults to File.
          Cannot be updated.
          +optional
      tty:
        description: |-
          Whether this container should allocate a TTY for itself, also requires 'stdin' to be true.
          Default is false.
          +optional
        type: boolean
      volumeDevices:
        description: |-
          volumeDevices is the list of block devices to be used by the container.
          +patchMergeKey=devicePath
          +patchStrategy=merge
          +listType=map
          +listMapKey=devicePath
          +optional
        items:
          $ref: '#/definitions/v1.VolumeDevice'
        type: array
      volumeMounts:
        description: |-
          Pod volumes to mount into the container's filesystem.
          Cannot be updated.
          +optional
          +patchMergeKey=mountPath
          +patchStrategy=merge
          +listType=map
          +listMapKey=mountPath
        items:
          $ref: '#/definitions/v1.VolumeMount'
        type: array
      workingDir:
        description: |-
          Container's working directory.
          If not specified, the container runtime's default will be used, which
          might be configured in the container image.
          Cannot be updated.
          +optional
        type: string
    type: object
  v1.ContainerPort:
    properties:
      containerPort:
        description: |-
          Number of port to expose on the pod's IP address.
          This must be a valid port number, 0 < x < 65536.
        type: integer
      hostIP:
        description: |-
          What host IP to bind the external port to.
          +optional
        type: string
      hostPort:
        description: |-
          Number of port to expose on the host.
          If specified, this must be a valid port number, 0 < x < 65536.
          If HostNetwork is specified, this must match ContainerPort.
          Most containers do not need this.
          +optional
        type: integer
      name:
        description: |-
          If specified, this must be an IANA_SVC_NAME and unique within the pod. Each
          named port in a pod must have a unique name. Name for the port that can be
          referred to by services.
          +optional
        type: string
      protocol:
        allOf:
        - $ref: '#/definitions/v1.Protocol'
        description: |-
          Protocol for port. Must be UDP, TCP, or SCTP.
          Defaults to "TCP".
          +optional
          +default="TCP"
    type: object
  v1.ContainerResizePolicy:
    properties:
      resourceName:
        allOf:
        - $ref: '#/definitions/v1.ResourceName'
        description: |-
          Name of the resource to which this resource resize policy applies.
          Supported values: cpu, memory.
      restartPolicy:
        allOf:
        - $ref: '#/definitions/v1.ResourceResizeRestartPolicy'
        description: |-
          Restart policy to apply when specified resource is resized.
          If not specified, it defaults to NotRequired.
    type: object
  v1.ContainerRestartPolicy:
    enum:
    - Always
    - Never
    - OnFailure
    type: string
    x-enum-varnames:
    - ContainerRestartPolicyAlways
    - ContainerRestartPolicyNever
    - ContainerRestartPolicyOnFailure
  v1.ContainerRestartRule:
    properties:
      action:
        allOf:
        - $ref: '#/definitions/v1.ContainerRestartRuleAction'
        description: |-
          Specifies the action taken on a container exit if the requirements
          are satisfied. The only possible value is "Restart" to restart the
          container.
          +required
      exitCodes:
        allOf:
        - $ref: '#/definitions/v1.ContainerRestartRuleOnExitCodes'
        description: |-
          Represents the exit codes to check on container exits.
          +optional
          +oneOf=when
    type: object
  v1.ContainerRestartRuleAction:
    enum:
    - Restart
    type: string
    x-enum-varnames:
    - ContainerRestartRuleActionRestart
  v1.ContainerRestartRuleOnExitCodes:
    properties:
      operator:
        allOf:
        - $ref: '#/definitions/v1.ContainerRestartRuleOnExitCodesOperator'
        description: |-
          Represents the relationship between the container exit code(s) and the
          specified values. Possible values are:
          - In: the requirement is satisfied if the container exit code is in the
            set of specified values.
          - NotIn: the requirement is satisfied if the container exit code is
            not in the set of specified values.
          +required
      values:
        description: |-
          Specifies the set of values to check for container exit codes.
          At most 255 elements are allowed.
          +optional
          +listType=set
        items:
          type: integer
        type: array
    type: object
  v1.ContainerRestartRuleOnExitCodesOperator:
    enum:
    - In
    - NotIn
    type: string
    x-enum-varnames:
    - ContainerRestartRuleOnExitCodesOpIn
    - ContainerRestartRuleOnExitCodesOpNotIn
  v1.DownwardAPIProjection:
    properties:
      items:
        description: |-
          Items is a list of DownwardAPIVolume file
          +optional
          +listType=atomic
        items:
          $ref: '#/definitions/v1.DownwardAPIVolumeFile'
        type: array
    type: object
  v1.DownwardAPIVolumeFile:
    properties:
      fieldRef:
        allOf:
        - $ref: '#/definitions/v1.ObjectFieldSelector'
        description: |-
          Required: Selects a field of the pod: only annotations, labels, name, namespace and uid are supported.
          +optional
      mode:
        description: |-
          Optional: mode bits used to set permissions on this file, must be an octal value
          between 0000 and 0777 or a decimal value between 0 and 511.
          YAML accepts both octal and decimal values, JSON requires decimal values for mode bits.
          If not specified, the volume defaultMode will be used.
          This might be in conflict with other options that affect the file
          mode, like fsGroup, and the result can be other mode bits set.
          +optional
        type: integer
      path:
        description: 'Required: Path is  the relative path name of the file to be
          created. Must not be absolute or contain the ''..'' path. Must be utf-8
          encoded. The first item of the relative path must not start with ''..'''
        type: string
      resourceFieldRef:
        allOf:
        - $ref: '#/definitions/v1.ResourceFieldSelector'
        description: |-
          Selects a resource of the container: only resources limits and requests
          (limits.cpu, limits.memory, requests.cpu and requests.memory) are currently supported.
          +optional
    type: object
  v1.DownwardAPIVolumeSource:
    properties:
      defaultMode:
        description: |-
          Optional: mode bits to use on created files by default. Must be a
          Optional: mode bits used to set permissions on created files by default.
          Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511.
          YAML accepts both octal and decimal values, JSON requires decimal values for mode bits.
          Defaults to 0644.
          Directories within the path are not affected by this setting.
          This might be in conflict with other options that affect the file
          mode, like fsGroup, and the result can be other mode bits set.
          +optional
        type: integer
      items:
        description: |-
          Items is a list of downward API volume file
          +optional
          +listType=atomic
        items:
          $ref: '#/definitions/v1.DownwardAPIVolumeFile'
        type: array
    type: object
  v1.EmptyDirVolumeSource:
    properties:
      medium:
        allOf:
        - $ref: '#/definitions/v1.StorageMedium'
        description: |-
          medium represents what type of storage medium should back this directory.
          The default is "" which means to use the node's default medium.
          Must be an empty string (default) or Memory.
          More info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir
          +optional
      sizeLimit:
        allOf:
        - $ref: '#/definitions/resource.Quantity'
        description: |-
          sizeLimit is the total amount of local storage required for this EmptyDir volume.
          The size limit is also applicable for memory medium.
          The maximum usage on memory medium EmptyDir would be the minimum value between
          the SizeLimit specified here and the sum of memory limits of all containers in a pod.
          The default is nil which means that the limit is undefined.
          More info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir
          +optional
    type: object
  v1.EnvFromSource:
    properties:
      configMapRef:
        allOf:
        - $ref: '#/definitions/v1.ConfigMapEnvSource'
        description: |-
          The ConfigMap to select from
          +optional
      prefix:
        description: |-
          Optional text to prepend to the name of each environment variable.
          May consist of any printable ASCII characters except '='.
          +optional
        type: string
      secretRef:
        allOf:
        - $ref: '#/definitions/v1.SecretEnvSource'
        description: |-
          The Secret to select from
          +optional
    type: object
  v1.EnvVar:
    properties:
      name:
        description: |-
          Name of the environment variable.
          May consist of any printable ASCII characters except '='.
        type: string
      value:
        description: |-
          Variable references $(VAR_NAME) are expanded
          using the previously defined environment variables in the container and
          any service environment variables. If a variable cannot be resolved,
          the reference in the input string will be unchanged. Double $$ are reduced
          to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
          "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
          Escaped references will never be expanded, regardless of whether the variable
          exists or not.
          Defaults to "".
          +optional
        type: string
      valueFrom:
        allOf:
        - $ref: '#/definitions/v1.EnvVarSource'
        description: |-
          Source for the environment variable's value. Cannot be used if value is not empty.
          +optional
    type: object
  v1.EnvVarSource:
    properties:
      configMapKeyRef:
        allOf:
        - $ref: '#/definitions/v1.ConfigMapKeySelector'
        description: |-
          Selects a key of a ConfigMap.
          +optional
      fieldRef:
        allOf:
        - $ref: '#/definitions/v1.ObjectFieldSelector'
        description: |-
          Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
          spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
          +optional
      fileKeyRef:
        allOf:
        - $ref: '#/definitions/v1.FileKeySelector'
        description: |-
          FileKeyRef selects a key of the env file.
          Requires the EnvFiles feature gate to be enabled.

          +featureGate=EnvFiles
          +optional
      resourceFieldRef:
        allOf:
        - $ref: '#/definitions/v1.ResourceFieldSelector'
        description: |-
          Selects a resource of the container: only resources limits and requests
          (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
          +optional
      secretKeyRef:
        allOf:
        - $ref: '#/definitions/v1.SecretKeySelector'
        description: |-
          Selects a key of a secret in the pod's namespace
          +optional
    type: object
  v1.EphemeralVolumeSource:
    properties:
      volumeClaimTemplate:
        allOf:
        - $ref: '#/definitions/v1.PersistentVolumeClaimTemplate'
        description: |-
          Will be used to create a stand-alone PVC to provision the volume.
          The pod in which this EphemeralVolumeSource is embedded will be the
          owner of the PVC, i.e. the PVC will be deleted together with the
          pod.  The name of the PVC will be `<pod name>-<volume name>` where
          `<volume name>` is the name from the `PodSpec.Volumes` array
          entry. Pod validation will reject the pod if the concatenated name
          is not valid for a PVC (for example, too long).

          An existing PVC with that name that is not owned by the pod
          will *not* be used for the pod to avoid using an unrelated
          volume by mistake. Starting the pod is then blocked until
          the unrelated PVC is removed. If such a pre-created PVC is
          meant to be used by the pod, the PVC has to updated with an
          owner reference to the pod once the pod exists. Normally
          this should not be necessary, but it may be useful when
          manually reconstructing a broken cluster.

          This field is read-only and no changes will be made by Kubernetes
          to the PVC after it has been created.

          Required, must not be nil.
    type: object
  v1.ExecAction:
    properties:
      command:
        description: |-
          Command is the command line to execute inside the container, the working directory for the
          command  is root ('/') in the container's filesystem. The command is simply exec'd, it is
          not run inside a shell, so traditional shell instructions ('|', etc) won't work. To use
          a shell, you need to explicitly call out to that shell.
          Exit status of 0 is treated as live/healthy and non-zero is unhealthy.
          +optional
          +listType=atomic
        items:
          type: string
        type: array
    type: object
  v1.FCVolumeSource:
    properties:
      fsType:
        description: |-
          fsType is the filesystem type to mount.
          Must be a filesystem type supported by the host operating system.
          Ex. "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
          TODO: how do we prevent errors in the filesystem from compromising the machine
          +optional
        type: string
      lun:
        description: |-
          lun is Optional: FC target lun number
          +optional
        type: integer
      readOnly:
        description: |-
          readOnly is Optional: Defaults to false (read/write). ReadOnly here will force
          the ReadOnly setting in VolumeMounts.
          +optional
        type: boolean
      targetWWNs:
        description: |-
          targetWWNs is Optional: FC target worldwide names (WWNs)
          +optional
          +listType=atomic
        items:
          type: string
        type: array
      wwids:
        description: |-
          wwids Optional: FC volume world wide identifiers (wwids)
          Either wwids or combination of targetWWNs and lun must be set, but not both simultaneously.
          +optional
          +listType=atomic
        items:
          type: string
        type: array
    type: object
  v1.FieldsV1:
    type: object
  v1.FileKeySelector:
    properties:
      key:
        description: |-
          The key within the env file. An invalid key will prevent the pod from starting.
          The keys defined within a source may consist of any printable ASCII characters except '='.
          During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
          +required
        type: string
      optional:
        description: |-
          Specify whether the file or its key must be defined. If the file or key
          does not exist, then the env var is not published.
          If optional is set to true and the specified key does not exist,
          the environment variable will not be set in the Pod's containers.

          If optional is set to false and the specified key does not exist,
          an error will be returned during Pod creation.
          +optional
          +default=false
        type: boolean
      path:
        description: |-
          The path within the volume from which to select the file.
          Must be relative and may not contain the '..' path or start with '..'.
          +required
        type: string
      volumeName:
        description: |-
          The name of the volume mount containing the env file.
          +required
        type: string
    type: object
  v1.FlexVolumeSource:
    properties:
      driver:
        description: driver is the name of the driver to use for this volume.
        type: string
      fsType:
        description: |-
          fsType is the filesystem type to mount.
          Must be a filesystem type supported by the host operating system.
          Ex. "ext4", "xfs", "ntfs". The default filesystem depends on FlexVolume script.
          +optional
        type: string
      options:
        additionalProperties:
          type: string
        description: |-
          options is Optional: this field holds extra command options if any.
          +optional
        type: object
      readOnly:
        description: |-
          readOnly is Optional: defaults to false (read/write). ReadOnly here will force
          the ReadOnly setting in VolumeMounts.
          +optional
        type: boolean
      secretRef:
        allOf:
        - $ref: '#/definitions/v1.LocalObjectReference'
        description: |-
          secretRef is Optional: secretRef is reference to the secret object containing
          sensitive information to pass to the plugin scripts. This may be
          empty if no secret object is specified. If the secret object
          contains more than one secret, all secrets are passed to the plugin
          scripts.
          +optional
    type: object
  v1.FlockerVolumeSource:
    properties:
      datasetName:
        description: |-
          datasetName is Name of the dataset stored as metadata -> name on the dataset for Flocker
          should be considered as deprecated
          +optional
        type: string
      datasetUUID:
        description: |-
          datasetUUID is the UUID of the dataset. This is unique identifier of a Flocker dataset
          +optional
        type: string
    type: object
  v1.GCEPersistentDiskVolumeSource:
    properties:
      fsType:
        description: |-
          fsType is filesystem type of the volume that you want to mount.
          Tip: Ensure that the filesystem type is supported by the host operating system.
          Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
          More info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk
          TODO: how do we prevent errors in the filesystem from compromising the machine
          +optional
        type: string
      partition:
        description: |-
          partition is the partition in the volume that you want to mount.
          If omitted, the default is to mount by volume name.
          Examples: For volume /dev/sda1, you specify the partition as "1".
          Similarly, the volume partition for /dev/sda is "0" (or you can leave the property empty).
          More info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk
          +optional
        type: integer
      pdName:
        description: |-
          pdName is unique name of the PD resource in GCE. Used to identify the disk in GCE.
          More info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk
        type: string
      readOnly:
        description: |-
          readOnly here will force the ReadOnly setting in VolumeMounts.
          Defaults to false.
          More info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk
          +optional
        type: boolean
    type: object
  v1.GRPCAction:
    properties:
      port:
        description: Port number of the gRPC service. Number must be in the range
          1 to 65535.
        type: integer
      service:
        description: |-
          Service is the name of the service to place in the gRPC HealthCheckRequest
          (see https://github.com/grpc/grpc/blob/master/doc/health-checking.md).

          If this is not specified, the default behavior is defined by gRPC.
          +optional
          +default=""
        type: string
    type: object
  v1.GitRepoVolumeSource:
    properties:
      directory:
        description: |-
          directory is the target directory name.
          Must not contain or start with '..'.  If '.' is supplied, the volume directory will be the
          git repository.  Otherwise, if specified, the volume will contain the git repository in
          the subdirectory with the given name.
          +optional
        type: string
      repository:
        description: repository is the URL
        type: string
      revision:
        description: |-
          revision is the commit hash for the specified revision.
          +optional
        type: string
    type: object
  v1.GlusterfsVolumeSource:
    properties:
      endpoints:
        description: endpoints is the endpoint name that details Glusterfs topology.
        type: string
      path:
        description: |-
          path is the Glusterfs volume path.
          More info: https://examples.k8s.io/volumes/glusterfs/README.md#create-a-pod
        type: string
      readOnly:
        description: |-
          readOnly here will force the Glusterfs volume to be mounted with read-only permissions.
          Defaults to false.
          More info: https://examples.k8s.io/volumes/glusterfs/README.md#create-a-pod
          +optional
        type: boolean
    type: object
  v1.HTTPGetAction:
    properties:
      host:
        description: |-
          Host name to connect to, defaults to the pod IP. You probably want to set
          "Host" in httpHeaders instead.
          +optional
        type: string
      httpHeaders:
        description: |-
          Custom headers to set in the request. HTTP allows repeated headers.
          +optional
          +listType=atomic
        items:
          $ref: '#/definitions/v1.HTTPHeader'
        type: array
      path:
        description: |-
          Path to access on the HTTP server.
          +optional
        type: string
      port:
        allOf:
        - $ref: '#/definitions/intstr.IntOrString'
        description: |-
          Name or number of the port to access on the container.
          Number must be in the range 1 to 65535.
          Name must be an IANA_SVC_NAME.
      scheme:
        allOf:
        - $ref: '#/definitions/v1.URIScheme'
        description: |-
          Scheme to use for connecting to the host.
          Defaults to HTTP.
          +optional
    type: object
  v1.HTTPHeader:
    properties:
      name:
        description: |-
          The header field name.
          This will be canonicalized upon output, so case-variant names will be understood as the same header.
        type: string
      value:
        description: The header field value
        type: string
    type: object
  v1.HostPathType:
    enum:
    - ""
    - DirectoryOrCreate
    - Directory
    - FileOrCreate
    - File
    - Socket
    - CharDevice
    - BlockDevice
    type: string
    x-enum-varnames:
    - HostPathUnset
    - HostPathDirectoryOrCreate
    - HostPathDirectory
    - HostPathFileOrCreate
    - HostPathFile
    - HostPathSocket
    - HostPathCharDev
    - HostPathBlockDev
  v1.HostPathVolumeSource:
    properties:
      path:
        description: |-
          path of the directory on the host.
          If the path is a symlink, it will follow the link to the real path.
          More info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath
        type: string
      type:
        allOf:
        - $ref: '#/definitions/v1.HostPathType'
        description: |-
          type for HostPath Volume
          Defaults to ""
          More info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath
          +optional
    type: object
  v1.ISCSIVolumeSource:
    properties:
      chapAuthDiscovery:
        description: |-
          chapAuthDiscovery defines whether support iSCSI Discovery CHAP authentication
          +optional
        type: boolean
      chapAuthSession:
        description: |-
          chapAuthSession defines whether support iSCSI Session CHAP authentication
          +optional
        type: boolean
      fsType:
        description: |-
          fsType is the filesystem type of the volume that you want to mount.
          Tip: Ensure that the filesystem type is supported by the host operating system.
          Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
          More info: https://kubernetes.io/docs/concepts/storage/volumes#iscsi
          TODO: how do we prevent errors in the filesystem from compromising the machine
          +optional
        type: string
      initiatorName:
        description: |-
          initiatorName is the custom iSCSI Initiator Name.
          If initiatorName is specified with iscsiInterface simultaneously, new iSCSI interface
          <target portal>:<volume name> will be created for the connection.
          +optional
        type: string
      iqn:
        description: iqn is the target iSCSI Qualified Name.
        type: string
      iscsiInterface:
        description: |-
          iscsiInterface is the interface Name that uses an iSCSI transport.
          Defaults to 'default' (tcp).
          +optional
          +default="default"
        type: string
      lun:
        description: lun represents iSCSI Target Lun number.
        type: integer
      portals:
        description: |-
          portals is the iSCSI Target Portal List. The portal is either an IP or ip_addr:port if the port
          is other than default (typically TCP ports 860 and 3260).
          +optional
          +listType=atomic
        items:
          type: string
        type: array
      readOnly:
        description: |-
          readOnly here will force the ReadOnly setting in VolumeMounts.
          Defaults to false.
          +optional
        type: boolean
      secretRef:
        allOf:
        - $ref: '#/definitions/v1.LocalObjectReference'
        description: |-
          secretRef is the CHAP Secret for iSCSI target and initiator authentication
          +optional
      targetPortal:
        description: |-
          targetPortal is iSCSI Target Portal. The Portal is either an IP or ip_addr:port if the port
          is other than default (typically TCP ports 860 and 3260).
        type: string
    type: object
  v1.ImageVolumeSource:
    properties:
      pullPolicy:
        allOf:
        - $ref: '#/definitions/v1.PullPolicy'
        description: |-
          Policy for pulling OCI objects. Possible values are:
          Always: the kubelet always attempts to pull the reference. Container creation will fail If the pull fails.
          Never: the kubelet never pulls the reference and only uses a local image or artifact. Container creation will fail if the reference isn't present.
          IfNotPresent: the kubelet pulls if the reference isn't already present on disk. Container creation will fail if the reference isn't present and the pull fails.
          Defaults to Always if :latest tag is specified, or IfNotPresent otherwise.
          +optional
      reference:
        description: |-
          Required: Image or artifact reference to be used.
          Behaves in the same way as pod.spec.containers[*].image.
          Pull secrets will be assembled in the same way as for the container image by looking up node credentials, SA image pull secrets, and pod spec image pull secrets.
          More info: https://kubernetes.io/docs/concepts/containers/images
          This field is optional to allow higher level config management to default or override
          container images in workload controllers like Deployments and StatefulSets.
          +optional
        type: string
    type: object
  v1.KeyToPath:
    properties:
      key:
        description: key is the key to project.
        type: string
      mode:
        description: |-
          mode is Optional: mode bits used to set permissions on this file.
          Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511.
          YAML accepts both octal and decimal values, JSON requires decimal values for mode bits.
          If not specified, the volume defaultMode will be used.
          This might be in conflict with other options that affect the file
          mode, like fsGroup, and the result can be other mode bits set.
          +optional
        type: integer
      path:
        description: |-
          path is the relative path of the file to map the key to.
          May not be an absolute path.
          May not contain the path element '..'.
          May not start with the string '..'.
        type: string
    type: object
  v1.LabelSelector:
    properties:
      matchExpressions:
        description: |-
          matchExpressions is a list of label selector requirements. The requirements are ANDed.
          +optional
          +listType=atomic
        items:
          $ref: '#/definitions/v1.LabelSelectorRequirement'
        type: array
      matchLabels:
        additionalProperties:
          type: string
        description: |-
          matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
          map is equivalent to an element of matchExpressions, whose key field is "key", the
          operator is "In", and the values array contains only "value". The requirements are ANDed.
          +optional
        type: object
    type: object
  v1.LabelSelectorOperator:
    enum:
    - In
    - NotIn
    - Exists
    - DoesNotExist
    type: string
    x-enum-varnames:
    - LabelSelectorOpIn
    - LabelSelectorOpNotIn
    - LabelSelectorOpExists
    - LabelSelectorOpDoesNotExist
  v1.LabelSelectorRequirement:
    properties:
      key:
        description: key is the label key that the selector applies to.
        type: string
      operator:
        allOf:
        - $ref: '#/definitions/v1.LabelSelectorOperator'
        description: |-
          operator represents a key's relationship to a set of values.
          Valid operators are In, NotIn, Exists and DoesNotExist.
      values:
        description: |-
          values is an array of string values. If the operator is In or NotIn,
          the values array must be non-empty. If the operator is Exists or DoesNotExist,
          the values array must be empty. This array is replaced during a strategic
          merge patch.
          +optional
          +listType=atomic
        items:
          type: string
        type: array
    type: object
  v1.Lifecycle:
    properties:
      postStart:
        allOf:
        - $ref: '#/definitions/v1.LifecycleHandler'
        description: |-
          PostStart is called immediately after a container is created. If the handler fails,
          the container is terminated and restarted according to its restart policy.
          Other management of the container blocks until the hook completes.
          More info: https://kubernetes.io/docs/concepts/containers/container-lifecycle-hooks/#container-hooks
          +optional
      preStop:
        allOf:
        - $ref: '#/definitions/v1.LifecycleHandler'
        description: |-
          PreStop is called immediately before a container is terminated due to an
          API request or management event such as liveness/startup probe failure,
          preemption, resource contention, etc. The handler is not called if the
          container crashes or exits. The Pod's termination grace period countdown begins before the
          PreStop hook is executed. Regardless of the outcome of the handler, the
          container will eventually terminate within the Pod's termination grace
          period (unless delayed by finalizers). Other management of the container blocks until the hook completes
          or until the termination grace period is reached.
          More info: https://kubernetes.io/docs/concepts/containers/container-lifecycle-hooks/#container-hooks
          +optional
      stopSignal:
        allOf:
        - $ref: '#/definitions/v1.Signal'
        description: |-
          StopSignal defines which signal will be sent to a container when it is being stopped.
          If not specified, the default is defined by the container runtime in use.
          StopSignal can only be set for Pods with a non-empty .spec.os.name
          +optional
    type: object
  v1.LifecycleHandler:
    properties:
      exec:
        allOf:
        - $ref: '#/definitions/v1.ExecAction'
        description: |-
          Exec specifies a command to execute in the container.
          +optional
      httpGet:
        allOf:
        - $ref: '#/definitions/v1.HTTPGetAction'
        description: |-
          HTTPGet specifies an HTTP GET request to perform.
          +optional
      sleep:
        allOf:
        - $ref: '#/definitions/v1.SleepAction'
        description: |-
          Sleep represents a duration that the container should sleep.
          +featureGate=PodLifecycleSleepAction
          +optional
      tcpSocket:
        allOf:
        - $ref: '#/definitions/v1.TCPSocketAction'
        description: |-
          Deprecated. TCPSocket is NOT supported as a LifecycleHandler and kept
          for backward compatibility. There is no validation of this field and
          lifecycle hooks will fail at runtime when it is specified.
          +optional
    type: object
  v1.LocalObjectReference:
    properties:
      name:
        description: |-
          Name of the referent.
          This field is effectively required, but due to backwards compatibility is
          allowed to be empty. Instances of this type with an empty value here are
          almost certainly wrong.
          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
          +optional
          +default=""
          +kubebuilder:default=""
          TODO: Drop `kubebuilder:default` when controller-gen doesn't need it https://github.com/kubernetes-sigs/kubebuilder/issues/3896.
        type: string
    type: object
  v1.ManagedFieldsEntry:
    properties:
      apiVersion:
        description: |-
          APIVersion defines the version of this resource that this field set
          applies to. The format is "group/version" just like the top-level
          APIVersion field. It is necessary to track the version of a field
          set because it cannot be automatically converted.
        type: string
      fieldsType:
        description: |-
          FieldsType is the discriminator for the different fields format and version.
          There is currently only one possible value: "FieldsV1"
        type: string
      fieldsV1:
        allOf:
        - $ref: '#/definitions/v1.FieldsV1'
        description: |-
          FieldsV1 holds the first JSON version format as described in the "FieldsV1" type.
          +optional
      manager:
        description: Manager is an identifier of the workflow managing these fields.
        type: string
      operation:
        allOf:
        - $ref: '#/definitions/v1.ManagedFieldsOperationType'
        description: |-
          Operation is the type of operation which lead to this ManagedFieldsEntry being created.
          The only valid values for this field are 'Apply' and 'Update'.
      subresource:
        description: |-
          Subresource is the name of the subresource used to update that object, or
          empty string if the object was updated through the main resource. The
          value of this field is used to distinguish between managers, even if they
          share the same name. For example, a status update will be distinct from a
          regular update using the same manager name.
          Note that the APIVersion field is not related to the Subresource field and
          it always corresponds to the version of the main resource.
        type: string
      time:
        description: |-
          Time is the timestamp of when the ManagedFields entry was added. The
          timestamp will also be updated if a field is added, the manager
          changes any of the owned fields value or removes a field. The
          timestamp does not update when a field is removed from the entry
          because another manager took it over.
          +optional
        type: string
    type: object
  v1.ManagedFieldsOperationType:
    enum:
    - Apply
    - Update
    type: string
    x-enum-varnames:
    - ManagedFieldsOperationApply
    - ManagedFieldsOperationUpdate
  v1.MountPropagationMode:
    enum:
    - None
    - HostToContainer
    - Bidirectional
    type: string
    x-enum-varnames:
    - MountPropagationNone
    - MountPropagationHostToContainer
    - MountPropagationBidirectional
  v1.NFSVolumeSource:
    properties:
      path:
        description: |-
          path that is exported by the NFS server.
          More info: https://kubernetes.io/docs/concepts/storage/volumes#nfs
        type: string
      readOnly:
        description: |-
          readOnly here will force the NFS export to be mounted with read-only permissions.
          Defaults to false.
          More info: https://kubernetes.io/docs/concepts/storage/volumes#nfs
          +optional
        type: boolean
      server:
        description: |-
          server is the hostname or IP address of the NFS server.
          More info: https://kubernetes.io/docs/concepts/storage/volumes#nfs
        type: string
    type: object
  v1.NodeAffinity:
    properties:
      preferredDuringSchedulingIgnoredDuringExecution:
        description: |-
          The scheduler will prefer to schedule pods to nodes that satisfy
          the affinity expressions specified by this field, but it may choose
          a node that violates one or more of the expressions. The node that is
          most preferred is the one with the greatest sum of weights, i.e.
          for each node that meets all of the scheduling requirements (resource
          request, requiredDuringScheduling affinity expressions, etc.),
          compute a sum by iterating through the elements of this field and adding
          "weight" to the sum if the node matches the corresponding matchExpressions; the
          node(s) with the highest sum are the most preferred.
          +optional
          +listType=atomic
        items:
          $ref: '#/definitions/v1.PreferredSchedulingTerm'
        type: array
      requiredDuringSchedulingIgnoredDuringExecution:
        allOf:
        - $ref: '#/definitions/v1.NodeSelector'
        description: |-
          If the affinity requirements specified by this field are not met at
          scheduling time, the pod will not be scheduled onto the node.
          If the affinity requirements specified by this field cease to be met
          at some point during pod execution (e.g. due to an update), the system
          may or may not try to eventually evict the pod from its node.
          +optional
    type: object
  v1.NodeSelector:
    properties:
      nodeSelectorTerms:
        description: |-
          Required. A list of node selector terms. The terms are ORed.
          +listType=atomic
        items:
          $ref: '#/definitions/v1.NodeSelectorTerm'
        type: array
    type: object
  v1.NodeSelectorOperator:
    enum:
    - In
    - NotIn
    - Exists
    - DoesNotExist
    - Gt
    - Lt
    type: string
    x-enum-varnames:
    - NodeSelectorOpIn
    - NodeSelectorOpNotIn
    - NodeSelectorOpExists
    - NodeSelectorOpDoesNotExist
    - NodeSelectorOpGt
    - NodeSelectorOpLt
  v1.NodeSelectorRequirement:
    properties:
      key:
        description: The label key that the selector applies to.
        type: string
      operator:
        allOf:
        - $ref: '#/definitions/v1.NodeSelectorOperator'
        description: |-
          Represents a key's relationship to a set of values.
          Valid operators are In, NotIn, Exists, DoesNotExist. Gt, and Lt.
      values:
        description: |-
          An array of string values. If the operator is In or NotIn,
          the values array must be non-empty. If the operator is Exists or DoesNotExist,
          the values array must be empty. If the operator is Gt or Lt, the values
          array must have a single element, which will be interpreted as an integer.
          This array is replaced during a strategic merge patch.
          +optional
          +listType=atomic
        items:
          type: string
        type: array
    type: object
  v1.NodeSelectorTerm:
    properties:
      matchExpressions:
        description: |-
          A list of node selector requirements by node's labels.
          +optional
          +listType=atomic
        items:
          $ref: '#/definitions/v1.NodeSelectorRequirement'
        type: array
      matchFields:
        description: |-
          A list of node selector requirements by node's fields.
          +optional
          +listType=atomic
        items:
          $ref: '#/definitions/v1.NodeSelectorRequirement'
        type: array
    type: object
  v1.ObjectFieldSelector:
    properties:
      apiVersion:
        description: |-
          Version of the schema the FieldPath is written in terms of, defaults to "v1".
          +optional
        type: string
      fieldPath:
        description: Path of the field to select in the specified API version.
        type: string
    type: object
  v1.ObjectMeta:
    properties:
      annotations:
        additionalProperties:
          type: string
        description: |-
          Annotations is an unstructured key value map stored with a resource that may be
          set by external tools to store and retrieve arbitrary metadata. They are not
          queryable and should be preserved when modifying objects.
          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations
          +optional
        type: object
      creationTimestamp:
        description: |-
          CreationTimestamp is a timestamp representing the server time when this object was
          created. It is not guaranteed to be set in happens-before order across separate operations.
          Clients may not set this value. It is represented in RFC3339 form and is in UTC.

          Populated by the system.
          Read-only.
          Null for lists.
          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
          +optional
        type: string
      deletionGracePeriodSeconds:
        description: |-
          Number of seconds allowed for this object to gracefully terminate before
          it will be removed from the system. Only set when deletionTimestamp is also set.
          May only be shortened.
          Read-only.
          +optional
        type: integer
      deletionTimestamp:
        description: |-
          DeletionTimestamp is RFC 3339 date and time at which this resource will be deleted. This
          field is set by the server when a graceful deletion is requested by the user, and is not
          directly settable by a client. The resource is expected to be deleted (no longer visible
          from resource lists, and not reachable by name) after the time in this field, once the
          finalizers list is empty. As long as the finalizers list contains items, deletion is blocked.
          Once the deletionTimestamp is set, this value may not be unset or be set further into the
          future, although it may be shortened or the resource may be deleted prior to this time.
          For example, a user may request that a pod is deleted in 30 seconds. The Kubelet will react
          by sending a graceful termination signal to the containers in the pod. After that 30 seconds,
          the Kubelet will send a hard termination signal (SIGKILL) to the container and after cleanup,
          remove the pod from the API. In the presence of network partitions, this object may still
          exist after this timestamp, until an administrator or automated process can determine the
          resource is fully terminated.
          If not set, graceful deletion of the object has not been requested.

          Populated by the system when a graceful deletion is requested.
          Read-only.
          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#metadata
          +optional
        type: string
      finalizers:
        description: |-
          Must be empty before the object is deleted from the registry. Each entry
          is an identifier for the responsible component that will remove the entry
          from the list. If the deletionTimestamp of the object is non-nil, entries
          in this list can only be removed.
          Finalizers may be processed and removed in any order.  Order is NOT enforced
          because it introduces significant risk of stuck finalizers.
          finalizers is a shared field, any actor with permission can reorder it.
          If the finalizer list is processed in order, then this can lead to a situation
          in which the component responsible for the first finalizer in the list is
          waiting for a signal (field value, external system, or other) produced by a
          component responsible for a finalizer later in the list, resulting in a deadlock.
          Without enforced ordering finalizers are free to order amongst themselves and
          are not vulnerable to ordering changes in the list.
          +optional
          +patchStrategy=merge
          +listType=set
        items:
          type: string
        type: array
      generateName:
        description: |-
          GenerateName is an optional prefix, used by the server, to generate a unique
          name ONLY IF the Name field has not been provided.
          If this field is used, the name returned to the client will be different
          than the name passed. This value will also be combined with a unique suffix.
          The provided value has the same validation rules as the Name field,
          and may be truncated by the length of the suffix required to make the value
          unique on the server.

          If this field is specified and the generated name exists, the server will return a 409.

          Applied only if Name is not specified.
          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#idempotency
          +optional
        type: string
      generation:
        description: |-
          A sequence number representing a specific generation of the desired state.
          Populated by the system. Read-only.
          +optional
        type: integer
      labels:
        additionalProperties:
          type: string
        description: |-
          Map of string keys and values that can be used to organize and categorize
          (scope and select) objects. May match selectors of replication controllers
          and services.
          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels
          +optional
        type: object
      managedFields:
        description: |-
          ManagedFields maps workflow-id and version to the set of fields
          that are managed by that workflow. This is mostly for internal
          housekeeping, and users typically shouldn't need to set or
          understand this field. A workflow can be the user's name, a
          controller's name, or the name of a specific apply path like
          "ci-cd". The set of fields is always in the version that the
          workflow used when modifying the object.

          +optional
          +listType=atomic
        items:
          $ref: '#/definitions/v1.ManagedFieldsEntry'
        type: array
      name:
        description: |-
          Name must be unique within a namespace. Is required when creating resources, although
          some resources may allow a client to request the generation of an appropriate name
          automatically. Name is primarily intended for creation idempotence and configuration
          definition.
          Cannot be updated.
          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names
          +optional
        type: string
      namespace:
        description: |-
          Namespace defines the space within which each name must be unique. An empty namespace is
          equivalent to the "default" namespace, but "default" is the canonical representation.
          Not all objects are required to be scoped to a namespace - the value of this field for
          those objects will be empty.

          Must be a DNS_LABEL.
          Cannot be updated.
          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/namespaces
          +optional
        type: string
      ownerReferences:
        description: |-
          List of objects depended by this object. If ALL objects in the list have
          been deleted, this object will be garbage collected. If this object is managed by a controller,
          then an entry in this list will point to this controller, with the controller field set to true.
          There cannot be more than one managing controller.
          +optional
          +patchMergeKey=uid
          +patchStrategy=merge
          +listType=map
          +listMapKey=uid
        items:
          $ref: '#/definitions/v1.OwnerReference'
        type: array
      resourceVersion:
        description: |-
          An opaque value that represents the internal version of this object that can
          be used by clients to determine when objects have changed. May be used for optimistic
          concurrency, change detection, and the watch operation on a resource or set of resources.
          Clients must treat these values as opaque and passed unmodified back to the server.
          They may only be valid for a particular resource or set of resources.

          Populated by the system.
          Read-only.
          Value must be treated as opaque by clients and .
          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#concurrency-control-and-consistency
          +optional
        type: string
      selfLink:
        description: |-
          Deprecated: selfLink is a legacy read-only field that is no longer populated by the system.
          +optional
        type: string
      uid:
        description: |-
          UID is the unique in time and space value for this object. It is typically generated by
          the server on successful creation of a resource and is not allowed to change on PUT
          operations.

          Populated by the system.
          Read-only.
          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids
          +optional
        type: string
    type: object
  v1.OwnerReference:
    properties:
      apiVersion:
        description: API version of the referent.
        type: string
      blockOwnerDeletion:
        description: |-
          If true, AND if the owner has the "foregroundDeletion" finalizer, then
          the owner cannot be deleted from the key-value store until this
          reference is removed.
          See https://kubernetes.io/docs/concepts/architecture/garbage-collection/#foreground-deletion
          for how the garbage collector interacts with this field and enforces the foreground deletion.
          Defaults to false.
          To set this field, a user needs "delete" permission of the owner,
          otherwise 422 (Unprocessable Entity) will be returned.
          +optional
        type: boolean
      controller:
        description: |-
          If true, this reference points to the managing controller.
          +optional
        type: boolean
      kind:
        description: |-
          Kind of the referent.
          More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
        type: string
      name:
        description: |-
          Name of the referent.
          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#names
        type: string
      uid:
        description: |-
          UID of the referent.
          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names#uids
        type: string
    type: object
  v1.PersistentVolumeAccessMode:
    enum:
    - ReadWriteOnce
    - ReadOnlyMany
    - ReadWriteMany
    - ReadWriteOncePod
    type: string
    x-enum-varnames:
    - ReadWriteOnce
    - ReadOnlyMany
    - ReadWriteMany
    - ReadWriteOncePod
  v1.PersistentVolumeClaimSpec:
    properties:
      accessModes:
        description: |-
          accessModes contains the desired access modes the volume should have.
          More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#access-modes-1
          +optional
          +listType=atomic
        items:
          $ref: '#/definitions/v1.PersistentVolumeAccessMode'
        type: array
      dataSource:
        allOf:
        - $ref: '#/definitions/v1.TypedLocalObjectReference'
        description: |-
          dataSource field can be used to specify either:
          * An existing VolumeSnapshot object (snapshot.storage.k8s.io/VolumeSnapshot)
          * An existing PVC (PersistentVolumeClaim)
          If the provisioner or an external controller can support the specified data source,
          it will create a new volume based on the contents of the specified data source.
          When the AnyVolumeDataSource feature gate is enabled, dataSource contents will be copied to dataSourceRef,
          and dataSourceRef contents will be copied to dataSource when dataSourceRef.namespace is not specified.
          If the namespace is specified, then dataSourceRef will not be copied to dataSource.
          +optional
      dataSourceRef:
        allOf:
        - $ref: '#/definitions/v1.TypedObjectReference'
        description: |-
          dataSourceRef specifies the object from which to populate the volume with data, if a non-empty
          volume is desired. This may be any object from a non-empty API group (non
          core object) or a PersistentVolumeClaim object.
          When this field is specified, volume binding will only succeed if the type of
          the specified object matches some installed volume populator or dynamic
          provisioner.
          This field will replace the functionality of the dataSource field and as such
          if both fields are non-empty, they must have the same value. For backwards
          compatibility, when namespace isn't specified in dataSourceRef,
          both fields (dataSource and dataSourceRef) will be set to the same
          value automatically if one of them is empty and the other is non-empty.
          When namespace is specified in dataSourceRef,
          dataSource isn't set to the same value and must be empty.
          There are three important differences between dataSource and dataSourceRef:
          * While dataSource only allows two specific types of objects, dataSourceRef
            allows any non-core object, as well as PersistentVolumeClaim objects.
          * While dataSource ignores disallowed values (dropping them), dataSourceRef
            preserves all values, and generates an error if a disallowed value is
            specified.
          * While dataSource only allows local objects, dataSourceRef allows objects
            in any namespaces.
          (Beta) Using this field requires the AnyVolumeDataSource feature gate to be enabled.
          (Alpha) Using the namespace field of dataSourceRef requires the CrossNamespaceVolumeDataSource feature gate to be enabled.
          +optional
      resources:
        allOf:
        - $ref: '#/definitions/v1.VolumeResourceRequirements'
        description: |-
          resources represents the minimum resources the volume should have.
          If RecoverVolumeExpansionFailure feature is enabled users are allowed to specify resource requirements
          that are lower than previous value but must still be higher than capacity recorded in the
          status field of the claim.
          More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#resources
          +optional
      selector:
        allOf:
        - $ref: '#/definitions/v1.LabelSelector'
        description: |-
          selector is a label query over volumes to consider for binding.
          +optional
      storageClassName:
        description: |-
          storageClassName is the name of the StorageClass required by the claim.
          More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#class-1
          +optional
        type: string
      volumeAttributesClassName:
        description: |-
          volumeAttributesClassName may be used to set the VolumeAttributesClass used by this claim.
          If specified, the CSI driver will create or update the volume with the attributes defined
          in the corresponding VolumeAttributesClass. This has a different purpose than storageClassName,
          it can be changed after the claim is created. An empty string or nil value indicates that no
          VolumeAttributesClass will be applied to the claim. If the claim enters an Infeasible error state,
          this field can be reset to its previous value (including nil) to cancel the modification.
          If the resource referred to by volumeAttributesClass does not exist, this PersistentVolumeClaim will be
          set to a Pending state, as reflected by the modifyVolumeStatus field, until such as a resource
          exists.
          More info: https://kubernetes.io/docs/concepts/storage/volume-attributes-classes/
          +featureGate=VolumeAttributesClass
          +optional
        type: string
      volumeMode:
        allOf:
        - $ref: '#/definitions/v1.PersistentVolumeMode'
        description: |-
          volumeMode defines what type of volume is required by the claim.
          Value of Filesystem is implied when not included in claim spec.
          +optional
      volumeName:
        description: |-
          volumeName is the binding reference to the PersistentVolume backing this claim.
          +optional
        type: string
    type: object
  v1.PersistentVolumeClaimTemplate:
    properties:
      metadata:
        allOf:
        - $ref: '#/definitions/v1.ObjectMeta'
        description: |-
          May contain labels and annotations that will be copied into the PVC
          when creating it. No other fields are allowed and will be rejected during
          validation.

          +optional
      spec:
        allOf:
        - $ref: '#/definitions/v1.PersistentVolumeClaimSpec'
        description: |-
          The specification for the PersistentVolumeClaim. The entire content is
          copied unchanged into the PVC that gets created from this
          template. The same fields as in a PersistentVolumeClaim
          are also valid here.
    type: object
  v1.PersistentVolumeClaimVolumeSource:
    properties:
      claimName:
        description: |-
          claimName is the name of a PersistentVolumeClaim in the same namespace as the pod using this volume.
          More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims
        type: string
      readOnly:
        description: |-
          readOnly Will force the ReadOnly setting in VolumeMounts.
          Default false.
          +optional
        type: boolean
    type: object
  v1.PersistentVolumeMode:
    enum:
    - Block
    - Filesystem
    type: string
    x-enum-varnames:
    - PersistentVolumeBlock
    - PersistentVolumeFilesystem
  v1.PhotonPersistentDiskVolumeSource:
    properties:
      fsType:
        description: |-
          fsType is the filesystem type to mount.
          Must be a filesystem type supported by the host operating system.
          Ex. "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
        type: string
      pdID:
        description: pdID is the ID that identifies Photon Controller persistent disk
        type: string
    type: object
  v1.PodAffinity:
    properties:
      preferredDuringSchedulingIgnoredDuringExecution:
        description: |-
          The scheduler will prefer to schedule pods to nodes that satisfy
          the affinity expressions specified by this field, but it may choose
          a node that violates one or more of the expressions. The node that is
          most preferred is the one with the greatest sum of weights, i.e.
          for each node that meets all of the scheduling requirements (resource
          request, requiredDuringScheduling affinity expressions, etc.),
          compute a sum by iterating through the elements of this field and adding
          "weight" to the sum if the node has pods which matches the corresponding podAffinityTerm; the
          node(s) with the highest sum are the most preferred.
          +optional
          +listType=atomic
        items:
          $ref: '#/definitions/v1.WeightedPodAffinityTerm'
        type: array
      requiredDuringSchedulingIgnoredDuringExecution:
        description: |-
          If the affinity requirements specified by this field are not met at
          scheduling time, the pod will not be scheduled onto the node.
          If the affinity requirements specified by this field cease to be met
          at some point during pod execution (e.g. due to a pod label update), the
          system may or may not try to eventually evict the pod from its node.
          When there are multiple elements, the lists of nodes corresponding to each
          podAffinityTerm are intersected, i.e. all terms must be satisfied.
          +optional
          +listType=atomic
        items:
          $ref: '#/definitions/v1.PodAffinityTerm'
        type: array
    type: object
  v1.PodAffinityTerm:
    properties:
      labelSelector:
        allOf:
        - $ref: '#/definitions/v1.LabelSelector'
        description: |-
          A label query over a set of resources, in this case pods.
          If it's null, this PodAffinityTerm matches with no Pods.
          +optional
      matchLabelKeys:
        description: |-
          MatchLabelKeys is a set of pod label keys to select which pods will
          be taken into consideration. The keys are used to lookup values from the
          incoming pod labels, those key-value labels are merged with `labelSelector` as `key in (value)`
          to select the group of existing pods which pods will be taken into consideration
          for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
          pod labels will be ignored. The default value is empty.
          The same key is forbidden to exist in both matchLabelKeys and labelSelector.
          Also, matchLabelKeys cannot be set when labelSelector isn't set.

          +listType=atomic
          +optional
        items:
          type: string
        type: array
      mismatchLabelKeys:
        description: |-
          MismatchLabelKeys is a set of pod label keys to select which pods will
          be taken into consideration. The keys are used to lookup values from the
          incoming pod labels, those key-value labels are merged with `labelSelector` as `key notin (value)`
          to select the group of existing pods which pods will be taken into consideration
          for the incoming pod's pod (anti) affinity. Keys that don't exist in the incoming
          pod labels will be ignored. The default value is empty.
          The same key is forbidden to exist in both mismatchLabelKeys and labelSelector.
          Also, mismatchLabelKeys cannot be set when labelSelector isn't set.

          +listType=atomic
          +optional
        items:
          type: string
        type: array
      namespaceSelector:
        allOf:
        - $ref: '#/definitions/v1.LabelSelector'
        description: |-
          A label query over the set of namespaces that the term applies to.
          The term is applied to the union of the namespaces selected by this field
          and the ones listed in the namespaces field.
          null selector and null or empty namespaces list means "this pod's namespace".
          An empty selector ({}) matches all namespaces.
          +optional
      namespaces:
        description: |-
          namespaces specifies a static list of namespace names that the term applies to.
          The term is applied to the union of the namespaces listed in this field
          and the ones selected by namespaceSelector.
          null or empty namespaces list and null namespaceSelector means "this pod's namespace".
          +optional
          +listType=atomic
        items:
          type: string
        type: array
      topologyKey:
        description: |-
          This pod should be co-located (affinity) or not co-located (anti-affinity) with the pods matching
          the labelSelector in the specified namespaces, where co-located is defined as running on a node
          whose value of the label with key topologyKey matches that of any node on which any of the
          selected pods is running.
          Empty topologyKey is not allowed.
        type: string
    type: object
  v1.PodAntiAffinity:
    properties:
      preferredDuringSchedulingIgnoredDuringExecution:
        description: |-
          The scheduler will prefer to schedule pods to nodes that satisfy
          the anti-affinity expressions specified by this field, but it may choose
          a node that violates one or more of the expressions. The node that is
          most preferred is the one with the greatest sum of weights, i.e.
          for each node that meets all of the scheduling requirements (resource
          request, requiredDuringScheduling anti-affinity expressions, etc.),
          compute a sum by iterating through the elements of this field and subtracting
          "weight" from the sum if the node has pods which matches the corresponding podAffinityTerm; the
          node(s) with the highest sum are the most preferred.
          +optional
          +listType=atomic
        items:
          $ref: '#/definitions/v1.WeightedPodAffinityTerm'
        type: array
      requiredDuringSchedulingIgnoredDuringExecution:
        description: |-
          If the anti-affinity requirements specified by this field are not met at
          scheduling time, the pod will not be scheduled onto the node.
          If the anti-affinity requirements specified by this field cease to be met
          at some point during pod execution (e.g. due to a pod label update), the
          system may or may not try to eventually evict the pod from its node.
          When there are multiple elements, the lists of nodes corresponding to each
          podAffinityTerm are intersected, i.e. all terms must be satisfied.
          +optional
          +listType=atomic
        items:
          $ref: '#/definitions/v1.PodAffinityTerm'
        type: array
    type: object
  v1.PodCertificateProjection:
    properties:
      certificateChainPath:
        description: |-
          Write the certificate chain at this path in the projected volume.

          Most applications should use credentialBundlePath.  When using keyPath
          and certificateChainPath, your application needs to check that the key
          and leaf certificate are consistent, because it is possible to read the
          files mid-rotation.

          +optional
        type: string
      credentialBundlePath:
        description: |-
          Write the credential bundle at this path in the projected volume.

          The credential bundle is a single file that contains multiple PEM blocks.
          The first PEM block is a PRIVATE KEY block, containing a PKCS#8 private
          key.

          The remaining blocks are CERTIFICATE blocks, containing the issued
          certificate chain from the signer (leaf and any intermediates).

          Using credentialBundlePath lets your Pod's application code make a single
          atomic read that retrieves a consistent key and certificate chain.  If you
          project them to separate files, your application code will need to
          additionally check that the leaf certificate was issued to the key.

          +optional
        type: string
      keyPath:
        description: |-
          Write the key at this path in the projected volume.

          Most applications should use credentialBundlePath.  When using keyPath
          and certificateChainPath, your application needs to check that the key
          and leaf certificate are consistent, because it is possible to read the
          files mid-rotation.

          +optional
        type: string
      keyType:
        description: |-
          The type of keypair Kubelet will generate for the pod.

          Valid values are "RSA3072", "RSA4096", "ECDSAP256", "ECDSAP384",
          "ECDSAP521", and "ED25519".

          +required
        type: string
      maxExpirationSeconds:
        description: |-
          maxExpirationSeconds is the maximum lifetime permitted for the
          certificate.

          Kubelet copies this value verbatim into the PodCertificateRequests it
          generates for this projection.

          If omitted, kube-apiserver will set it to 86400(24 hours). kube-apiserver
          will reject values shorter than 3600 (1 hour).  The maximum allowable
          value is 7862400 (91 days).

          The signer implementation is then free to issue a certificate with any
          lifetime *shorter* than MaxExpirationSeconds, but no shorter than 3600
          seconds (1 hour).  This constraint is enforced by kube-apiserver.
          `kubernetes.io` signers will never issue certificates with a lifetime
          longer than 24 hours.

          +optional
        type: integer
      signerName:
        description: |-
          Kubelet's generated CSRs will be addressed to this signer.

          +required
        type: string
    type: object
  v1.PortworxVolumeSource:
    properties:
      fsType:
        description: |-
          fSType represents the filesystem type to mount
          Must be a filesystem type supported by the host operating system.
          Ex. "ext4", "xfs". Implicitly inferred to be "ext4" if unspecified.
        type: string
      readOnly:
        description: |-
          readOnly defaults to false (read/write). ReadOnly here will force
          the ReadOnly setting in VolumeMounts.
          +optional
        type: boolean
      volumeID:
        description: volumeID uniquely identifies a Portworx volume
        type: string
    type: object
  v1.PreferredSchedulingTerm:
    properties:
      preference:
        allOf:
        - $ref: '#/definitions/v1.NodeSelectorTerm'
        description: A node selector term, associated with the corresponding weight.
      weight:
        description: Weight associated with matching the corresponding nodeSelectorTerm,
          in the range 1-100.
        type: integer
    type: object
  v1.Probe:
    properties:
      exec:
        allOf:
        - $ref: '#/definitions/v1.ExecAction'
        description: |-
          Exec specifies a command to execute in the container.
          +optional
      failureThreshold:
        description: |-
          Minimum consecutive failures for the probe to be considered failed after having succeeded.
          Defaults to 3. Minimum value is 1.
          +optional
        type: integer
      grpc:
        allOf:
        - $ref: '#/definitions/v1.GRPCAction'
        description: |-
          GRPC specifies a GRPC HealthCheckRequest.
          +optional
      httpGet:
        allOf:
        - $ref: '#/definitions/v1.HTTPGetAction'
        description: |-
          HTTPGet specifies an HTTP GET request to perform.
          +optional
      initialDelaySeconds:
        description: |-
          Number of seconds after the container has started before liveness probes are initiated.
          More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes
          +optional
        type: integer
      periodSeconds:
        description: |-
          How often (in seconds) to perform the probe.
          Default to 10 seconds. Minimum value is 1.
          +optional
        type: integer
      successThreshold:
        description: |-
          Minimum consecutive successes for the probe to be considered successful after having failed.
          Defaults to 1. Must be 1 for liveness and startup. Minimum value is 1.
          +optional
        type: integer
      tcpSocket:
        allOf:
        - $ref: '#/definitions/v1.TCPSocketAction'
        description: |-
          TCPSocket specifies a connection to a TCP port.
          +optional
      terminationGracePeriodSeconds:
        description: |-
          Optional duration in seconds the pod needs to terminate gracefully upon probe failure.
          The grace period is the duration in seconds after the processes running in the pod are sent
          a termination signal and the time when the processes are forcibly halted with a kill signal.
          Set this value longer than the expected cleanup time for your process.
          If this value is nil, the pod's terminationGracePeriodSeconds will be used. Otherwise, this
          value overrides the value provided by the pod spec.
          Value must be non-negative integer. The value zero indicates stop immediately via
          the kill signal (no opportunity to shut down).
          This is a beta field and requires enabling ProbeTerminationGracePeriod feature gate.
          Minimum value is 1. spec.terminationGracePeriodSeconds is used if unset.
          +optional
        type: integer
      timeoutSeconds:
        description: |-
          Number of seconds after which the probe times out.
          Defaults to 1 second. Minimum value is 1.
          More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes
          +optional
        type: integer
    type: object
  v1.ProcMountType:
    enum:
    - Default
    - Unmasked
    type: string
    x-enum-varnames:
    - DefaultProcMount
    - UnmaskedProcMount
  v1.ProjectedVolumeSource:
    properties:
      defaultMode:
        description: |-
          defaultMode are the mode bits used to set permissions on created files by default.
          Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511.
          YAML accepts both octal and decimal values, JSON requires decimal values for mode bits.
          Directories within the path are not affected by this setting.
          This might be in conflict with other options that affect the file
          mode, like fsGroup, and the result can be other mode bits set.
          +optional
        type: integer
      sources:
        description: |-
          sources is the list of volume projections. Each entry in this list
          handles one source.
          +optional
          +listType=atomic
        items:
          $ref: '#/definitions/v1.VolumeProjection'
        type: array
    type: object
  v1.Protocol:
    enum:
    - TCP
    - UDP
    - SCTP
    type: string
    x-enum-varnames:
    - ProtocolTCP
    - ProtocolUDP
    - ProtocolSCTP
  v1.PullPolicy:
    enum:
    - Always
    - Never
    - IfNotPresent
    type: string
    x-enum-varnames:
    - PullAlways
    - PullNever
    - PullIfNotPresent
  v1.QuobyteVolumeSource:
    properties:
      group:
        description: |-
          group to map volume access to
          Default is no group
          +optional
        type: string
      readOnly:
        description: |-
          readOnly here will force the Quobyte volume to be mounted with read-only permissions.
          Defaults to false.
          +optional
        type: boolean
      registry:
        description: |-
          registry represents a single or multiple Quobyte Registry services
          specified as a string as host:port pair (multiple entries are separated with commas)
          which acts as the central registry for volumes
        type: string
      tenant:
        description: |-
          tenant owning the given Quobyte volume in the Backend
          Used with dynamically provisioned Quobyte volumes, value is set by the plugin
          +optional
        type: string
      user:
        description: |-
          user to map volume access to
          Defaults to serivceaccount user
          +optional
        type: string
      volume:
        description: volume is a string that references an already created Quobyte
          volume by name.
        type: string
    type: object
  v1.RBDVolumeSource:
    properties:
      fsType:
        description: |-
          fsType is the filesystem type of the volume that you want to mount.
          Tip: Ensure that the filesystem type is supported by the host operating system.
          Examples: "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
          More info: https://kubernetes.io/docs/concepts/storage/volumes#rbd
          TODO: how do we prevent errors in the filesystem from compromising the machine
          +optional
        type: string
      image:
        description: |-
          image is the rados image name.
          More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it
        type: string
      keyring:
        description: |-
          keyring is the path to key ring for RBDUser.
          Default is /etc/ceph/keyring.
          More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it
          +optional
          +default="/etc/ceph/keyring"
        type: string
      monitors:
        description: |-
          monitors is a collection of Ceph monitors.
          More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it
          +listType=atomic
        items:
          type: string
        type: array
      pool:
        description: |-
          pool is the rados pool name.
          Default is rbd.
          More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it
          +optional
          +default="rbd"
        type: string
      readOnly:
        description: |-
          readOnly here will force the ReadOnly setting in VolumeMounts.
          Defaults to false.
          More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it
          +optional
        type: boolean
      secretRef:
        allOf:
        - $ref: '#/definitions/v1.LocalObjectReference'
        description: |-
          secretRef is name of the authentication secret for RBDUser. If provided
          overrides keyring.
          Default is nil.
          More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it
          +optional
      user:
        description: |-
          user is the rados user name.
          Default is admin.
          More info: https://examples.k8s.io/volumes/rbd/README.md#how-to-use-it
          +optional
          +default="admin"
        type: string
    type: object
  v1.RecursiveReadOnlyMode:
    enum:
    - Disabled
    - IfPossible
    - Enabled
    type: string
    x-enum-varnames:
    - RecursiveReadOnlyDisabled
    - RecursiveReadOnlyIfPossible
    - RecursiveReadOnlyEnabled
  v1.ResourceFieldSelector:
    properties:
      containerName:
        description: |-
          Container name: required for volumes, optional for env vars
          +optional
        type: string
      divisor:
        allOf:
        - $ref: '#/definitions/resource.Quantity'
        description: |-
          Specifies the output format of the exposed resources, defaults to "1"
          +optional
      resource:
        description: 'Required: resource to select'
        type: string
    type: object
  v1.ResourceList:
    additionalProperties:
      $ref: '#/definitions/resource.Quantity'
    type: object
  v1.ResourceName:
    enum:
    - cpu
    - memory
    - storage
    - ephemeral-storage
    - pods
    - services
    - replicationcontrollers
    - resourcequotas
    - secrets
    - configmaps
    - persistentvolumeclaims
    - services.nodeports
    - services.loadbalancers
    - requests.cpu
    - requests.memory
    - requests.storage
    - requests.ephemeral-storage
    - limits.cpu
    - limits.memory
    - limits.ephemeral-storage
    type: string
    x-enum-varnames:
    - ResourceCPU
    - ResourceMemory
    - ResourceStorage
    - ResourceEphemeralStorage
    - ResourcePods
    - ResourceServices
    - ResourceReplicationControllers
    - ResourceQuotas
    - ResourceSecrets
    - ResourceConfigMaps
    - ResourcePersistentVolumeClaims
    - ResourceServicesNodePorts
    - ResourceServicesLoadBalancers
    - ResourceRequestsCPU
    - ResourceRequestsMemory
    - ResourceRequestsStorage
    - ResourceRequestsEphemeralStorage
    - ResourceLimitsCPU
    - ResourceLimitsMemory
    - ResourceLimitsEphemeralStorage
  v1.ResourceRequirements:
    properties:
      claims:
        description: |-
          Claims lists the names of resources, defined in spec.resourceClaims,
          that are used by this container.

          This field depends on the
          DynamicResourceAllocation feature gate.

          This field is immutable. It can only be set for containers.

          +listType=map
          +listMapKey=name
          +featureGate=DynamicResourceAllocation
          +optional
        items:
          $ref: '#/definitions/k8s_io_api_core_v1.ResourceClaim'
        type: array
      limits:
        allOf:
        - $ref: '#/definitions/v1.ResourceList'
        description: |-
          Limits describes the maximum amount of compute resources allowed.
          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
          +optional
      requests:
        allOf:
        - $ref: '#/definitions/v1.ResourceList'
        description: |-
          Requests describes the minimum amount of compute resources required.
          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
          otherwise to an implementation-defined value. Requests cannot exceed Limits.
          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
          +optional
    type: object
  v1.ResourceResizeRestartPolicy:
    enum:
    - NotRequired
    - RestartContainer
    type: string
    x-enum-varnames:
    - NotRequired
    - RestartContainer
  v1.SELinuxOptions:
    properties:
      level:
        description: |-
          Level is SELinux level label that applies to the container.
          +optional
        type: string
      role:
        description: |-
          Role is a SELinux role label that applies to the container.
          +optional
        type: string
      type:
        description: |-
          Type is a SELinux type label that applies to the container.
          +optional
        type: string
      user:
        description: |-
          User is a SELinux user label that applies to the container.
          +optional
        type: string
    type: object
  v1.ScaleIOVolumeSource:
    properties:
      fsType:
        description: |-
          fsType is the filesystem type to mount.
          Must be a filesystem type supported by the host operating system.
          Ex. "ext4", "xfs", "ntfs".
          Default is "xfs".
          +optional
          +default="xfs"
        type: string
      gateway:
        description: gateway is the host address of the ScaleIO API Gateway.
        type: string
      protectionDomain:
        description: |-
          protectionDomain is the name of the ScaleIO Protection Domain for the configured storage.
          +optional
        type: string
      readOnly:
        description: |-
          readOnly Defaults to false (read/write). ReadOnly here will force
          the ReadOnly setting in VolumeMounts.
          +optional
        type: boolean
      secretRef:
        allOf:
        - $ref: '#/definitions/v1.LocalObjectReference'
        description: |-
          secretRef references to the secret for ScaleIO user and other
          sensitive information. If this is not provided, Login operation will fail.
      sslEnabled:
        description: |-
          sslEnabled Flag enable/disable SSL communication with Gateway, default false
          +optional
        type: boolean
      storageMode:
        description: |-
          storageMode indicates whether the storage for a volume should be ThickProvisioned or ThinProvisioned.
          Default is ThinProvisioned.
          +optional
          +default="ThinProvisioned"
        type: string
      storagePool:
        description: |-
          storagePool is the ScaleIO Storage Pool associated with the protection domain.
          +optional
        type: string
      system:
        description: system is the name of the storage system as configured in ScaleIO.
        type: string
      volumeName:
        description: |-
          volumeName is the name of a volume already created in the ScaleIO system
          that is associated with this volume source.
        type: string
    type: object
  v1.SeccompProfile:
    properties:
      localhostProfile:
        description: |-
          localhostProfile indicates a profile defined in a file on the node should be used.
          The profile must be preconfigured on the node to work.
          Must be a descending path, relative to the kubelet's configured seccomp profile location.
          Must be set if type is "Localhost". Must NOT be set for any other type.
          +optional
        type: string
      type:
        allOf:
        - $ref: '#/definitions/v1.SeccompProfileType'
        description: |-
          type indicates which kind of seccomp profile will be applied.
          Valid options are:

          Localhost - a profile defined in a file on the node should be used.
          RuntimeDefault - the container runtime default profile should be used.
          Unconfined - no profile should be applied.
          +unionDiscriminator
    type: object
  v1.SeccompProfileType:
    enum:
    - Unconfined
    - RuntimeDefault
    - Localhost
    type: string
    x-enum-varnames:
    - SeccompProfileTypeUnconfined
    - SeccompProfileTypeRuntimeDefault
    - SeccompProfileTypeLocalhost
  v1.SecretEnvSource:
    properties:
      name:
        description: |-
          Name of the referent.
          This field is effectively required, but due to backwards compatibility is
          allowed to be empty. Instances of this type with an empty value here are
          almost certainly wrong.
          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
          +optional
          +default=""
          +kubebuilder:default=""
          TODO: Drop `kubebuilder:default` when controller-gen doesn't need it https://github.com/kubernetes-sigs/kubebuilder/issues/3896.
        type: string
      optional:
        description: |-
          Specify whether the Secret must be defined
          +optional
        type: boolean
    type: object
  v1.SecretKeySelector:
    properties:
      key:
        description: The key of the secret to select from.  Must be a valid secret
          key.
        type: string
      name:
        description: |-
          Name of the referent.
          This field is effectively required, but due to backwards compatibility is
          allowed to be empty. Instances of this type with an empty value here are
          almost certainly wrong.
          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
          +optional
          +default=""
          +kubebuilder:default=""
          TODO: Drop `kubebuilder:default` when controller-gen doesn't need it https://github.com/kubernetes-sigs/kubebuilder/issues/3896.
        type: string
      optional:
        description: |-
          Specify whether the Secret or its key must be defined
          +optional
        type: boolean
    type: object
  v1.SecretProjection:
    properties:
      items:
        description: |-
          items if unspecified, each key-value pair in the Data field of the referenced
          Secret will be projected into the volume as a file whose name is the
          key and content is the value. If specified, the listed keys will be
          projected into the specified paths, and unlisted keys will not be
          present. If a key is specified which is not present in the Secret,
          the volume setup will error unless it is marked optional. Paths must be
          relative and may not contain the '..' path or start with '..'.
          +optional
          +listType=atomic
        items:
          $ref: '#/definitions/v1.KeyToPath'
        type: array
      name:
        description: |-
          Name of the referent.
          This field is effectively required, but due to backwards compatibility is
          allowed to be empty. Instances of this type with an empty value here are
          almost certainly wrong.
          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
          +optional
          +default=""
          +kubebuilder:default=""
          TODO: Drop `kubebuilder:default` when controller-gen doesn't need it https://github.com/kubernetes-sigs/kubebuilder/issues/3896.
        type: string
      optional:
        description: |-
          optional field specify whether the Secret or its key must be defined
          +optional
        type: boolean
    type: object
  v1.SecretVolumeSource:
    properties:
      defaultMode:
        description: |-
          defaultMode is Optional: mode bits used to set permissions on created files by default.
          Must be an octal value between 0000 and 0777 or a decimal value between 0 and 511.
          YAML accepts both octal and decimal values, JSON requires decimal values
          for mode bits. Defaults to 0644.
          Directories within the path are not affected by this setting.
          This might be in conflict with other options that affect the file
          mode, like fsGroup, and the result can be other mode bits set.
          +optional
        type: integer
      items:
        description: |-
          items If unspecified, each key-value pair in the Data field of the referenced
          Secret will be projected into the volume as a file whose name is the
          key and content is the value. If specified, the listed keys will be
          projected into the specified paths, and unlisted keys will not be
          present. If a key is specified which is not present in the Secret,
          the volume setup will error unless it is marked optional. Paths must be
          relative and may not contain the '..' path or start with '..'.
          +optional
          +listType=atomic
        items:
          $ref: '#/definitions/v1.KeyToPath'
        type: array
      optional:
        description: |-
          optional field specify whether the Secret or its keys must be defined
          +optional
        type: boolean
      secretName:
        description: |-
          secretName is the name of the secret in the pod's namespace to use.
          More info: https://kubernetes.io/docs/concepts/storage/volumes#secret
          +optional
        type: string
    type: object
  v1.SecurityContext:
    properties:
      allowPrivilegeEscalation:
        description: |-
          AllowPrivilegeEscalation controls whether a process can gain more
          privileges than its parent process. This bool directly controls if
          the no_new_privs flag will be set on the container process.
          AllowPrivilegeEscalation is true always when the container is:
          1) run as Privileged
          2) has CAP_SYS_ADMIN
          Note that this field cannot be set when spec.os.name is windows.
          +optional
        type: boolean
      appArmorProfile:
        allOf:
        - $ref: '#/definitions/v1.AppArmorProfile'
        description: |-
          appArmorProfile is the AppArmor options to use by this container. If set, this profile
          overrides the pod's appArmorProfile.
          Note that this field cannot be set when spec.os.name is windows.
          +optional
      capabilities:
        allOf:
        - $ref: '#/definitions/v1.Capabilities'
        description: |-
          The capabilities to add/drop when running containers.
          Defaults to the default set of capabilities granted by the container runtime.
          Note that this field cannot be set when spec.os.name is windows.
          +optional
      privileged:
        description: |-
          Run container in privileged mode.
          Processes in privileged containers are essentially equivalent to root on the host.
          Defaults to false.
          Note that this field cannot be set when spec.os.name is windows.
          +optional
        type: boolean
      procMount:
        allOf:
        - $ref: '#/definitions/v1.ProcMountType'
        description: |-
          procMount denotes the type of proc mount to use for the containers.
          The default value is Default which uses the container runtime defaults for
          readonly paths and masked paths.
          This requires the ProcMountType feature flag to be enabled.
          Note that this field cannot be set when spec.os.name is windows.
          +optional
      readOnlyRootFilesystem:
        description: |-
          Whether this container has a read-only root filesystem.
          Default is false.
          Note that this field cannot be set when spec.os.name is windows.
          +optional
        type: boolean
      runAsGroup:
        description: |-
          The GID to run the entrypoint of the container process.
          Uses runtime default if unset.
          May also be set in PodSecurityContext.  If set in both SecurityContext and
          PodSecurityContext, the value specified in SecurityContext takes precedence.
          Note that this field cannot be set when spec.os.name is windows.
          +optional
        type: integer
      runAsNonRoot:
        description: |-
          Indicates that the container must run as a non-root user.
          If true, the Kubelet will validate the image at runtime to ensure that it
          does not run as UID 0 (root) and fail to start the container if it does.
          If unset or false, no such validation will be performed.
          May also be set in PodSecurityContext.  If set in both SecurityContext and
          PodSecurityContext, the value specified in SecurityContext takes precedence.
          +optional
        type: boolean
      runAsUser:
        description: |-
          The UID to run the entrypoint of the container process.
          Defaults to user specified in image metadata if unspecified.
          May also be set in PodSecurityContext.  If set in both SecurityContext and
          PodSecurityContext, the value specified in SecurityContext takes precedence.
          Note that this field cannot be set when spec.os.name is windows.
          +optional
        type: integer
      seLinuxOptions:
        allOf:
        - $ref: '#/definitions/v1.SELinuxOptions'
        description: |-
          The SELinux context to be applied to the container.
          If unspecified, the container runtime will allocate a random SELinux context for each
          container.  May also be set in PodSecurityContext.  If set in both SecurityContext and
          PodSecurityContext, the value specified in SecurityContext takes precedence.
          Note that this field cannot be set when spec.os.name is windows.
          +optional
      seccompProfile:
        allOf:
        - $ref: '#/definitions/v1.SeccompProfile'
        description: |-
          The seccomp options to use by this container. If seccomp options are
          provided at both the pod & container level, the container options
          override the pod options.
          Note that this field cannot be set when spec.os.name is windows.
          +optional
      windowsOptions:
        allOf:
        - $ref: '#/definitions/v1.WindowsSecurityContextOptions'
        description: |-
          The Windows specific settings applied to all containers.
          If unspecified, the options from the PodSecurityContext will be used.
          If set in both SecurityContext and PodSecurityContext, the value specified in SecurityContext takes precedence.
          Note that this field cannot be set when spec.os.name is linux.
          +optional
    type: object
  v1.ServiceAccountTokenProjection:
    properties:
      audience:
        description: |-
          audience is the intended audience of the token. A recipient of a token
          must identify itself with an identifier specified in the audience of the
          token, and otherwise should reject the token. The audience defaults to the
          identifier of the apiserver.
          +optional
        type: string
      expirationSeconds:
        description: |-
          expirationSeconds is the requested duration of validity of the service
          account token. As the token approaches expiration, the kubelet volume
          plugin will proactively rotate the service account token. The kubelet will
          start trying to rotate the token if the token is older than 80 percent of
          its time to live or if the token is older than 24 hours.Defaults to 1 hour
          and must be at least 10 minutes.
          +optional
        type: integer
      path:
        description: |-
          path is the path relative to the mount point of the file to project the
          token into.
        type: string
    type: object
  v1.Signal:
    enum:
    - SIGABRT
    - SIGALRM
    - SIGBUS
    - SIGCHLD
    - SIGCLD
    - SIGCONT
    - SIGFPE
    - SIGHUP
    - SIGILL
    - SIGINT
    - SIGIO
    - SIGIOT
    - SIGKILL
    - SIGPIPE
    - SIGPOLL
    - SIGPROF
    - SIGPWR
    - SIGQUIT
    - SIGSEGV
    - SIGSTKFLT
    - SIGSTOP
    - SIGSYS
    - SIGTERM
    - SIGTRAP
    - SIGTSTP
    - SIGTTIN
    - SIGTTOU
    - SIGURG
    - SIGUSR1
    - SIGUSR2
    - SIGVTALRM
    - SIGWINCH
    - SIGXCPU
    - SIGXFSZ
    - SIGRTMIN
    - SIGRTMIN+1
    - SIGRTMIN+2
    - SIGRTMIN+3
    - SIGRTMIN+4
    - SIGRTMIN+5
    - SIGRTMIN+6
    - SIGRTMIN+7
    - SIGRTMIN+8
    - SIGRTMIN+9
    - SIGRTMIN+10
    - SIGRTMIN+11
    - SIGRTMIN+12
    - SIGRTMIN+13
    - SIGRTMIN+14
    - SIGRTMIN+15
    - SIGRTMAX-14
    - SIGRTMAX-13
    - SIGRTMAX-12
    - SIGRTMAX-11
    - SIGRTMAX-10
    - SIGRTMAX-9
    - SIGRTMAX-8
    - SIGRTMAX-7
    - SIGRTMAX-6
    - SIGRTMAX-5
    - SIGRTMAX-4
    - SIGRTMAX-3
    - SIGRTMAX-2
    - SIGRTMAX-1
    - SIGRTMAX
    type: string
    x-enum-varnames:
    - SIGABRT
    - SIGALRM
    - SIGBUS
    - SIGCHLD
    - SIGCLD
    - SIGCONT
    - SIGFPE
    - SIGHUP
    - SIGILL
    - SIGINT
    - SIGIO
    - SIGIOT
    - SIGKILL
    - SIGPIPE
    - SIGPOLL
    - SIGPROF
    - SIGPWR
    - SIGQUIT
    - SIGSEGV
    - SIGSTKFLT
    - SIGSTOP
    - SIGSYS
    - SIGTERM
    - SIGTRAP
    - SIGTSTP
    - SIGTTIN
    - SIGTTOU
    - SIGURG
    - SIGUSR1
    - SIGUSR2
    - SIGVTALRM
    - SIGWINCH
    - SIGXCPU
    - SIGXFSZ
    - SIGRTMIN
    - SIGRTMINPLUS1
    - SIGRTMINPLUS2
    - SIGRTMINPLUS3
    - SIGRTMINPLUS4
    - SIGRTMINPLUS5
    - SIGRTMINPLUS6
    - SIGRTMINPLUS7
    - SIGRTMINPLUS8
    - SIGRTMINPLUS9
    - SIGRTMINPLUS10
    - SIGRTMINPLUS11
    - SIGRTMINPLUS12
    - SIGRTMINPLUS13
    - SIGRTMINPLUS14
    - SIGRTMINPLUS15
    - SIGRTMAXMINUS14
    - SIGRTMAXMINUS13
    - SIGRTMAXMINUS12
    - SIGRTMAXMINUS11
    - SIGRTMAXMINUS10
    - SIGRTMAXMINUS9
    - SIGRTMAXMINUS8
    - SIGRTMAXMINUS7
    - SIGRTMAXMINUS6
    - SIGRTMAXMINUS5
    - SIGRTMAXMINUS4
    - SIGRTMAXMINUS3
    - SIGRTMAXMINUS2
    - SIGRTMAXMINUS1
    - SIGRTMAX
  v1.SleepAction:
    properties:
      seconds:
        description: Seconds is the number of seconds to sleep.
        type: integer
    type: object
  v1.StorageMedium:
    enum:
    - ""
    - Memory
    - HugePages
    - HugePages-
    type: string
    x-enum-comments:
      StorageMediumDefault: use whatever the default is for the node, assume anything
        we don't explicitly handle is this
      StorageMediumHugePages: use hugepages
      StorageMediumHugePagesPrefix: prefix for full medium notation HugePages-<size>
      StorageMediumMemory: use memory (e.g. tmpfs on linux)
    x-enum-descriptions:
    - use whatever the default is for the node, assume anything we don't explicitly
      handle is this
    - use memory (e.g. tmpfs on linux)
    - use hugepages
    - prefix for full medium notation HugePages-<size>
    x-enum-varnames:
    - StorageMediumDefault
    - StorageMediumMemory
    - StorageMediumHugePages
    - StorageMediumHugePagesPrefix
  v1.StorageOSVolumeSource:
    properties:
      fsType:
        description: |-
          fsType is the filesystem type to mount.
          Must be a filesystem type supported by the host operating system.
          Ex. "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
          +optional
        type: string
      readOnly:
        description: |-
          readOnly defaults to false (read/write). ReadOnly here will force
          the ReadOnly setting in VolumeMounts.
          +optional
        type: boolean
      secretRef:
        allOf:
        - $ref: '#/definitions/v1.LocalObjectReference'
        description: |-
          secretRef specifies the secret to use for obtaining the StorageOS API
          credentials.  If not specified, default values will be attempted.
          +optional
      volumeName:
        description: |-
          volumeName is the human-readable name of the StorageOS volume.  Volume
          names are only unique within a namespace.
        type: string
      volumeNamespace:
        description: |-
          volumeNamespace specifies the scope of the volume within StorageOS.  If no
          namespace is specified then the Pod's namespace will be used.  This allows the
          Kubernetes name scoping to be mirrored within StorageOS for tighter integration.
          Set VolumeName to any name to override the default behaviour.
          Set to "default" if you are not using namespaces within StorageOS.
          Namespaces that do not pre-exist within StorageOS will be created.
          +optional
        type: string
    type: object
  v1.TCPSocketAction:
    properties:
      host:
        description: |-
          Optional: Host name to connect to, defaults to the pod IP.
          +optional
        type: string
      port:
        allOf:
        - $ref: '#/definitions/intstr.IntOrString'
        description: |-
          Number or name of the port to access on the container.
          Number must be in the range 1 to 65535.
          Name must be an IANA_SVC_NAME.
    type: object
  v1.Taint:
    properties:
      effect:
        allOf:
        - $ref: '#/definitions/v1.TaintEffect'
        description: |-
          Required. The effect of the taint on pods
          that do not tolerate the taint.
          Valid effects are NoSchedule, PreferNoSchedule and NoExecute.
      key:
        description: Required. The taint key to be applied to a node.
        type: string
      timeAdded:
        description: |-
          TimeAdded represents the time at which the taint was added.
          +optional
        type: string
      value:
        description: |-
          The taint value corresponding to the taint key.
          +optional
        type: string
    type: object
  v1.TaintEffect:
    enum:
    - NoSchedule
    - PreferNoSchedule
    - NoExecute
    type: string
    x-enum-varnames:
    - TaintEffectNoSchedule
    - TaintEffectPreferNoSchedule
    - TaintEffectNoExecute
  v1.TerminationMessagePolicy:
    enum:
    - File
    - FallbackToLogsOnError
    type: string
    x-enum-varnames:
    - TerminationMessageReadFile
    - TerminationMessageFallbackToLogsOnError
  v1.Toleration:
    properties:
      effect:
        allOf:
        - $ref: '#/definitions/v1.TaintEffect'
        description: |-
          Effect indicates the taint effect to match. Empty means match all taint effects.
          When specified, allowed values are NoSchedule, PreferNoSchedule and NoExecute.
          +optional
      key:
        description: |-
          Key is the taint key that the toleration applies to. Empty means match all taint keys.
          If the key is empty, operator must be Exists; this combination means to match all values and all keys.
          +optional
        type: string
      operator:
        allOf:
        - $ref: '#/definitions/v1.TolerationOperator'
        description: |-
          Operator represents a key's relationship to the value.
          Valid operators are Exists and Equal. Defaults to Equal.
          Exists is equivalent to wildcard for value, so that a pod can
          tolerate all taints of a particular category.
          +optional
      tolerationSeconds:
        description: |-
          TolerationSeconds represents the period of time the toleration (which must be
          of effect NoExecute, otherwise this field is ignored) tolerates the taint. By default,
          it is not set, which means tolerate the taint forever (do not evict). Zero and
          negative values will be treated as 0 (evict immediately) by the system.
          +optional
        type: integer
      value:
        description: |-
          Value is the taint value the toleration matches to.
          If the operator is Exists, the value should be empty, otherwise just a regular string.
          +optional
        type: string
    type: object
  v1.TolerationOperator:
    enum:
    - Exists
    - Equal
    type: string
    x-enum-varnames:
    - TolerationOpExists
    - TolerationOpEqual
  v1.TypedLocalObjectReference:
    properties:
      apiGroup:
        description: |-
          APIGroup is the group for the resource being referenced.
          If APIGroup is not specified, the specified Kind must be in the core API group.
          For any other third-party types, APIGroup is required.
          +optional
        type: string
      kind:
        description: Kind is the type of resource being referenced
        type: string
      name:
        description: Name is the name of resource being referenced
        type: string
    type: object
  v1.TypedObjectReference:
    properties:
      apiGroup:
        description: |-
          APIGroup is the group for the resource being referenced.
          If APIGroup is not specified, the specified Kind must be in the core API group.
          For any other third-party types, APIGroup is required.
          +optional
        type: string
      kind:
        description: Kind is the type of resource being referenced
        type: string
      name:
        description: Name is the name of resource being referenced
        type: string
      namespace:
        description: |-
          Namespace is the namespace of resource being referenced
          Note that when a namespace is specified, a gateway.networking.k8s.io/ReferenceGrant object is required in the referent namespace to allow that namespace's owner to accept the reference. See the ReferenceGrant documentation for details.
          (Alpha) This field requires the CrossNamespaceVolumeDataSource feature gate to be enabled.
          +featureGate=CrossNamespaceVolumeDataSource
          +optional
        type: string
    type: object
  v1.URIScheme:
    enum:
    - HTTP
    - HTTPS
    type: string
    x-enum-varnames:
    - URISchemeHTTP
    - URISchemeHTTPS
  v1.Volume:
    properties:
      awsElasticBlockStore:
        allOf:
        - $ref: '#/definitions/v1.AWSElasticBlockStoreVolumeSource'
        description: |-
          awsElasticBlockStore represents an AWS Disk resource that is attached to a
          kubelet's host machine and then exposed to the pod.
          Deprecated: AWSElasticBlockStore is deprecated. All operations for the in-tree
          awsElasticBlockStore type are redirected to the ebs.csi.aws.com CSI driver.
          More info: https://kubernetes.io/docs/concepts/storage/volumes#awselasticblockstore
          +optional
      azureDisk:
        allOf:
        - $ref: '#/definitions/v1.AzureDiskVolumeSource'
        description: |-
          azureDisk represents an Azure Data Disk mount on the host and bind mount to the pod.
          Deprecated: AzureDisk is deprecated. All operations for the in-tree azureDisk type
          are redirected to the disk.csi.azure.com CSI driver.
          +optional
      azureFile:
        allOf:
        - $ref: '#/definitions/v1.AzureFileVolumeSource'
        description: |-
          azureFile represents an Azure File Service mount on the host and bind mount to the pod.
          Deprecated: AzureFile is deprecated. All operations for the in-tree azureFile type
          are redirected to the file.csi.azure.com CSI driver.
          +optional
      cephfs:
        allOf:
        - $ref: '#/definitions/v1.CephFSVolumeSource'
        description: |-
          cephFS represents a Ceph FS mount on the host that shares a pod's lifetime.
          Deprecated: CephFS is deprecated and the in-tree cephfs type is no longer supported.
          +optional
      cinder:
        allOf:
        - $ref: '#/definitions/v1.CinderVolumeSource'
        description: |-
          cinder represents a cinder volume attached and mounted on kubelets host machine.
          Deprecated: Cinder is deprecated. All operations for the in-tree cinder type
          are redirected to the cinder.csi.openstack.org CSI driver.
          More info: https://examples.k8s.io/mysql-cinder-pd/README.md
          +optional
      configMap:
        allOf:
        - $ref: '#/definitions/v1.ConfigMapVolumeSource'
        description: |-
          configMap represents a configMap that should populate this volume
          +optional
      csi:
        allOf:
        - $ref: '#/definitions/v1.CSIVolumeSource'
        description: |-
          csi (Container Storage Interface) represents ephemeral storage that is handled by certain external CSI drivers.
          +optional
      downwardAPI:
        allOf:
        - $ref: '#/definitions/v1.DownwardAPIVolumeSource'
        description: |-
          downwardAPI represents downward API about the pod that should populate this volume
          +optional
      emptyDir:
        allOf:
        - $ref: '#/definitions/v1.EmptyDirVolumeSource'
        description: |-
          emptyDir represents a temporary directory that shares a pod's lifetime.
          More info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir
          +optional
      ephemeral:
        allOf:
        - $ref: '#/definitions/v1.EphemeralVolumeSource'
        description: |-
          ephemeral represents a volume that is handled by a cluster storage driver.
          The volume's lifecycle is tied to the pod that defines it - it will be created before the pod starts,
          and deleted when the pod is removed.

          Use this if:
          a) the volume is only needed while the pod runs,
          b) features of normal volumes like restoring from snapshot or capacity
             tracking are needed,
          c) the storage driver is specified through a storage class, and
          d) the storage driver supports dynamic volume provisioning through
             a PersistentVolumeClaim (see EphemeralVolumeSource for more
             information on the connection between this volume type
             and PersistentVolumeClaim).

          Use PersistentVolumeClaim or one of the vendor-specific
          APIs for volumes that persist for longer than the lifecycle
          of an individual pod.

          Use CSI for light-weight local ephemeral volumes if the CSI driver is meant to
          be used that way - see the documentation of the driver for
          more information.

          A pod can use both types of ephemeral volumes and
          persistent volumes at the same time.

          +optional
      fc:
        allOf:
        - $ref: '#/definitions/v1.FCVolumeSource'
        description: |-
          fc represents a Fibre Channel resource that is attached to a kubelet's host machine and then exposed to the pod.
          +optional
      flexVolume:
        allOf:
        - $ref: '#/definitions/v1.FlexVolumeSource'
        description: |-
          flexVolume represents a generic volume resource that is
          provisioned/attached using an exec based plugin.
          Deprecated: FlexVolume is deprecated. Consider using a CSIDriver instead.
          +optional
      flocker:
        allOf:
        - $ref: '#/definitions/v1.FlockerVolumeSource'
        description: |-
          flocker represents a Flocker volume attached to a kubelet's host machine. This depends on the Flocker control service being running.
          Deprecated: Flocker is deprecated and the in-tree flocker type is no longer supported.
          +optional
      gcePersistentDisk:
        allOf:
        - $ref: '#/definitions/v1.GCEPersistentDiskVolumeSource'
        description: |-
          gcePersistentDisk represents a GCE Disk resource that is attached to a
          kubelet's host machine and then exposed to the pod.
          Deprecated: GCEPersistentDisk is deprecated. All operations for the in-tree
          gcePersistentDisk type are redirected to the pd.csi.storage.gke.io CSI driver.
          More info: https://kubernetes.io/docs/concepts/storage/volumes#gcepersistentdisk
          +optional
      gitRepo:
        allOf:
        - $ref: '#/definitions/v1.GitRepoVolumeSource'
        description: |-
          gitRepo represents a git repository at a particular revision.
          Deprecated: GitRepo is deprecated. To provision a container with a git repo, mount an
          EmptyDir into an InitContainer that clones the repo using git, then mount the EmptyDir
          into the Pod's container.
          +optional
      glusterfs:
        allOf:
        - $ref: '#/definitions/v1.GlusterfsVolumeSource'
        description: |-
          glusterfs represents a Glusterfs mount on the host that shares a pod's lifetime.
          Deprecated: Glusterfs is deprecated and the in-tree glusterfs type is no longer supported.
          +optional
      hostPath:
        allOf:
        - $ref: '#/definitions/v1.HostPathVolumeSource'
        description: |-
          hostPath represents a pre-existing file or directory on the host
          machine that is directly exposed to the container. This is generally
          used for system agents or other privileged things that are allowed
          to see the host machine. Most containers will NOT need this.
          More info: https://kubernetes.io/docs/concepts/storage/volumes#hostpath
          ---
          TODO(jonesdl) We need to restrict who can use host directory mounts and who can/can not
          mount host directories as read/write.
          +optional
      image:
        allOf:
        - $ref: '#/definitions/v1.ImageVolumeSource'
        description: |-
          image represents an OCI object (a container image or artifact) pulled and mounted on the kubelet's host machine.
          The volume is resolved at pod startup depending on which PullPolicy value is provided:

          - Always: the kubelet always attempts to pull the reference. Container creation will fail If the pull fails.
          - Never: the kubelet never pulls the reference and only uses a local image or artifact. Container creation will fail if the reference isn't present.
          - IfNotPresent: the kubelet pulls if the reference isn't already present on disk. Container creation will fail if the reference isn't present and the pull fails.

          The volume gets re-resolved if the pod gets deleted and recreated, which means that new remote content will become available on pod recreation.
          A failure to resolve or pull the image during pod startup will block containers from starting and may add significant latency. Failures will be retried using normal volume backoff and will be reported on the pod reason and message.
          The types of objects that may be mounted by this volume are defined by the container runtime implementation on a host machine and at minimum must include all valid types supported by the container image field.
          The OCI object gets mounted in a single directory (spec.containers[*].volumeMounts.mountPath) by merging the manifest layers in the same way as for container images.
          The volume will be mounted read-only (ro) and non-executable files (noexec).
          Sub path mounts for containers are not supported (spec.containers[*].volumeMounts.subpath) before 1.33.
          The field spec.securityContext.fsGroupChangePolicy has no effect on this volume type.
          +featureGate=ImageVolume
          +optional
      iscsi:
        allOf:
        - $ref: '#/definitions/v1.ISCSIVolumeSource'
        description: |-
          iscsi represents an ISCSI Disk resource that is attached to a
          kubelet's host machine and then exposed to the pod.
          More info: https://kubernetes.io/docs/concepts/storage/volumes/#iscsi
          +optional
      name:
        description: |-
          name of the volume.
          Must be a DNS_LABEL and unique within the pod.
          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
        type: string
      nfs:
        allOf:
        - $ref: '#/definitions/v1.NFSVolumeSource'
        description: |-
          nfs represents an NFS mount on the host that shares a pod's lifetime
          More info: https://kubernetes.io/docs/concepts/storage/volumes#nfs
          +optional
      persistentVolumeClaim:
        allOf:
        - $ref: '#/definitions/v1.PersistentVolumeClaimVolumeSource'
        description: |-
          persistentVolumeClaimVolumeSource represents a reference to a
          PersistentVolumeClaim in the same namespace.
          More info: https://kubernetes.io/docs/concepts/storage/persistent-volumes#persistentvolumeclaims
          +optional
      photonPersistentDisk:
        allOf:
        - $ref: '#/definitions/v1.PhotonPersistentDiskVolumeSource'
        description: |-
          photonPersistentDisk represents a PhotonController persistent disk attached and mounted on kubelets host machine.
          Deprecated: PhotonPersistentDisk is deprecated and the in-tree photonPersistentDisk type is no longer supported.
      portworxVolume:
        allOf:
        - $ref: '#/definitions/v1.PortworxVolumeSource'
        description: |-
          portworxVolume represents a portworx volume attached and mounted on kubelets host machine.
          Deprecated: PortworxVolume is deprecated. All operations for the in-tree portworxVolume type
          are redirected to the pxd.portworx.com CSI driver when the CSIMigrationPortworx feature-gate
          is on.
          +optional
      projected:
        allOf:
        - $ref: '#/definitions/v1.ProjectedVolumeSource'
        description: projected items for all in one resources secrets, configmaps,
          and downward API
      quobyte:
        allOf:
        - $ref: '#/definitions/v1.QuobyteVolumeSource'
        description: |-
          quobyte represents a Quobyte mount on the host that shares a pod's lifetime.
          Deprecated: Quobyte is deprecated and the in-tree quobyte type is no longer supported.
          +optional
      rbd:
        allOf:
        - $ref: '#/definitions/v1.RBDVolumeSource'
        description: |-
          rbd represents a Rados Block Device mount on the host that shares a pod's lifetime.
          Deprecated: RBD is deprecated and the in-tree rbd type is no longer supported.
          +optional
      scaleIO:
        allOf:
        - $ref: '#/definitions/v1.ScaleIOVolumeSource'
        description: |-
          scaleIO represents a ScaleIO persistent volume attached and mounted on Kubernetes nodes.
          Deprecated: ScaleIO is deprecated and the in-tree scaleIO type is no longer supported.
          +optional
      secret:
        allOf:
        - $ref: '#/definitions/v1.SecretVolumeSource'
        description: |-
          secret represents a secret that should populate this volume.
          More info: https://kubernetes.io/docs/concepts/storage/volumes#secret
          +optional
      storageos:
        allOf:
        - $ref: '#/definitions/v1.StorageOSVolumeSource'
        description: |-
          storageOS represents a StorageOS volume attached and mounted on Kubernetes nodes.
          Deprecated: StorageOS is deprecated and the in-tree storageos type is no longer supported.
          +optional
      vsphereVolume:
        allOf:
        - $ref: '#/definitions/v1.VsphereVirtualDiskVolumeSource'
        description: |-
          vsphereVolume represents a vSphere volume attached and mounted on kubelets host machine.
          Deprecated: VsphereVolume is deprecated. All operations for the in-tree vsphereVolume type
          are redirected to the csi.vsphere.vmware.com CSI driver.
          +optional
    type: object
  v1.VolumeDevice:
    properties:
      devicePath:
        description: devicePath is the path inside of the container that the device
          will be mapped to.
        type: string
      name:
        description: name must match the name of a persistentVolumeClaim in the pod
        type: string
    type: object
  v1.VolumeMount:
    properties:
      mountPath:
        description: |-
          Path within the container at which the volume should be mounted.  Must
          not contain ':'.
        type: string
      mountPropagation:
        allOf:
        - $ref: '#/definitions/v1.MountPropagationMode'
        description: |-
          mountPropagation determines how mounts are propagated from the host
          to container and the other way around.
          When not set, MountPropagationNone is used.
          This field is beta in 1.10.
          When RecursiveReadOnly is set to IfPossible or to Enabled, MountPropagation must be None or unspecified
          (which defaults to None).
          +optional
      name:
        description: This must match the Name of a Volume.
        type: string
      readOnly:
        description: |-
          Mounted read-only if true, read-write otherwise (false or unspecified).
          Defaults to false.
          +optional
        type: boolean
      recursiveReadOnly:
        allOf:
        - $ref: '#/definitions/v1.RecursiveReadOnlyMode'
        description: |-
          RecursiveReadOnly specifies whether read-only mounts should be handled
          recursively.

          If ReadOnly is false, this field has no meaning and must be unspecified.

          If ReadOnly is true, and this field is set to Disabled, the mount is not made
          recursively read-only.  If this field is set to IfPossible, the mount is made
          recursively read-only, if it is supported by the container runtime.  If this
          field is set to Enabled, the mount is made recursively read-only if it is
          supported by the container runtime, otherwise the pod will not be started and
          an error will be generated to indicate the reason.

          If this field is set to IfPossible or Enabled, MountPropagation must be set to
          None (or be unspecified, which defaults to None).

          If this field is not specified, it is treated as an equivalent of Disabled.

          +featureGate=RecursiveReadOnlyMounts
          +optional
      subPath:
        description: |-
          Path within the volume from which the container's volume should be mounted.
          Defaults to "" (volume's root).
          +optional
        type: string
      subPathExpr:
        description: |-
          Expanded path within the volume from which the container's volume should be mounted.
          Behaves similarly to SubPath but environment variable references $(VAR_NAME) are expanded using the container's environment.
          Defaults to "" (volume's root).
          SubPathExpr and SubPath are mutually exclusive.
          +optional
        type: string
    type: object
  v1.VolumeProjection:
    properties:
      clusterTrustBundle:
        allOf:
        - $ref: '#/definitions/v1.ClusterTrustBundleProjection'
        description: |-
          ClusterTrustBundle allows a pod to access the `.spec.trustBundle` field
          of ClusterTrustBundle objects in an auto-updating file.

          Alpha, gated by the ClusterTrustBundleProjection feature gate.

          ClusterTrustBundle objects can either be selected by name, or by the
          combination of signer name and a label selector.

          Kubelet performs aggressive normalization of the PEM contents written
          into the pod filesystem.  Esoteric PEM features such as inter-block
          comments and block headers are stripped.  Certificates are deduplicated.
          The ordering of certificates within the file is arbitrary, and Kubelet
          may change the order over time.

          +featureGate=ClusterTrustBundleProjection
          +optional
      configMap:
        allOf:
        - $ref: '#/definitions/v1.ConfigMapProjection'
        description: |-
          configMap information about the configMap data to project
          +optional
      downwardAPI:
        allOf:
        - $ref: '#/definitions/v1.DownwardAPIProjection'
        description: |-
          downwardAPI information about the downwardAPI data to project
          +optional
      podCertificate:
        allOf:
        - $ref: '#/definitions/v1.PodCertificateProjection'
        description: |-
          Projects an auto-rotating credential bundle (private key and certificate
          chain) that the pod can use either as a TLS client or server.

          Kubelet generates a private key and uses it to send a
          PodCertificateRequest to the named signer.  Once the signer approves the
          request and issues a certificate chain, Kubelet writes the key and
          certificate chain to the pod filesystem.  The pod does not start until
          certificates have been issued for each podCertificate projected volume
          source in its spec.

          Kubelet will begin trying to rotate the certificate at the time indicated
          by the signer using the PodCertificateRequest.Status.BeginRefreshAt
          timestamp.

          Kubelet can write a single file, indicated by the credentialBundlePath
          field, or separate files, indicated by the keyPath and
          certificateChainPath fields.

          The credential bundle is a single file in PEM format.  The first PEM
          entry is the private key (in PKCS#8 format), and the remaining PEM
          entries are the certificate chain issued by the signer (typically,
          signers will return their certificate chain in leaf-to-root order).

          Prefer using the credential bundle format, since your application code
          can read it atomically.  If you use keyPath and certificateChainPath,
          your application must make two separate file reads. If these coincide
          with a certificate rotation, it is possible that the private key and leaf
          certificate you read may not correspond to each other.  Your application
          will need to check for this condition, and re-read until they are
          consistent.

          The named signer controls chooses the format of the certificate it
          issues; consult the signer implementation's documentation to learn how to
          use the certificates it issues.

          +featureGate=PodCertificateProjection +optional
      secret:
        allOf:
        - $ref: '#/definitions/v1.SecretProjection'
        description: |-
          secret information about the secret data to project
          +optional
      serviceAccountToken:
        allOf:
        - $ref: '#/definitions/v1.ServiceAccountTokenProjection'
        description: |-
          serviceAccountToken is information about the serviceAccountToken data to project
          +optional
    type: object
  v1.VolumeResourceRequirements:
    properties:
      limits:
        allOf:
        - $ref: '#/definitions/v1.ResourceList'
        description: |-
          Limits describes the maximum amount of compute resources allowed.
          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
          +optional
      requests:
        allOf:
        - $ref: '#/definitions/v1.ResourceList'
        description: |-
          Requests describes the minimum amount of compute resources required.
          If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
          otherwise to an implementation-defined value. Requests cannot exceed Limits.
          More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
          +optional
    type: object
  v1.VsphereVirtualDiskVolumeSource:
    properties:
      fsType:
        description: |-
          fsType is filesystem type to mount.
          Must be a filesystem type supported by the host operating system.
          Ex. "ext4", "xfs", "ntfs". Implicitly inferred to be "ext4" if unspecified.
          +optional
        type: string
      storagePolicyID:
        description: |-
          storagePolicyID is the storage Policy Based Management (SPBM) profile ID associated with the StoragePolicyName.
          +optional
        type: string
      storagePolicyName:
        description: |-
          storagePolicyName is the storage Policy Based Management (SPBM) profile name.
          +optional
        type: string
      volumePath:
        description: volumePath is the path that identifies vSphere volume vmdk
        type: string
    type: object
  v1.WeightedPodAffinityTerm:
    properties:
      podAffinityTerm:
        allOf:
        - $ref: '#/definitions/v1.PodAffinityTerm'
        description: Required. A pod affinity term, associated with the corresponding
          weight.
      weight:
        description: |-
          weight associated with matching the corresponding podAffinityTerm,
          in the range 1-100.
        type: integer
    type: object
  v1.WindowsSecurityContextOptions:
    properties:
      gmsaCredentialSpec:
        description: |-
          GMSACredentialSpec is where the GMSA admission webhook
          (https://github.com/kubernetes-sigs/windows-gmsa) inlines the contents of the
          GMSA credential spec named by the GMSACredentialSpecName field.
          +optional
        type: string
      gmsaCredentialSpecName:
        description: |-
          GMSACredentialSpecName is the name of the GMSA credential spec to use.
          +optional
        type: string
      hostProcess:
        description: |-
          HostProcess determines if a container should be run as a 'Host Process' container.
          All of a Pod's containers must have the same effective HostProcess value
          (it is not allowed to have a mix of HostProcess containers and non-HostProcess containers).
          In addition, if HostProcess is true then HostNetwork must also be set to true.
          +optional
        type: boolean
      runAsUserName:
        description: |-
          The UserName in Windows to run the entrypoint of the container process.
          Defaults to the user specified in image metadata if unspecified.
          May also be set in PodSecurityContext. If set in both SecurityContext and
          PodSecurityContext, the value specified in SecurityContext takes precedence.
          +optional
        type: string
    type: object
info:
  contact: {}
  description: 基于Gin+Vue开发的K8s管理平台
  termsOfService: https://gitee.com/jay-kim/k8s_operation
  title: K8s管理平台
  version: "1.0"
paths:
  /api/:
    get:
      produces:
      - application/json
      responses:
        "200":
          description: OK
      summary: HelloWorld
      tags:
      - HelloWorld
  /api/v1/ai/approvals:
    get:
      description: 管理员获取所有审批，普通用户仅看自己的申请
      parameters:
      - default: 0
        description: '状态筛选: 1=待审批 2=已通过 3=已拒绝 4=已过期 5=已取消'
        in: query
        name: status
        type: integer
      - default: 1
        description: 页码
        in: query
        name: page
        type: integer
      - default: 20
        description: 每页数量
        in: query
        name: page_size
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
      summary: 获取审批列表
      tags:
      - AI 审批管理
  /api/v1/ai/approvals/{id}:
    delete:
      description: 管理员可删除任何记录，普通用户只能删除自己的非已通过记录
      parameters:
      - description: 审批ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
      summary: 删除审批记录
      tags:
      - AI 审批管理
    get:
      parameters:
      - description: 审批ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
      summary: 获取审批详情
      tags:
      - AI 审批管理
    put:
      consumes:
      - application/json
      description: 仅待审批状态的记录可编辑备注
      parameters:
      - description: 审批ID
        in: path
        name: id
        required: true
        type: integer
      - description: 更新内容
        in: body
        name: body
        required: true
        schema:
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
      summary: 更新审批备注
      tags:
      - AI 审批管理
  /api/v1/ai/approvals/{id}/approve:
    post:
      consumes:
      - application/json
      description: 管理员通过高危操作审批（不可自审）
      parameters:
      - description: 审批ID
        in: path
        name: id
        required: true
        type: integer
      - description: 审批备注
        in: body
        name: body
        required: true
        schema:
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
      summary: 通过审批
      tags:
      - AI 审批管理
  /api/v1/ai/approvals/{id}/cancel:
    post:
      parameters:
      - description: 审批ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
      summary: 取消审批
      tags:
      - AI 审批管理
  /api/v1/ai/approvals/{id}/reject:
    post:
      consumes:
      - application/json
      description: 管理员拒绝高危操作审批（不可自审）
      parameters:
      - description: 审批ID
        in: path
        name: id
        required: true
        type: integer
      - description: 拒绝原因
        in: body
        name: body
        required: true
        schema:
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
      summary: 拒绝审批
      tags:
      - AI 审批管理
  /api/v1/ai/approvals/mine:
    get:
      parameters:
      - default: 1
        description: 页码
        in: query
        name: page
        type: integer
      - default: 20
        description: 每页数量
        in: query
        name: page_size
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
      summary: 获取我的审批申请
      tags:
      - AI 审批管理
  /api/v1/ai/approvals/pending-count:
    get:
      description: 管理员看全局待审批数，普通用户看自己的
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
      summary: 获取待审批数量
      tags:
      - AI 审批管理
  /api/v1/ai/approvals/stats:
    get:
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
      summary: 获取审批统计数据
      tags:
      - AI 审批管理
  /api/v1/ai/chat:
    post:
      consumes:
      - application/json
      description: 向 AI 助手发送消息并获取回复，高危操作会自动触发审批流程
      parameters:
      - description: 聊天请求
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_services.AIChatRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_internal_app_services.AIChatResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: AI 对话
      tags:
      - AI 助手
  /api/v1/ai/chat/stream:
    post:
      consumes:
      - application/json
      description: 向 AI 助手发送消息，通过 SSE 流式接收回复
      parameters:
      - description: 聊天请求
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_services.AIChatRequest'
      produces:
      - text/event-stream
      responses: {}
      summary: AI 流式对话
      tags:
      - AI 助手
  /api/v1/ai/conversations:
    get:
      parameters:
      - default: 1
        description: 页码
        in: query
        name: page
        type: integer
      - default: 20
        description: 每页数量
        in: query
        name: page_size
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
      summary: 获取 AI 会话列表
      tags:
      - AI 助手
  /api/v1/ai/conversations/{id}:
    delete:
      parameters:
      - description: 会话ID
        in: path
        name: id
        required: true
        type: integer
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
      summary: 删除 AI 会话
      tags:
      - AI 助手
  /api/v1/ai/conversations/{id}/messages:
    get:
      parameters:
      - description: 会话ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
      summary: 获取会话消息列表
      tags:
      - AI 助手
  /api/v1/ai/intent:
    post:
      consumes:
      - application/json
      description: 分析用户输入的操作意图，返回风险等级和是否需要审批
      parameters:
      - description: 请求体
        in: body
        name: body
        required: true
        schema:
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
      summary: AI 意图分析
      tags:
      - AI 助手
  /api/v1/ai/logs:
    get:
      description: 获取 AI 助手的最近日志记录，用于排查大模型问题
      parameters:
      - default: 100
        description: 返回行数
        in: query
        name: lines
        type: integer
      - default: '""'
        description: 级别过滤(error/warn/info)
        in: query
        name: level
        type: string
      - description: 关键字搜索
        in: query
        name: keyword
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
      summary: AI 日志查询
      tags:
      - AI 助手
  /api/v1/ai/quick-ask:
    post:
      consumes:
      - application/json
      description: 无需会话上下文的快捷提问，适用于全局 AI 入口
      parameters:
      - description: 请求体
        in: body
        name: body
        required: true
        schema:
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
      summary: AI 快捷问答
      tags:
      - AI 助手
  /api/v1/auth/forgot_password:
    post:
      description: 根据用户名重置密码
      parameters:
      - description: body
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.AuthForgotPasswordRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 忘记密码
      tags:
      - 认证管理
  /api/v1/auth/login:
    post:
      description: 用户登录（支持本地/LDAP 双模式）
      parameters:
      - description: body
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.AuthLoginRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 用户登录
      tags:
      - 认证管理
  /api/v1/auth/logout:
    post:
      description: 清空会话并让会话 Cookie 立即过期（注意：不会自动使现有 JWT 失效）
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties:
              type: string
            type: object
      summary: 退出登录
      tags:
      - 认证管理
  /api/v1/auth/refresh:
    post:
      description: 使用旧的 Token 刷新并返回新的 Token
      parameters:
      - description: Bearer token
        in: header
        name: Authorization
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "401":
          description: Token 无效或已超过最大刷新时间
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 刷新访问令牌
      tags:
      - 认证管理
  /api/v1/auth/register:
    post:
      consumes:
      - application/json
      description: 用户注册接口：校验参数后创建用户
      parameters:
      - description: 注册参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.AuthRegisterRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 注册成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "409":
          description: 用户已存在/冲突
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 用户注册
      tags:
      - 认证管理
  /api/v1/cicd/release/create:
    post:
      consumes:
      - application/json
      parameters:
      - description: 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.CicdReleaseCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 创建 CICD 发布单
      tags:
      - CICD Release
  /api/v1/image/browse/delete:
    post:
      description: 删除指定镜像的标签
      parameters:
      - description: 仓库ID
        in: query
        name: registry_id
        required: true
        type: integer
      - description: 镜像名称
        in: query
        name: repository
        required: true
        type: string
      - description: 标签
        in: query
        name: tag
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 删除镜像标签
      tags:
      - 镜像浏览
  /api/v1/image/browse/detail:
    get:
      description: 获取指定镜像标签的详细信息
      parameters:
      - description: 仓库ID
        in: query
        name: registry_id
        required: true
        type: integer
      - description: 镜像名称
        in: query
        name: repository
        required: true
        type: string
      - description: 标签
        in: query
        name: tag
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 获取镜像详情
      tags:
      - 镜像浏览
  /api/v1/image/browse/repositories:
    get:
      description: 获取指定镜像仓库中的所有镜像项目/命名空间
      parameters:
      - description: 仓库ID
        in: query
        name: registry_id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 列出仓库中的镜像项目
      tags:
      - 镜像浏览
  /api/v1/image/browse/tags:
    get:
      description: 获取指定镜像的所有标签/版本
      parameters:
      - description: 仓库ID
        in: query
        name: registry_id
        required: true
        type: integer
      - description: 镜像名称
        in: query
        name: repository
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 列出镜像的所有标签
      tags:
      - 镜像浏览
  /api/v1/image/cleanup/create:
    post:
      consumes:
      - application/json
      description: 创建新的镜像清理策略
      parameters:
      - description: 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_services.CleanupPolicyRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 创建清理策略
      tags:
      - 镜像清理
  /api/v1/image/cleanup/delete:
    post:
      description: 删除镜像清理策略
      parameters:
      - description: 策略ID
        in: query
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 删除清理策略
      tags:
      - 镜像清理
  /api/v1/image/cleanup/list:
    get:
      description: 分页获取清理策略列表
      parameters:
      - description: 仓库ID（可选）
        in: query
        name: registry_id
        type: integer
      - description: 搜索关键词
        in: query
        name: keyword
        type: string
      - description: 页码
        in: query
        name: page
        required: true
        type: integer
      - description: 每页数量
        in: query
        name: page_size
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 获取清理策略列表
      tags:
      - 镜像清理
  /api/v1/image/cleanup/logs:
    get:
      description: 获取清理任务执行日志
      parameters:
      - description: 策略ID（可选）
        in: query
        name: policy_id
        type: integer
      - description: 返回数量（默认20）
        in: query
        name: limit
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 获取清理日志
      tags:
      - 镜像清理
  /api/v1/image/cleanup/run:
    post:
      description: 手动触发一次清理任务
      parameters:
      - description: 策略ID
        in: query
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 手动执行清理策略
      tags:
      - 镜像清理
  /api/v1/image/cleanup/toggle:
    post:
      description: 切换清理策略的启用状态
      parameters:
      - description: 策略ID
        in: query
        name: id
        required: true
        type: integer
      - description: 是否启用
        in: query
        name: enabled
        required: true
        type: boolean
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 启用/禁用清理策略
      tags:
      - 镜像清理
  /api/v1/image/cleanup/update:
    post:
      consumes:
      - application/json
      description: 更新镜像清理策略
      parameters:
      - description: 更新参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_services.CleanupPolicyRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 更新清理策略
      tags:
      - 镜像清理
  /api/v1/image/registry/all:
    get:
      description: 获取所有镜像仓库（不分页，用于下拉选择）
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 获取所有镜像仓库
      tags:
      - 镜像管理
  /api/v1/image/registry/check:
    post:
      description: 检测镜像仓库连接状态
      parameters:
      - description: 仓库ID
        in: query
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 检测仓库连接
      tags:
      - 镜像管理
  /api/v1/image/registry/create:
    post:
      consumes:
      - application/json
      description: 创建新的镜像仓库
      parameters:
      - description: 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_services.RegistryCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 创建镜像仓库
      tags:
      - 镜像管理
  /api/v1/image/registry/default:
    post:
      description: 设置默认镜像仓库
      parameters:
      - description: 仓库ID
        in: query
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 设置默认仓库
      tags:
      - 镜像管理
  /api/v1/image/registry/delete:
    post:
      description: 删除镜像仓库
      parameters:
      - description: 仓库ID
        in: query
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 删除镜像仓库
      tags:
      - 镜像管理
  /api/v1/image/registry/detail:
    get:
      description: 根据ID获取镜像仓库详情
      parameters:
      - description: 仓库ID
        in: query
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 获取镜像仓库详情
      tags:
      - 镜像管理
  /api/v1/image/registry/list:
    get:
      description: 分页获取镜像仓库列表
      parameters:
      - description: 搜索关键词
        in: query
        name: keyword
        type: string
      - description: 仓库类型
        in: query
        name: type
        type: string
      - description: 页码
        in: query
        name: page
        required: true
        type: integer
      - description: 每页数量
        in: query
        name: page_size
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 获取镜像仓库列表
      tags:
      - 镜像管理
  /api/v1/image/registry/stats:
    get:
      description: 获取镜像仓库统计信息
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 获取仓库统计
      tags:
      - 镜像管理
  /api/v1/image/registry/update:
    post:
      consumes:
      - application/json
      description: 更新镜像仓库信息
      parameters:
      - description: 更新参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_services.RegistryUpdateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 更新镜像仓库
      tags:
      - 镜像管理
  /api/v1/k8s/appconfig/create:
    post:
      consumes:
      - application/json
      parameters:
      - description: 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeAppConfigCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 创建 AppConfig
      tags:
      - Operator AppConfig
  /api/v1/k8s/appconfig/delete:
    delete:
      consumes:
      - application/json
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: App 名称
        in: query
        name: app_name
        required: true
        type: string
      - description: 集群 ID
        in: query
        name: cluster_id
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 删除 AppConfig
      tags:
      - Operator AppConfig
  /api/v1/k8s/appconfig/detail:
    get:
      consumes:
      - application/json
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: App 名称
        in: query
        name: app_name
        required: true
        type: string
      - description: 集群 ID
        in: query
        name: cluster_id
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 AppConfig 详情
      tags:
      - Operator AppConfig
  /api/v1/k8s/appconfig/list:
    get:
      consumes:
      - application/json
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        type: string
      - description: 集群 ID
        in: query
        name: cluster_id
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 AppConfig 列表
      tags:
      - Operator AppConfig
  /api/v1/k8s/appconfig/update:
    put:
      consumes:
      - application/json
      parameters:
      - description: 更新参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeAppConfigUpdateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 更新 AppConfig
      tags:
      - Operator AppConfig
  /api/v1/k8s/cicd/agent/by-scope:
    get:
      parameters:
      - description: 语言类型：java/go/python
        in: query
        name: scope
        required: true
        type: string
      produces:
      - application/json
      responses: {}
      summary: 获取指定语言的已启用探针
      tags:
      - CICD BuildAgent
  /api/v1/k8s/cicd/agent/delete:
    post:
      consumes:
      - application/json
      produces:
      - application/json
      responses: {}
      summary: 删除构建探针
      tags:
      - CICD BuildAgent
  /api/v1/k8s/cicd/agent/detail:
    get:
      parameters:
      - description: 探针ID
        in: query
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses: {}
      summary: 获取构建探针详情
      tags:
      - CICD BuildAgent
  /api/v1/k8s/cicd/agent/download:
    get:
      parameters:
      - description: 探针ID（二选一）
        in: query
        name: id
        type: integer
      - description: 探针名称（二选一，流水线构建使用）
        in: query
        name: name
        type: string
      produces:
      - application/octet-stream
      responses: {}
      summary: 下载构建探针文件
      tags:
      - CICD BuildAgent
  /api/v1/k8s/cicd/agent/list:
    get:
      parameters:
      - description: 分类：observability/diagnostics/security/custom
        in: query
        name: category
        type: string
      - description: 适用语言：java/go/python/all
        in: query
        name: scope
        type: string
      - description: 状态：active/inactive
        in: query
        name: status
        type: string
      - description: 关键词搜索
        in: query
        name: keyword
        type: string
      produces:
      - application/json
      responses: {}
      summary: 获取构建探针列表
      tags:
      - CICD BuildAgent
  /api/v1/k8s/cicd/agent/toggle:
    post:
      consumes:
      - application/json
      produces:
      - application/json
      responses: {}
      summary: 切换构建探针启用状态
      tags:
      - CICD BuildAgent
  /api/v1/k8s/cicd/agent/update:
    post:
      consumes:
      - application/json
      produces:
      - application/json
      responses: {}
      summary: 更新构建探针信息
      tags:
      - CICD BuildAgent
  /api/v1/k8s/cicd/agent/upload:
    post:
      consumes:
      - multipart/form-data
      parameters:
      - description: 探针文件（如 opentelemetry-javaagent.jar）
        in: formData
        name: file
        required: true
        type: file
      - description: 探针名称（唯一标识）
        in: formData
        name: name
        required: true
        type: string
      - description: 显示名称
        in: formData
        name: display_name
        required: true
        type: string
      - description: 分类
        in: formData
        name: category
        required: true
        type: string
      - description: 适用语言
        in: formData
        name: scope
        required: true
        type: string
      - description: 版本号
        in: formData
        name: version
        type: string
      produces:
      - application/json
      responses: {}
      summary: 上传构建探针文件
      tags:
      - CICD BuildAgent
  /api/v1/k8s/cicd/approval/action:
    post:
      consumes:
      - application/json
      description: 通过或拒绝审批申请
      parameters:
      - description: 操作参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.ApprovalActionRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 操作成功
          schema:
            additionalProperties: true
            type: object
      summary: 审批操作
      tags:
      - CICD Approval
  /api/v1/k8s/cicd/approval/batch-action:
    post:
      consumes:
      - application/json
      description: 批量通过或拒绝审批申请
      parameters:
      - description: 批量操作参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.ApprovalBatchActionRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 操作结果
          schema:
            additionalProperties: true
            type: object
      summary: 批量审批操作
      tags:
      - CICD Approval
  /api/v1/k8s/cicd/approval/create:
    post:
      consumes:
      - application/json
      description: 创建部署审批申请
      parameters:
      - description: 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.ApprovalCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 返回审批ID
          schema:
            additionalProperties: true
            type: object
      summary: 创建审批申请
      tags:
      - CICD Approval
  /api/v1/k8s/cicd/approval/delete:
    post:
      consumes:
      - application/json
      description: 删除审批记录（已通过的不允许删除）
      parameters:
      - description: 删除参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.ApprovalDeleteRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 删除成功
          schema:
            additionalProperties: true
            type: object
      summary: 删除审批记录
      tags:
      - CICD Approval
  /api/v1/k8s/cicd/approval/detail:
    get:
      description: 获取单个审批记录的详细信息
      parameters:
      - description: 审批ID
        in: query
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 返回审批详情
          schema:
            additionalProperties: true
            type: object
      summary: 获取审批详情
      tags:
      - CICD Approval
  /api/v1/k8s/cicd/approval/feishu-callback:
    get:
      consumes:
      - application/json
      description: 飞书交互卡片审批回调接口（公开接口，无需JWT）
      parameters:
      - description: 审批Token
        in: query
        name: token
        required: true
        type: string
      - description: '审批动作: approve/reject'
        in: query
        name: action
        required: true
        type: string
      - description: 审批意见
        in: query
        name: reason
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 审批处理成功
          schema:
            additionalProperties: true
            type: object
      summary: 飞书审批回调
      tags:
      - CICD Approval
  /api/v1/k8s/cicd/approval/list:
    get:
      description: 分页查询审批记录
      parameters:
      - description: 页码，默认1
        in: query
        name: page
        type: integer
      - description: 每页数量，默认10
        in: query
        name: page_size
        type: integer
      - description: 流水线ID筛选
        in: query
        name: pipeline_id
        type: integer
      - description: 状态筛选（pending/approved/rejected/expired）
        in: query
        name: status
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 返回审批列表
          schema:
            additionalProperties: true
            type: object
      summary: 获取审批列表
      tags:
      - CICD Approval
  /api/v1/k8s/cicd/approval/pending:
    get:
      description: 获取当前用户待审批的申请列表
      parameters:
      - description: 页码，默认1
        in: query
        name: page
        type: integer
      - description: 每页数量，默认10
        in: query
        name: page_size
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 返回待审批列表
          schema:
            additionalProperties: true
            type: object
      summary: 获取待审批列表
      tags:
      - CICD Approval
  /api/v1/k8s/cicd/approval/stats:
    get:
      description: 获取各状态审批数量统计
      produces:
      - application/json
      responses:
        "200":
          description: 返回统计数据
          schema:
            additionalProperties: true
            type: object
      summary: 获取审批统计
      tags:
      - CICD Approval
  /api/v1/k8s/cicd/approval/update:
    post:
      consumes:
      - application/json
      description: 更新待审批记录的字段信息
      parameters:
      - description: 更新参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.ApprovalUpdateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 更新成功
          schema:
            additionalProperties: true
            type: object
      summary: 更新审批记录
      tags:
      - CICD Approval
  /api/v1/k8s/cicd/artifact/attach:
    post:
      consumes:
      - multipart/form-data
      parameters:
      - description: 制品ID
        in: formData
        name: id
        required: true
        type: integer
      - description: 制品文件
        in: formData
        name: file
        required: true
        type: file
      produces:
      - application/json
      responses: {}
      summary: 为已有制品补传文件
      tags:
      - CICD Artifact
  /api/v1/k8s/cicd/artifact/batch-delete:
    post:
      consumes:
      - application/json
      produces:
      - application/json
      responses: {}
      summary: 批量删除制品
      tags:
      - CICD Artifact
  /api/v1/k8s/cicd/artifact/by-run:
    get:
      parameters:
      - description: 运行记录ID
        in: query
        name: run_id
        required: true
        type: integer
      produces:
      - application/json
      responses: {}
      summary: 获取流水线某次运行产出的制品
      tags:
      - CICD Artifact
  /api/v1/k8s/cicd/artifact/create:
    post:
      consumes:
      - application/json
      produces:
      - application/json
      responses: {}
      summary: 创建制品记录
      tags:
      - CICD Artifact
  /api/v1/k8s/cicd/artifact/delete:
    post:
      consumes:
      - application/json
      produces:
      - application/json
      responses: {}
      summary: 删除制品
      tags:
      - CICD Artifact
  /api/v1/k8s/cicd/artifact/detail:
    get:
      parameters:
      - description: 制品ID
        in: query
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses: {}
      summary: 获取制品详情
      tags:
      - CICD Artifact
  /api/v1/k8s/cicd/artifact/download:
    get:
      parameters:
      - description: 制品ID
        in: query
        name: id
        required: true
        type: integer
      - description: 认证Token（用于 window.open 下载）
        in: query
        name: token
        type: string
      produces:
      - application/octet-stream
      responses: {}
      summary: 下载制品文件
      tags:
      - CICD Artifact
  /api/v1/k8s/cicd/artifact/list:
    get:
      parameters:
      - description: 页码
        in: query
        name: page
        type: integer
      - description: 每页数量
        in: query
        name: page_size
        type: integer
      - description: 流水线ID
        in: query
        name: pipeline_id
        type: integer
      - description: 制品类型：jar/binary/dist/wheel/image
        in: query
        name: artifact_type
        type: string
      - description: 语言类型：go/java/frontend/python
        in: query
        name: language_type
        type: string
      - description: 状态：ready/expired
        in: query
        name: status
        type: string
      produces:
      - application/json
      responses: {}
      summary: 获取制品列表
      tags:
      - CICD Artifact
  /api/v1/k8s/cicd/artifact/stats:
    get:
      parameters:
      - description: 流水线ID（不传则统计全部）
        in: query
        name: pipeline_id
        type: integer
      produces:
      - application/json
      responses: {}
      summary: 获取制品统计数据
      tags:
      - CICD Artifact
  /api/v1/k8s/cicd/artifact/update:
    post:
      consumes:
      - application/json
      produces:
      - application/json
      responses: {}
      summary: 更新制品信息
      tags:
      - CICD Artifact
  /api/v1/k8s/cicd/artifact/upload:
    post:
      consumes:
      - multipart/form-data
      parameters:
      - description: 制品文件
        in: formData
        name: file
        required: true
        type: file
      - description: 流水线ID
        in: formData
        name: pipeline_id
        type: integer
      - description: 运行记录ID
        in: formData
        name: run_id
        type: integer
      - description: 版本号
        in: formData
        name: version
        type: string
      - description: 语言类型
        in: formData
        name: language_type
        type: string
      - description: 制品类型
        in: formData
        name: artifact_type
        type: string
      produces:
      - application/json
      responses: {}
      summary: 上传制品文件
      tags:
      - CICD Artifact
  /api/v1/k8s/cicd/callback/build:
    post:
      consumes:
      - application/json
      parameters:
      - description: 回调参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.CicdBuildCallbackRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: Jenkins 构建回调
      tags:
      - CICD Callback
  /api/v1/k8s/cicd/environment/create:
    post:
      consumes:
      - application/json
      description: 创建新的部署环境配置
      parameters:
      - description: 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.EnvironmentCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 返回环境ID
          schema:
            additionalProperties: true
            type: object
      summary: 创建环境
      tags:
      - CICD Environment
  /api/v1/k8s/cicd/environment/delete:
    post:
      consumes:
      - application/json
      description: 删除部署环境配置
      parameters:
      - description: 删除参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.EnvironmentIDRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 删除成功
          schema:
            additionalProperties: true
            type: object
      summary: 删除环境
      tags:
      - CICD Environment
  /api/v1/k8s/cicd/environment/detail:
    get:
      description: 获取单个部署环境的详细信息
      parameters:
      - description: 环境ID
        in: query
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 返回环境详情
          schema:
            additionalProperties: true
            type: object
      summary: 获取环境详情
      tags:
      - CICD Environment
  /api/v1/k8s/cicd/environment/list:
    get:
      description: 分页查询部署环境列表
      parameters:
      - description: 页码，默认1
        in: query
        name: page
        type: integer
      - description: 每页数量，默认20
        in: query
        name: page_size
        type: integer
      - description: 关键字搜索
        in: query
        name: keyword
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 返回环境列表
          schema:
            additionalProperties: true
            type: object
      summary: 获取环境列表
      tags:
      - CICD Environment
  /api/v1/k8s/cicd/environment/update:
    post:
      consumes:
      - application/json
      description: 更新部署环境配置
      parameters:
      - description: 更新参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.EnvironmentUpdateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 更新成功
          schema:
            additionalProperties: true
            type: object
      summary: 更新环境
      tags:
      - CICD Environment
  /api/v1/k8s/cicd/git/branches:
    post:
      consumes:
      - application/json
      description: 获取指定 Git 仓库的所有远程分支
      parameters:
      - description: 仓库信息
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.GitBranchesRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 返回分支列表
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 Git 仓库分支列表
      tags:
      - CICD Git
  /api/v1/k8s/cicd/git/validate:
    post:
      consumes:
      - application/json
      description: 测试 Git 仓库是否可访问
      parameters:
      - description: 仓库信息
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.GitValidateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 验证结果
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 验证 Git 仓库连接
      tags:
      - CICD Git
  /api/v1/k8s/cicd/pipeline/batch-create:
    post:
      consumes:
      - application/json
      description: 一次性导入多个项目的流水线配置，支持跳过已存在项目
      parameters:
      - description: 批量创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.PipelineBatchCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 返回批量创建结果
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 批量创建流水线
      tags:
      - CICD Pipeline
  /api/v1/k8s/cicd/pipeline/batch-run:
    post:
      consumes:
      - application/json
      description: 批量触发多个流水线的 Jenkins 构建
      parameters:
      - description: 批量运行参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.PipelineBatchRunRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 返回批量运行结果
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 批量运行流水线
      tags:
      - CICD Pipeline
  /api/v1/k8s/cicd/pipeline/batch-stop:
    post:
      consumes:
      - application/json
      description: 批量停止多个正在运行的流水线
      parameters:
      - description: 批量停止参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.PipelineBatchStopRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 返回批量停止结果
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 批量停止流水线
      tags:
      - CICD Pipeline
  /api/v1/k8s/cicd/pipeline/build-records:
    get:
      description: 分页查询所有流水线的构建运行记录，支持按状态、流水线ID、关键字筛选
      parameters:
      - description: 页码，默认1
        in: query
        name: page
        type: integer
      - description: 每页数量，默认20
        in: query
        name: page_size
        type: integer
      - description: 状态筛选(pending/running/success/failed/aborted)
        in: query
        name: status
        type: string
      - description: 关键字搜索
        in: query
        name: keyword
        type: string
      - description: 流水线ID筛选
        in: query
        name: pipeline_id
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 返回构建记录列表
          schema:
            additionalProperties: true
            type: object
      summary: 获取全量构建记录（跨流水线）
      tags:
      - CICD Pipeline
  /api/v1/k8s/cicd/pipeline/build-records/export:
    get:
      description: 导出所有构建记录为 CSV 文件
      parameters:
      - description: 状态筛选
        in: query
        name: status
        type: string
      - description: 关键字
        in: query
        name: keyword
        type: string
      - description: 流水线ID
        in: query
        name: pipeline_id
        type: integer
      produces:
      - text/csv
      responses:
        "200":
          description: CSV文件
          schema:
            type: string
      summary: 导出构建记录为 CSV
      tags:
      - CICD Pipeline
  /api/v1/k8s/cicd/pipeline/build-stats:
    get:
      description: 返回构建成功率、平均时长、今日/本周构建数、近N天趋势
      parameters:
      - description: 趋势天数，默认7
        in: query
        name: days
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 获取构建统计数据
      tags:
      - CICD Pipeline
  /api/v1/k8s/cicd/pipeline/callback:
    post:
      consumes:
      - application/json
      description: Jenkins 构建完成后调用此接口通知平台更新状态
      parameters:
      - description: HMAC-SHA256 签名（用于验证请求真实性）
        in: header
        name: X-Signature
        type: string
      - description: 回调参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.PipelineCallbackRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 回调处理成功
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "401":
          description: 签名验证失败
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: Jenkins 构建状态回调（生产级）
      tags:
      - CICD Pipeline
  /api/v1/k8s/cicd/pipeline/check-name:
    get:
      description: 检查指定应用名称是否已被占用
      parameters:
      - description: 应用名称
        in: query
        name: name
        required: true
        type: string
      - description: 排除的流水线 ID（编辑时使用）
        in: query
        name: exclude_id
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 返回 available
          schema:
            additionalProperties: true
            type: object
      summary: 检查应用名称是否可用
      tags:
      - CICD Pipeline
  /api/v1/k8s/cicd/pipeline/create:
    post:
      consumes:
      - application/json
      description: 创建新的 CI/CD 流水线配置，关联 Git 仓库和 Jenkins Job
      parameters:
      - description: 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.PipelineCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 返回 pipeline_id
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 创建流水线
      tags:
      - CICD Pipeline
  /api/v1/k8s/cicd/pipeline/delete:
    post:
      consumes:
      - application/json
      description: 软删除流水线（运行中的流水线无法删除）
      parameters:
      - description: 删除参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.PipelineIDRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 删除成功
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 删除流水线
      tags:
      - CICD Pipeline
  /api/v1/k8s/cicd/pipeline/deploy-silence-status:
    get:
      description: 查看当前流水线是否有活跃的发布静默规则
      parameters:
      - description: 流水线ID
        in: query
        name: pipeline_id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 返回静默状态
          schema:
            additionalProperties: true
            type: object
      summary: 获取流水线发布静默状态
      tags:
      - CICD Pipeline
  /api/v1/k8s/cicd/pipeline/detail:
    get:
      description: 获取流水线的完整配置信息
      parameters:
      - description: 流水线ID
        in: query
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 返回流水线详情
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取流水线详情
      tags:
      - CICD Pipeline
  /api/v1/k8s/cicd/pipeline/discover:
    get:
      description: 根据集群ID、命名空间和Deployment名称，自动解析镜像仓库、容器名等信息，用于流水线创建时的"从K8s导入"功能
      parameters:
      - description: 集群ID
        in: query
        name: cluster_id
        required: true
        type: integer
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Deployment名称
        in: query
        name: deployment
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 返回自动发现的应用信息
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 从K8s Deployment自动发现应用信息
      tags:
      - CICD Pipeline
  /api/v1/k8s/cicd/pipeline/discover-apps:
    get:
      responses: {}
  /api/v1/k8s/cicd/pipeline/history:
    get:
      description: 分页获取流水线的历史运行记录
      parameters:
      - description: 流水线ID
        in: query
        name: id
        required: true
        type: integer
      - description: 页码，默认1
        in: query
        name: page
        type: integer
      - description: 每页数量，默认10
        in: query
        name: page_size
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 返回历史记录列表
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取流水线运行历史
      tags:
      - CICD Pipeline
  /api/v1/k8s/cicd/pipeline/jenkins-config:
    get:
      description: 返回当前平台的 Jenkins 连接配置（脱敏），用于前端展示和诊断
      produces:
      - application/json
      responses:
        "200":
          description: 返回 Jenkins 配置信息
          schema:
            additionalProperties: true
            type: object
      summary: 获取 Jenkins 配置信息
      tags:
      - CICD Pipeline
  /api/v1/k8s/cicd/pipeline/list:
    get:
      description: 分页查询流水线列表，支持关键字和状态筛选
      parameters:
      - description: 页码，默认1
        in: query
        name: page
        type: integer
      - description: 每页数量，默认10
        in: query
        name: page_size
        type: integer
      - description: 关键字（名称/描述/Git仓库）
        in: query
        name: keyword
        type: string
      - description: 状态筛选（idle/running/disabled）
        in: query
        name: status
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 返回列表和总数
          schema:
            additionalProperties: true
            type: object
      summary: 获取流水线列表
      tags:
      - CICD Pipeline
  /api/v1/k8s/cicd/pipeline/logs:
    get:
      description: 获取 Jenkins 构建的控制台日志，支持增量获取
      parameters:
      - description: 流水线ID
        in: query
        name: id
        required: true
        type: integer
      - description: 构建号（不传则获取最新的）
        in: query
        name: build_number
        type: integer
      - description: 起始行号（用于增量获取）
        in: query
        name: start_line
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 返回日志内容
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取流水线日志
      tags:
      - CICD Pipeline
  /api/v1/k8s/cicd/pipeline/run:
    post:
      consumes:
      - application/json
      description: 触发 Jenkins 构建，开始执行流水线
      parameters:
      - description: 运行参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.PipelineRunRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 返回运行记录ID
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 运行流水线
      tags:
      - CICD Pipeline
  /api/v1/k8s/cicd/pipeline/sonar-callback:
    post:
      consumes:
      - application/json
      description: 接收 Jenkins 回传的 SonarQube 扫描结果，存储代码质量报告
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            additionalProperties: true
            type: object
      summary: SonarQube 扫描结果回调
      tags:
      - CICD Pipeline
  /api/v1/k8s/cicd/pipeline/sonar-report:
    get:
      description: 返回指定流水线的代码质量扫描结果，包括 Bug、漏洞、覆盖率等指标
      parameters:
      - description: 流水线ID
        in: query
        name: pipeline_id
        required: true
        type: integer
      - description: 运行记录ID（空则获取最新一次）
        in: query
        name: run_id
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 返回代码质量报告
          schema:
            additionalProperties: true
            type: object
      summary: 获取流水线 SonarQube 代码质量报告
      tags:
      - CICD Pipeline
  /api/v1/k8s/cicd/pipeline/stages:
    get:
      description: 获取 Jenkins Pipeline 的阶段执行数据（来自 Jenkins Workflow API）
      parameters:
      - description: 流水线ID
        in: query
        name: id
        required: true
        type: integer
      - description: 构建号（不传则获取最新的）
        in: query
        name: build_number
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 返回阶段数据
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取流水线阶段数据
      tags:
      - CICD Pipeline
  /api/v1/k8s/cicd/pipeline/status:
    get:
      description: 获取流水线和当前构建的实时状态
      parameters:
      - description: 流水线ID
        in: query
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 返回状态信息
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取流水线实时状态
      tags:
      - CICD Pipeline
  /api/v1/k8s/cicd/pipeline/stop:
    post:
      consumes:
      - application/json
      description: 停止正在运行的 Jenkins 构建
      parameters:
      - description: 停止参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.PipelineStopRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 停止成功
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 停止流水线
      tags:
      - CICD Pipeline
  /api/v1/k8s/cicd/pipeline/template-simulate:
    get:
      description: 模拟完整发布流程，验证参数和 Jenkins 配置（不实际触发构建）
      parameters:
      - description: '语言类型: go/java/frontend/python'
        in: query
        name: language_type
        required: true
        type: string
      - description: Git 仓库地址
        in: query
        name: git_repo
        required: true
        type: string
      - description: Git 分支，默认 main
        in: query
        name: git_branch
        type: string
      - description: 镜像仓库地址
        in: query
        name: image_repo
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 返回模拟结果
          schema:
            additionalProperties: true
            type: object
      summary: 模拟模板化发布流程
      tags:
      - CICD Pipeline
  /api/v1/k8s/cicd/pipeline/template-verify:
    get:
      description: 返回所有支持的语言模板信息，包括 Jenkins Job 名称、阶段、默认参数等
      produces:
      - application/json
      responses:
        "200":
          description: 返回模板列表
          schema:
            additionalProperties: true
            type: object
      summary: 验证模板化发布配置
      tags:
      - CICD Pipeline
  /api/v1/k8s/cicd/pipeline/update:
    post:
      consumes:
      - application/json
      description: 更新流水线配置信息
      parameters:
      - description: 更新参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.PipelineUpdateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 更新成功
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 更新流水线
      tags:
      - CICD Pipeline
  /api/v1/k8s/cicd/promote/chain:
    get:
      parameters:
      - description: 流水线ID
        in: query
        name: pipeline_id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 获取流水线的晋级链视图（各环境当前部署镜像/状态）
      tags:
      - CICD Promote
  /api/v1/k8s/cicd/promote/run:
    post:
      consumes:
      - application/json
      parameters:
      - description: 晋级参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.PipelinePromoteRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 镜像晋级：将已构建镜像发布到目标环境
      tags:
      - CICD Promote
  /api/v1/k8s/cicd/promote/targets:
    get:
      parameters:
      - description: 流水线ID
        in: query
        name: pipeline_id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 获取流水线的环境部署目标列表
      tags:
      - CICD Promote
  /api/v1/k8s/cicd/promote/targets/delete:
    post:
      consumes:
      - application/json
      parameters:
      - description: 删除参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.PipelineTargetDeleteRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 删除单个环境部署目标
      tags:
      - CICD Promote
  /api/v1/k8s/cicd/promote/targets/save:
    post:
      consumes:
      - application/json
      parameters:
      - description: 环境目标配置
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.PipelineTargetSaveRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 全量保存流水线的环境部署目标
      tags:
      - CICD Promote
  /api/v1/k8s/cicd/quick-onboard:
    post:
      consumes:
      - application/json
      description: '支持 5 种工作负载类型: Deployment/StatefulSet/DaemonSet/CronJob/Job'
      parameters:
      - description: 接入参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.QuickOnboardRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 快速接入应用（一键创建 K8s 资源 + 接入流水线 + 可选首次部署）
      tags:
      - CICD QuickOnboard
  /api/v1/k8s/cicd/release/batch-cancel:
    post:
      consumes:
      - application/json
      description: 批量取消（智能判断：已部署成功/运行中的会触发回滚，未部署的直接取消）
      parameters:
      - description: 批量取消参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.CicdReleaseBatchCancelRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 批量取消发布单
      tags:
      - CICD Release
  /api/v1/k8s/cicd/release/batch-retry:
    post:
      consumes:
      - application/json
      description: 批量重新发布（根据最近一次发布记录重新发布）
      parameters:
      - description: 批量发布参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.CicdReleaseBatchRetryRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 批量重新发布
      tags:
      - CICD Release
  /api/v1/k8s/cicd/release/batch-rollback:
    post:
      consumes:
      - application/json
      description: 批量回滚（根据最近一次发布记录回滚到上一个版本）
      parameters:
      - description: 批量回滚参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.CicdReleaseBatchRollbackRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 批量回滚发布单
      tags:
      - CICD Release
  /api/v1/k8s/cicd/release/cancel:
    post:
      consumes:
      - application/json
      description: 智能取消：已部署成功/运行中的会触发回滚，未部署的直接取消
      parameters:
      - description: 取消参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.CicdReleaseIDRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 取消发布单
      tags:
      - CICD Release
  /api/v1/k8s/cicd/release/delete:
    post:
      consumes:
      - application/json
      parameters:
      - description: 删除参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.CicdReleaseIDRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 删除发布单
      tags:
      - CICD Release
  /api/v1/k8s/cicd/release/detail:
    get:
      parameters:
      - description: 发布单ID
        in: query
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 获取发布单详情
      tags:
      - CICD Release
  /api/v1/k8s/cicd/release/history:
    get:
      description: 支持时间范围、命名空间、应用名筛选的发布历史查询
      parameters:
      - description: 页码
        in: query
        name: page
        type: integer
      - description: 每页数量
        in: query
        name: page_size
        type: integer
      - description: 应用名称
        in: query
        name: app_name
        type: string
      - description: 命名空间
        in: query
        name: namespace
        type: string
      - description: 状态
        in: query
        name: status
        type: string
      - description: 开始时间戳
        in: query
        name: start_time
        type: integer
      - description: 结束时间戳
        in: query
        name: end_time
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 应用发布历史查询（增强版）
      tags:
      - CICD Release
  /api/v1/k8s/cicd/release/list:
    get:
      parameters:
      - description: 页码
        in: query
        name: page
        type: integer
      - description: 每页数量
        in: query
        name: page_size
        type: integer
      - description: 应用名称
        in: query
        name: app_name
        type: string
      - description: 状态
        in: query
        name: status
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 获取发布单列表
      tags:
      - CICD Release
  /api/v1/k8s/cicd/release/retry:
    post:
      consumes:
      - application/json
      parameters:
      - description: 重试参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.CicdReleaseIDRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 重试发布单
      tags:
      - CICD Release
  /api/v1/k8s/cicd/release/rollback:
    post:
      consumes:
      - application/json
      description: 将已部署的工作负载回滚到上一个版本，会创建新的发布单执行回滚操作
      parameters:
      - description: 回滚参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.CicdReleaseIDRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 回滚发布单
      tags:
      - CICD Release
  /api/v1/k8s/cicd/release/stats:
    get:
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 获取发布单统计
      tags:
      - CICD Release
  /api/v1/k8s/cicd/release/stats-enhanced:
    get:
      description: 返回今日/本周发布数、成功率、状态分布
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 增强版发布统计
      tags:
      - CICD Release
  /api/v1/k8s/cicd/release/sync-from-pipeline:
    post:
      description: 将最近的流水线运行记录同步到发布管理页面
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 同步流水线运行记录到发布管理
      tags:
      - CICD Release
  /api/v1/k8s/cicd/release/tasks:
    get:
      parameters:
      - description: 发布单ID
        in: query
        name: release_id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 获取发布单下的任务列表
      tags:
      - CICD Release
  /api/v1/k8s/cicd/release/update:
    post:
      consumes:
      - application/json
      parameters:
      - description: 编辑参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.CicdReleaseUpdateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 编辑发布单
      tags:
      - CICD Release
  /api/v1/k8s/cicd/resource/approval/{id}:
    get:
      parameters:
      - description: 审批ID
        in: path
        name: id
        required: true
        type: integer
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 获取审批详情
      tags:
      - CICD Resource
  /api/v1/k8s/cicd/resource/approval/{id}/approve:
    put:
      consumes:
      - application/json
      parameters:
      - description: 审批ID
        in: path
        name: id
        required: true
        type: integer
      - description: 审批意见
        in: body
        name: body
        required: true
        schema:
          additionalProperties:
            type: string
          type: object
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 通过审批
      tags:
      - CICD Resource
  /api/v1/k8s/cicd/resource/approval/{id}/reject:
    put:
      consumes:
      - application/json
      parameters:
      - description: 审批ID
        in: path
        name: id
        required: true
        type: integer
      - description: 拒绝原因
        in: body
        name: body
        required: true
        schema:
          additionalProperties:
            type: string
          type: object
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 拒绝审批
      tags:
      - CICD Resource
  /api/v1/k8s/cicd/resource/approvals:
    get:
      parameters:
      - description: 状态：pending/approved/rejected
        in: query
        name: status
        type: string
      - description: 页码
        in: query
        name: page
        type: integer
      - description: 每页数量
        in: query
        name: page_size
        type: integer
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 获取审批列表
      tags:
      - CICD Resource
  /api/v1/k8s/cicd/resource/rule/{id}:
    put:
      consumes:
      - application/json
      parameters:
      - description: 规则ID
        in: path
        name: id
        required: true
        type: integer
      - description: 规则信息
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_models.CicdEnvResourceRule'
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 更新环境规则
      tags:
      - CICD Resource
  /api/v1/k8s/cicd/resource/rules:
    get:
      parameters:
      - description: 环境
        in: query
        name: env
        type: string
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 获取环境规则列表
      tags:
      - CICD Resource
  /api/v1/k8s/cicd/resource/template:
    post:
      consumes:
      - application/json
      parameters:
      - description: 模板信息
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_models.CicdResourceTemplate'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 创建资源模板
      tags:
      - CICD Resource
  /api/v1/k8s/cicd/resource/template/{id}:
    delete:
      parameters:
      - description: 模板ID
        in: path
        name: id
        required: true
        type: integer
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 删除资源模板
      tags:
      - CICD Resource
    get:
      parameters:
      - description: 模板ID
        in: path
        name: id
        required: true
        type: integer
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 获取资源模板详情
      tags:
      - CICD Resource
    put:
      consumes:
      - application/json
      parameters:
      - description: 模板ID
        in: path
        name: id
        required: true
        type: integer
      - description: 模板信息
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_models.CicdResourceTemplate'
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 更新资源模板
      tags:
      - CICD Resource
  /api/v1/k8s/cicd/resource/template/default:
    get:
      parameters:
      - description: 环境
        in: query
        name: env
        required: true
        type: string
      - description: 服务类型
        in: query
        name: service_type
        required: true
        type: string
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 获取默认资源模板
      tags:
      - CICD Resource
  /api/v1/k8s/cicd/resource/templates:
    get:
      consumes:
      - application/json
      description: 根据环境和服务类型获取资源模板列表
      parameters:
      - description: 环境：dev/test/staging/prod
        in: query
        name: env
        type: string
      - description: 服务类型：java/go/node/python
        in: query
        name: service_type
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 获取资源模板列表
      tags:
      - CICD Resource
  /api/v1/k8s/cicd/resource/validate:
    post:
      consumes:
      - application/json
      description: 校验资源配置是否符合环境规则，返回警告和错误
      parameters:
      - description: 校验参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.ResourceValidateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_internal_app_models.ResourceValidationResult'
      summary: 校验资源配置
      tags:
      - CICD Resource
  /api/v1/k8s/cicd/stage/approve:
    post:
      consumes:
      - application/json
      parameters:
      - description: 审批请求
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.StageApproveRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
      summary: 审批通过/拒绝阶段
      tags:
      - CICD-Stage
  /api/v1/k8s/cicd/stage/callback:
    post:
      consumes:
      - application/json
      parameters:
      - description: HMAC-SHA256 签名
        in: header
        name: X-Signature
        type: string
      - description: 阶段回调请求
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.StageCallbackRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
      summary: Jenkins 阶段回调
      tags:
      - CICD-Stage
  /api/v1/k8s/cicd/stage/cancel:
    post:
      consumes:
      - application/json
      parameters:
      - description: 阶段ID
        in: query
        name: stage_id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
      summary: 取消部署阶段
      tags:
      - CICD-Stage
  /api/v1/k8s/cicd/stage/deploy:
    post:
      consumes:
      - application/json
      parameters:
      - description: 部署请求
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.StageDeployRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
      summary: 执行部署阶段
      tags:
      - CICD-Stage
  /api/v1/k8s/cicd/stage/deploy-status:
    get:
      consumes:
      - application/json
      parameters:
      - description: 阶段ID
        in: query
        name: stage_id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
      summary: 查询部署阶段实时状态与 Pod 列表
      tags:
      - CICD-Stage
  /api/v1/k8s/cicd/stage/history:
    get:
      consumes:
      - application/json
      parameters:
      - description: 阶段ID
        in: query
        name: stage_id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
      summary: 获取部署历史版本列表
      tags:
      - CICD-Stage
  /api/v1/k8s/cicd/stage/list:
    get:
      consumes:
      - application/json
      parameters:
      - description: 运行记录ID
        in: query
        name: run_id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
      summary: 获取流水线运行阶段列表
      tags:
      - CICD-Stage
  /api/v1/k8s/cicd/stage/logs:
    get:
      consumes:
      - application/json
      parameters:
      - description: 阶段ID
        in: query
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
      summary: 获取阶段执行日志
      tags:
      - CICD-Stage
  /api/v1/k8s/cicd/stage/rollback:
    post:
      consumes:
      - application/json
      parameters:
      - description: 阶段ID
        in: query
        name: stage_id
        required: true
        type: integer
      - description: 目标 ReplicaSet 名称
        in: query
        name: target_rs
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
      summary: 回滚部署到指定版本
      tags:
      - CICD-Stage
  /api/v1/k8s/cicd/template/create:
    post:
      consumes:
      - application/json
      description: 创建新的流水线模板，可用于快速创建流水线
      parameters:
      - description: 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.TemplateCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 返回 template_id
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 创建流水线模板
      tags:
      - CICD Template
  /api/v1/k8s/cicd/template/delete:
    post:
      consumes:
      - application/json
      description: 软删除模板
      parameters:
      - description: 删除参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.TemplateIDRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 删除成功
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 删除流水线模板
      tags:
      - CICD Template
  /api/v1/k8s/cicd/template/detail:
    get:
      description: 获取模板的完整配置信息
      parameters:
      - description: 模板ID
        in: query
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 返回模板详情
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取流水线模板详情
      tags:
      - CICD Template
  /api/v1/k8s/cicd/template/list:
    get:
      description: 分页查询模板列表，支持关键字和类型筛选
      parameters:
      - description: 页码，默认1
        in: query
        name: page
        type: integer
      - description: 每页数量，默认10
        in: query
        name: page_size
        type: integer
      - description: 关键字（名称/描述）
        in: query
        name: keyword
        type: string
      - description: 类型筛选（frontend/backend/microservice/database/custom）
        in: query
        name: type
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 返回列表和总数
          schema:
            additionalProperties: true
            type: object
      summary: 获取流水线模板列表
      tags:
      - CICD Template
  /api/v1/k8s/cicd/template/update:
    post:
      consumes:
      - application/json
      description: 更新模板配置信息
      parameters:
      - description: 更新参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.TemplateUpdateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 更新成功
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 更新流水线模板
      tags:
      - CICD Template
  /api/v1/k8s/cluster/batch-delete:
    post:
      description: 批量软删除K8s集群
      parameters:
      - description: body
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.K8sClusterBatchDeleteRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      security:
      - ApiKeyAuth: []
      summary: 批量删除K8s集群
      tags:
      - K8s集群管理
  /api/v1/k8s/cluster/create:
    post:
      description: 创建K8s集群
      parameters:
      - description: body
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.K8sClusterCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      security:
      - ApiKeyAuth: []
      summary: 创建K8s集群
      tags:
      - K8s集群管理
  /api/v1/k8s/cluster/delete:
    post:
      description: 删除K8s集群（软删除）
      parameters:
      - description: body
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.K8sClusterDeleteRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      security:
      - ApiKeyAuth: []
      summary: 删除K8s集群
      tags:
      - K8s集群管理
  /api/v1/k8s/cluster/init:
    post:
      description: 初始化K8s集群（连通性检测/预热）
      parameters:
      - description: body
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.K8sClusterInitRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      security:
      - ApiKeyAuth: []
      summary: 初始化K8s集群
      tags:
      - K8s集群管理
  /api/v1/k8s/cluster/list:
    get:
      description: 列出K8s集群
      parameters:
      - description: K8s集群名
        in: query
        maxLength: 100
        name: cluster_name
        type: string
      - description: 页码
        in: query
        name: page
        required: true
        type: integer
      - description: 每页数量
        in: query
        name: limit
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      security:
      - ApiKeyAuth: []
      summary: 列出K8s集群
      tags:
      - K8s集群管理
  /api/v1/k8s/cluster/update:
    post:
      description: 修改K8s集群
      parameters:
      - description: body
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.K8sClusterUpdateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      security:
      - ApiKeyAuth: []
      summary: 修改K8s集群
      tags:
      - K8s集群管理
  /api/v1/k8s/configmap/apply-yaml:
    post:
      consumes:
      - application/json
      parameters:
      - description: YAML 内容
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeConfigMapApplyYamlRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 从 YAML 创建或更新 ConfigMap
      tags:
      - K8s ConfigMap 管理
  /api/v1/k8s/configmap/create:
    post:
      consumes:
      - application/json
      description: 创建 Kubernetes ConfigMap（支持 data 与 binaryData）
      parameters:
      - description: ConfigMap 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeConfigMapCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 创建 ConfigMap
      tags:
      - K8s ConfigMap 管理
  /api/v1/k8s/configmap/delete:
    delete:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: ConfigMap 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 删除 ConfigMap
      tags:
      - K8s ConfigMap 管理
  /api/v1/k8s/configmap/detail:
    get:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: ConfigMap 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 获取 ConfigMap 详情
      tags:
      - K8s ConfigMap 管理
  /api/v1/k8s/configmap/list:
    get:
      description: 支持分页、命名空间过滤、名称模糊查询
      parameters:
      - description: 命名空间
        in: query
        maxLength: 100
        name: namespace
        type: string
      - description: ConfigMap 名称(模糊匹配)
        in: query
        maxLength: 100
        name: name
        type: string
      - description: 页码 (从1开始)
        in: query
        name: page
        required: true
        type: integer
      - description: 每页数量 (默认20)
        in: query
        name: limit
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 K8s ConfigMap 列表
      tags:
      - K8s ConfigMap 管理
  /api/v1/k8s/configmap/patch:
    patch:
      consumes:
      - application/strategic-merge-patch+json
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: ConfigMap 名称
        in: query
        name: name
        required: true
        type: string
      - description: Patch Body(JSON字符串)
        in: body
        name: content
        required: true
        schema:
          type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: Patch ConfigMap（StrategicMergePatch）
      tags:
      - K8s ConfigMap 管理
  /api/v1/k8s/configmap/patch-json:
    post:
      consumes:
      - application/merge-patch+json
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: ConfigMap 名称
        in: query
        name: name
        required: true
        type: string
      - description: Patch Body(JSON字符串)
        in: body
        name: content
        required: true
        schema:
          type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: Patch ConfigMap（JSON Merge Patch – 覆盖式）
      tags:
      - K8s ConfigMap 管理
  /api/v1/k8s/configmap/update-data:
    put:
      consumes:
      - application/json
      parameters:
      - description: ConfigMap data 更新参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeConfigMapUpdateDataRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 更新 ConfigMap data 字段
      tags:
      - K8s ConfigMap 管理
  /api/v1/k8s/configmap/yaml:
    get:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: ConfigMap 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 ConfigMap YAML
      tags:
      - K8s ConfigMap 管理
  /api/v1/k8s/cronjob/apply-yaml:
    put:
      consumes:
      - application/json
      parameters:
      - description: YAML 内容
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.YamlCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 应用 CronJob YAML 配置
      tags:
      - K8s CronJob 管理
  /api/v1/k8s/cronjob/create:
    post:
      consumes:
      - application/json
      parameters:
      - description: 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeCronJobCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 创建 CronJob
      tags:
      - K8s CronJob 管理
  /api/v1/k8s/cronjob/create-from-yaml:
    post:
      consumes:
      - application/json
      parameters:
      - description: YAML 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.YamlCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 从 YAML 创建 CronJob
      tags:
      - K8s CronJob 管理
  /api/v1/k8s/cronjob/delete:
    delete:
      parameters:
      - description: 删除参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeCronJobDeleteRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 删除 CronJob
      tags:
      - K8s CronJob 管理
  /api/v1/k8s/cronjob/detail:
    get:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: CronJob 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 CronJob 详情（含历史 Job 列表）
      tags:
      - K8s CronJob 管理
  /api/v1/k8s/cronjob/events:
    get:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        type: string
      - description: CronJob 名称
        in: query
        name: name
        type: string
      - description: 返回条数限制（默认50）
        in: query
        name: limit
        type: integer
      - description: 最近N秒的事件
        in: query
        name: since_seconds
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 CronJob 相关事件
      tags:
      - K8s CronJob 管理
  /api/v1/k8s/cronjob/list:
    get:
      description: 支持分页、命名空间过滤、名称模糊查询
      parameters:
      - description: 命名空间
        in: query
        maxLength: 100
        name: namespace
        type: string
      - description: CronJob 名称(模糊匹配)
        in: query
        maxLength: 100
        name: name
        type: string
      - description: 页码 (从1开始)
        in: query
        name: page
        required: true
        type: integer
      - description: 每页数量 (默认20)
        in: query
        name: limit
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 K8s CronJob 列表
      tags:
      - K8s CronJob 管理
  /api/v1/k8s/cronjob/suspend:
    patch:
      consumes:
      - application/json
      description: 用于暂停或恢复指定命名空间下的 CronJob（通过设置 .spec.suspend 字段为 true/false）
      parameters:
      - description: CronJob 暂停/恢复请求体
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeCronJobSuspendRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 暂停或恢复 CronJob
      tags:
      - K8s CronJob 管理
  /api/v1/k8s/cronjob/trigger:
    post:
      consumes:
      - application/json
      description: 立即创建一个 Job（不等待调度时间）
      parameters:
      - description: 触发参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeCronJobTriggerRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 手动触发 CronJob
      tags:
      - K8s CronJob 管理
  /api/v1/k8s/cronjob/update-from-yaml:
    put:
      consumes:
      - application/json
      parameters:
      - description: YAML 更新参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.YamlCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 从 YAML 更新 CronJob
      tags:
      - K8s CronJob 管理
  /api/v1/k8s/cronjob/yaml:
    get:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: CronJob 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 CronJob YAML
      tags:
      - K8s CronJob 管理
  /api/v1/k8s/daemonset/apply_yaml:
    put:
      consumes:
      - application/json
      description: 应用修改后的 YAML 配置到 DaemonSet
      parameters:
      - description: YAML内容
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeApplyYamlRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 应用 DaemonSet YAML 配置
      tags:
      - K8s DaemonSet 管理
  /api/v1/k8s/daemonset/create:
    post:
      consumes:
      - application/json
      parameters:
      - description: 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeDaemonSetCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 创建 DaemonSet（可选创建 Service）
      tags:
      - K8s DaemonSet 管理
  /api/v1/k8s/daemonset/create-from-yaml:
    post:
      consumes:
      - application/json
      parameters:
      - description: YAML 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.YamlCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 从 YAML 创建 DaemonSet
      tags:
      - K8s DaemonSet 管理
  /api/v1/k8s/daemonset/delete:
    delete:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: DaemonSet 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 删除 DaemonSet
      tags:
      - K8s DaemonSet 管理
  /api/v1/k8s/daemonset/delete_service:
    delete:
      description: 删除指定命名空间下，与 DaemonSet 同名的 Service 资源
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: DaemonSet 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Service 删除成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 服务器内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 删除 DaemonSet 对应的 Service
      tags:
      - K8s DaemonSet 管理
  /api/v1/k8s/daemonset/detail:
    get:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: DaemonSet 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 获取 DaemonSet 详情
      tags:
      - K8s DaemonSet 管理
  /api/v1/k8s/daemonset/ds_pods:
    get:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: DaemonSet 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 获取 DaemonSet 关联的 Pod 列表
      tags:
      - K8s DaemonSet 管理
  /api/v1/k8s/daemonset/events:
    post:
      consumes:
      - application/json
      parameters:
      - description: 事件查询参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeEventListRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 获取 DaemonSet 相关事件
      tags:
      - K8s DaemonSet 管理
  /api/v1/k8s/daemonset/history:
    get:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: DaemonSet 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 获取 DaemonSet 历史版本
      tags:
      - K8s DaemonSet 管理
  /api/v1/k8s/daemonset/list:
    get:
      description: 支持分页、命名空间过滤、名称模糊查询
      parameters:
      - description: 命名空间
        in: query
        maxLength: 100
        name: namespace
        type: string
      - description: DaemonSet 名称(模糊匹配)
        in: query
        maxLength: 100
        name: name
        type: string
      - description: 页码 (从1开始)
        in: query
        name: page
        required: true
        type: integer
      - description: 每页数量 (默认20)
        in: query
        name: limit
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 K8s DaemonSet 列表
      tags:
      - K8s DaemonSet 管理
  /api/v1/k8s/daemonset/restart:
    post:
      consumes:
      - application/json
      description: 触发指定命名空间下 DaemonSet 的滚动重启（等价于 kubectl rollout restart ds <name>）
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: DaemonSet 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: DaemonSet 重启成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 重启 DaemonSet
      tags:
      - K8s DaemonSet 管理
  /api/v1/k8s/daemonset/rollback:
    post:
      consumes:
      - application/json
      description: 将 DaemonSet 回滚到指定的历史版本（ControllerRevision）。不传可在服务端实现“回滚到上一个版本”的兜底策略（可选）。
      parameters:
      - description: 回滚参数（namespace、name、revision_name）
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeDaemonSetRollbackRequest'
      produces:
      - application/json
      responses:
        "200":
          description: DaemonSet 回滚成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 回滚 DaemonSet
      tags:
      - K8s DaemonSet 管理
  /api/v1/k8s/daemonset/update-resources:
    post:
      consumes:
      - application/json
      description: 直接修改 DaemonSet 容器的 CPU/Memory requests/limits
      parameters:
      - description: 资源配置
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeDeploymentUpdateResourcesRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 快速修改容器资源限制
      tags:
      - K8s DaemonSet 管理
  /api/v1/k8s/daemonset/update_image:
    put:
      consumes:
      - application/json
      description: 修改指定命名空间下 DaemonSet 的容器镜像（支持滚动更新）
      parameters:
      - description: 更新镜像参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeDaemonSetUpdateImageRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 更新成功
          schema:
            type: string
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 服务器内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 更新 DaemonSet 容器镜像
      tags:
      - K8s DaemonSet 管理
  /api/v1/k8s/daemonset/yaml:
    get:
      description: 获取指定 DaemonSet 的 YAML 配置
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: DaemonSet 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 DaemonSet YAML 配置
      tags:
      - K8s DaemonSet 管理
  /api/v1/k8s/deployment/apply_yaml:
    put:
      consumes:
      - application/json
      description: 应用修改后的 YAML 配置到 Deployment
      parameters:
      - description: YAML内容
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeApplyYamlRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 应用 Deployment YAML 配置
      tags:
      - K8s Deployment 管理
  /api/v1/k8s/deployment/create:
    post:
      consumes:
      - application/json
      parameters:
      - description: 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeDeploymentCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 创建 Deployment（可选创建 Service）
      tags:
      - K8s Deployment 管理
  /api/v1/k8s/deployment/create-from-yaml:
    post:
      consumes:
      - application/json
      parameters:
      - description: YAML 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.YamlCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 从 YAML 创建 Deployment
      tags:
      - K8s Deployment 管理
  /api/v1/k8s/deployment/delete:
    delete:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Deployment 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 删除 Deployment
      tags:
      - K8s Deployment 管理
  /api/v1/k8s/deployment/delete_service:
    delete:
      description: 删除指定命名空间下，与 Deployment 同名的 Service 资源
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Deployment 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: Service 删除成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 服务器内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 删除 Deployment 对应的 Service
      tags:
      - K8s Deployment 管理
  /api/v1/k8s/deployment/detail:
    get:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Deployment 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 获取 Deployment 详情
      tags:
      - K8s Deployment 管理
  /api/v1/k8s/deployment/events:
    post:
      description: 按命名空间、资源类型/名称、事件类型(Type)、原因(Reason)等过滤；支持最近N秒窗口与分页（continue游标）。
      parameters:
      - description: 命名空间（为空=全局）
        in: query
        name: namespace
        type: string
      - description: 资源类型（如 Pod/Deployment/Node）
        in: query
        name: kind
        type: string
      - description: 资源名称
        in: query
        name: name
        type: string
      - description: 事件类型（Normal | Warning）
        in: query
        name: type
        type: string
      - description: 事件原因（如 FailedScheduling/BackOff）
        in: query
        name: reason
        type: string
      - description: 返回条数限制（默认50，最大500）
        in: query
        name: limit
        type: integer
      - description: K8s 分页游标（下一页传回上次返回的 next）
        in: query
        name: continue
        type: string
      - description: 最近N秒的事件（客户端二次过滤）
        in: query
        name: since_seconds
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 事件列表加载完成
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 服务器内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取事件列表（支持全局或按对象筛选）
      tags:
      - K8s Event 管理
  /api/v1/k8s/deployment/history:
    get:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Deployment 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 获取 Deployment 历史版本（ReplicaSet 列表）
      tags:
      - K8s Deployment 管理
  /api/v1/k8s/deployment/list:
    get:
      description: 支持分页、命名空间过滤、名称模糊查询
      parameters:
      - description: 命名空间
        in: query
        maxLength: 100
        name: namespace
        type: string
      - description: Deployment 名称(模糊匹配)
        in: query
        maxLength: 100
        name: name
        type: string
      - description: 页码 (从1开始)
        in: query
        name: page
        required: true
        type: integer
      - description: 每页数量 (默认20)
        in: query
        name: limit
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 K8s Deployment 列表
      tags:
      - K8s Deployment 管理
  /api/v1/k8s/deployment/patch:
    put:
      consumes:
      - application/json
      parameters:
      - description: Patch 内容
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeDeploymentUpdateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Patch 模板（JSONPatch / StrategicMergePatch）
      tags:
      - K8s Deployment 管理
  /api/v1/k8s/deployment/pause:
    post:
      consumes:
      - application/json
      description: 暂停 Deployment 的滚动更新（等同于 kubectl rollout pause）
      parameters:
      - description: 参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeDeploymentPauseResumeRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 暂停 Rollout
      tags:
      - K8s Deployment 管理
  /api/v1/k8s/deployment/pods.js:
    get:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Deployment 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 获取 Deployment 对应的 Pod 列表
      tags:
      - K8s Deployment 管理
  /api/v1/k8s/deployment/restart:
    post:
      consumes:
      - application/json
      parameters:
      - description: 重启参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeDeploymentRestartRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 滚动重启 Deployment
      tags:
      - K8s Deployment 管理
  /api/v1/k8s/deployment/resume:
    post:
      consumes:
      - application/json
      description: 恢复 Deployment 的滚动更新（等同于 kubectl rollout resume）
      parameters:
      - description: 参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeDeploymentPauseResumeRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 恢复 Rollout
      tags:
      - K8s Deployment 管理
  /api/v1/k8s/deployment/rollback:
    post:
      consumes:
      - application/json
      parameters:
      - description: 回滚参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeDeploymentRollbackRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 回滚到指定 ReplicaSet
      tags:
      - K8s Deployment 管理
  /api/v1/k8s/deployment/rollout-status:
    get:
      description: 获取 Deployment 的滚动更新详细状态（副本进度、策略配置、RS 列表等）
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Deployment 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 获取 Rollout 状态
      tags:
      - K8s Deployment 管理
  /api/v1/k8s/deployment/scale:
    put:
      consumes:
      - application/json
      parameters:
      - description: 扩缩容参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeDeploymentScaleRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 扩缩容（修改副本数）
      tags:
      - K8s Deployment 管理
  /api/v1/k8s/deployment/update-image:
    put:
      consumes:
      - application/json
      parameters:
      - description: 更新镜像参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeDeploymentUpdateImageRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 更新镜像（触发滚动升级）
      tags:
      - K8s Deployment 管理
  /api/v1/k8s/deployment/update-resources:
    post:
      consumes:
      - application/json
      description: 直接修改 Deployment 容器的 CPU/Memory requests/limits，用于快速应对 OOM 等场景
      parameters:
      - description: 资源配置
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeDeploymentUpdateResourcesRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 快速修改容器资源限制
      tags:
      - K8s Deployment 管理
  /api/v1/k8s/deployment/update-strategy:
    post:
      consumes:
      - application/json
      description: 更新 Deployment 的滚动更新策略（maxSurge/maxUnavailable/minReadySeconds 等）
      parameters:
      - description: 滚动更新策略参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeDeploymentRollingUpdateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 更新滚动更新策略
      tags:
      - K8s Deployment 管理
  /api/v1/k8s/deployment/yaml:
    get:
      description: 获取指定 Deployment 的 YAML 格式配置
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Deployment 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 Deployment 的 YAML 配置
      tags:
      - K8s Deployment 管理
  /api/v1/k8s/hpa/batch-scale:
    post:
      consumes:
      - application/json
      parameters:
      - description: 批量扩缩容
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeHPABatchScaleRequest'
      responses: {}
      summary: 批量扩容/缩容 HPA（618 促销场景）
      tags:
      - K8s HPA 弹性扩缩容
  /api/v1/k8s/hpa/batch-status:
    post:
      responses: {}
      summary: 批量查询 HPA 当前状态（统一数据，验证扩缩容是否成功）
      tags:
      - K8s HPA 弹性扩缩容
  /api/v1/k8s/hpa/create:
    post:
      consumes:
      - application/json
      parameters:
      - description: 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeHPACreateRequest'
      responses: {}
      summary: 创建 HPA
      tags:
      - K8s HPA 弹性扩缩容
  /api/v1/k8s/hpa/create-from-yaml:
    post:
      consumes:
      - application/json
      parameters:
      - description: YAML 内容
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.YamlCreateRequest'
      responses: {}
      summary: 通过 YAML 创建 HPA
      tags:
      - K8s HPA 弹性扩缩容
  /api/v1/k8s/hpa/delete:
    delete:
      responses: {}
      summary: 删除 HPA
      tags:
      - K8s HPA 弹性扩缩容
  /api/v1/k8s/hpa/detail:
    get:
      responses: {}
      summary: 获取 HPA 详情
      tags:
      - K8s HPA 弹性扩缩容
  /api/v1/k8s/hpa/list:
    get:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        type: string
      - description: HPA 名称(模糊)
        in: query
        name: name
        type: string
      - description: 页码
        in: query
        name: page
        type: integer
      - description: 每页数量
        in: query
        name: limit
        type: integer
      produces:
      - application/json
      responses: {}
      summary: 获取 HPA 列表
      tags:
      - K8s HPA 弹性扩缩容
  /api/v1/k8s/hpa/scale:
    post:
      responses: {}
      summary: 单独修改 HPA 副本数（min/max）
      tags:
      - K8s HPA 弹性扩缩容
  /api/v1/k8s/hpa/update:
    post:
      responses: {}
      summary: 更新 HPA
      tags:
      - K8s HPA 弹性扩缩容
  /api/v1/k8s/ingress/apply-yaml:
    put:
      consumes:
      - application/json
      parameters:
      - description: YAML 内容
        in: body
        name: body
        required: true
        schema:
          properties:
            yaml:
              type: string
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 应用 Ingress YAML
      tags:
      - K8s Ingress 管理
  /api/v1/k8s/ingress/create:
    post:
      consumes:
      - application/json
      parameters:
      - description: 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeIngressCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 创建 Ingress
      tags:
      - K8s Ingress 管理
  /api/v1/k8s/ingress/create-from-yaml:
    post:
      consumes:
      - application/json
      parameters:
      - description: YAML 内容
        in: body
        name: body
        required: true
        schema:
          properties:
            yaml:
              type: string
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 从 YAML 创建 Ingress
      tags:
      - K8s Ingress 管理
  /api/v1/k8s/ingress/delete:
    delete:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Ingress 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 删除 Ingress
      tags:
      - K8s Ingress 管理
  /api/v1/k8s/ingress/detail:
    get:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Ingress 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 获取 Ingress 详情
      tags:
      - K8s Ingress 管理
  /api/v1/k8s/ingress/list:
    get:
      description: 支持分页、命名空间过滤、名称模糊查询
      parameters:
      - description: 命名空间
        in: query
        maxLength: 100
        name: namespace
        type: string
      - description: Ingress 名称(模糊匹配)
        in: query
        maxLength: 100
        name: name
        type: string
      - description: 页码 (从1开始)
        in: query
        name: page
        required: true
        type: integer
      - description: 每页数量 (默认20)
        in: query
        name: limit
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 K8s Ingress 列表
      tags:
      - K8s Ingress 管理
  /api/v1/k8s/ingress/patch:
    patch:
      consumes:
      - application/strategic-merge-patch+json
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Ingress 名称
        in: query
        name: name
        required: true
        type: string
      - description: Patch Body(JSON字符串)
        in: body
        name: content
        required: true
        schema:
          type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Patch Ingress（StrategicMergePatch）
      tags:
      - K8s Ingress 管理
  /api/v1/k8s/ingress/patch-json:
    post:
      consumes:
      - application/merge-patch+json
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Ingress 名称
        in: query
        name: name
        required: true
        type: string
      - description: Patch Body(JSON字符串)
        in: body
        name: content
        required: true
        schema:
          type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Patch Ingress（JSON Merge Patch – 覆盖式）
      tags:
      - K8s Ingress 管理
  /api/v1/k8s/ingress/yaml:
    get:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Ingress 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - text/plain
      responses:
        "200":
          description: YAML 内容
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 获取 Ingress 的 YAML
      tags:
      - K8s Ingress 管理
  /api/v1/k8s/job/apply-yaml:
    put:
      consumes:
      - application/json
      parameters:
      - description: YAML 应用参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.YamlApplyRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 应用 YAML 修改 Job
      tags:
      - K8s Job 管理
  /api/v1/k8s/job/create:
    post:
      consumes:
      - application/json
      parameters:
      - description: 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeJobCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 创建 Job
      tags:
      - K8s Job 管理
  /api/v1/k8s/job/create-from-yaml:
    post:
      consumes:
      - application/json
      parameters:
      - description: YAML 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.YamlCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 从 YAML 创建 Job
      tags:
      - K8s Job 管理
  /api/v1/k8s/job/delete:
    delete:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Job 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 删除 Job
      tags:
      - K8s Job 管理
  /api/v1/k8s/job/detail:
    get:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Job 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 Job 详情
      tags:
      - K8s Job 管理
  /api/v1/k8s/job/events:
    get:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        type: string
      - description: Job 名称
        in: query
        name: name
        type: string
      - description: 返回条数限制（默认50）
        in: query
        name: limit
        type: integer
      - description: 最近N秒的事件
        in: query
        name: since_seconds
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 Job 相关事件
      tags:
      - K8s Job 管理
  /api/v1/k8s/job/list:
    get:
      description: 支持分页、命名空间过滤、名称模糊查询
      parameters:
      - description: 命名空间
        in: query
        maxLength: 100
        name: namespace
        type: string
      - description: Job 名称(模糊匹配)
        in: query
        maxLength: 100
        name: name
        type: string
      - description: 页码 (从1开始)
        in: query
        name: page
        required: true
        type: integer
      - description: 每页数量 (默认20)
        in: query
        name: limit
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 K8s Job 列表
      tags:
      - K8s Job 管理
  /api/v1/k8s/job/restart:
    post:
      consumes:
      - application/json
      description: 基于已有 Job 模板重新创建一个新的 Job（清除状态、生成新名称）
      parameters:
      - description: Job 重启请求体
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeJobRestartRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 重启 Job
      tags:
      - K8s Job 管理
  /api/v1/k8s/job/suspend:
    patch:
      consumes:
      - application/json
      description: 用于暂停或恢复指定命名空间下的 Job（通过设置 .spec.suspend 字段为 true/false）
      parameters:
      - description: Job 暂停/恢复请求体
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeJobSuspendRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 暂停或恢复 Job
      tags:
      - K8s Job 管理
  /api/v1/k8s/job/update-image:
    put:
      consumes:
      - application/json
      description: 更新指定容器的镜像地址
      parameters:
      - description: 更新镜像参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeJobUpdateImageRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 更新 Job 镜像
      tags:
      - K8s Job 管理
  /api/v1/k8s/job/yaml:
    get:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Job 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 Job YAML
      tags:
      - K8s Job 管理
  /api/v1/k8s/multi-resource/apply-yaml:
    post:
      consumes:
      - application/json
      parameters:
      - description: 多资源 YAML 内容
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeMultiResourceApplyYamlRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 创建结果
          schema:
            $ref: '#/definitions/k8soperation_internal_app_requests.MultiResourceCreateResult'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 应用多资源 YAML
      tags:
      - K8s 多资源管理
  /api/v1/k8s/multi-resource/parse-yaml:
    post:
      consumes:
      - application/json
      parameters:
      - description: 多资源 YAML 内容
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeMultiResourceApplyYamlRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 解析结果
          schema:
            $ref: '#/definitions/k8soperation_internal_app_requests.MultiResourceParsedResult'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 解析多资源 YAML
      tags:
      - K8s 多资源管理
  /api/v1/k8s/namespace/apply_yaml:
    put:
      consumes:
      - application/json
      description: 应用修改后的 YAML 配置到 Namespace
      parameters:
      - description: YAML内容
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeApplyYamlClusterRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 应用 Namespace YAML 配置
      tags:
      - K8s Namespace 管理
  /api/v1/k8s/namespace/create:
    post:
      consumes:
      - application/json
      description: 创建命名空间，并可选设置 labels/annotations 和资源配额（CPU/Memory/Pods）
      parameters:
      - description: Namespace 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeNamespaceCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 创建 Namespace
      tags:
      - K8s Namespace 管理
  /api/v1/k8s/namespace/delete:
    delete:
      description: 删除指定 Namespace（级联删除内部资源）
      parameters:
      - description: Namespace 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 删除失败
          schema:
            additionalProperties: true
            type: object
      summary: 删除 Namespace
      tags:
      - K8s Namespace 管理
  /api/v1/k8s/namespace/detail:
    get:
      description: 查询指定 Namespace 的详细信息，包含资源统计
      parameters:
      - description: Namespace 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "404":
          description: 资源不存在
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 Namespace 详情
      tags:
      - K8s Namespace 管理
  /api/v1/k8s/namespace/labels:
    patch:
      consumes:
      - application/json
      description: 添加或删除 Namespace 标签
      parameters:
      - description: 标签修改参数
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeNamespaceLabelPatchRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 修改 Namespace 标签
      tags:
      - K8s Namespace 管理
  /api/v1/k8s/namespace/list:
    get:
      description: 支持分页、名称模糊查询，返回每个命名空间的资源统计
      parameters:
      - description: 命名空间名称(模糊匹配)
        in: query
        maxLength: 100
        name: name
        type: string
      - description: 页码(从1开始)
        in: query
        name: page
        required: true
        type: integer
      - description: 每页数量(默认20)
        in: query
        name: limit
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 Namespace 列表
      tags:
      - K8s Namespace 管理
  /api/v1/k8s/namespace/patch:
    patch:
      description: 支持新增、更新、删除 labels 与 annotations
      parameters:
      - description: Namespace 名称
        in: query
        name: name
        required: true
        type: string
      - description: Patch 内容
        in: body
        name: patch
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeNamespaceUpdateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 修改成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 修改 Namespace（labels / annotations）
      tags:
      - K8s Namespace 管理
  /api/v1/k8s/namespace/yaml:
    get:
      description: 获取指定 Namespace 的 YAML 配置
      parameters:
      - description: Namespace 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 Namespace YAML 配置
      tags:
      - K8s Namespace 管理
  /api/v1/k8s/node/apply_yaml:
    put:
      consumes:
      - application/json
      description: 应用修改后的 YAML 配置到 Node
      parameters:
      - description: YAML内容
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeApplyYamlClusterRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 应用 Node YAML 配置
      tags:
      - K8s Node 管理
  /api/v1/k8s/node/cordon:
    post:
      consumes:
      - application/json
      description: 通过设置 spec.unschedulable 实现 cordon / uncordon
      parameters:
      - description: Node 调度控制参数
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeNodeCordonRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "404":
          description: 资源不存在
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 标记 Node 是否可调度（cordon / uncordon）
      tags:
      - K8s Node 管理
  /api/v1/k8s/node/detail:
    get:
      description: 查询指定 Node 的详情（Node 为集群级资源，无 namespace 参数）
      parameters:
      - description: Node 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "404":
          description: 资源不存在
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 Node 详情
      tags:
      - K8s Node 管理
  /api/v1/k8s/node/drain:
    post:
      consumes:
      - application/json
      description: cordon 节点并驱逐其上非 DaemonSet/非静态 Pod（维护/下线常用）
      parameters:
      - description: Node 驱逐参数
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeNodeDrainRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "404":
          description: 资源不存在
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 驱逐节点上的可驱逐 Pod（drain）
      tags:
      - K8s Node 管理
  /api/v1/k8s/node/events:
    get:
      description: 查询与节点相关的 Events
      parameters:
      - description: Node 名称
        in: query
        name: name
        required: true
        type: string
      - description: 最大返回条数
        in: query
        name: limit
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取节点事件
      tags:
      - K8s Node 管理
  /api/v1/k8s/node/labels:
    patch:
      consumes:
      - application/json
      description: 添加或删除节点标签
      parameters:
      - description: 标签修改参数
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeNodeLabelPatchRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 修改节点标签
      tags:
      - K8s Node 管理
  /api/v1/k8s/node/list:
    get:
      description: 支持分页、名称模糊查询（Node 为集群级资源，无 namespace 参数）
      parameters:
      - description: Node 名称(模糊匹配)
        in: query
        maxLength: 100
        name: name
        type: string
      - description: 页码(从1开始)
        in: query
        name: page
        required: true
        type: integer
      - description: 每页数量(默认20)
        in: query
        name: limit
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 Node 列表
      tags:
      - K8s Node 管理
  /api/v1/k8s/node/metrics:
    get:
      description: name 为空则返回全量节点指标；填写则返回指定节点。
      parameters:
      - description: Node 名称（为空=全局）
        in: query
        name: name
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 Node 指标（CPU/内存使用率）
      tags:
      - K8s Node 管理
  /api/v1/k8s/node/pods.js:
    get:
      description: 支持分页、名称模糊查询（Pod 属于命名空间级资源，但此处跨命名空间按 Node 过滤）
      parameters:
      - description: Node 名称
        in: query
        name: nodeName
        required: true
        type: string
      - description: Pod 名称(模糊匹配)
        in: query
        maxLength: 100
        name: name
        type: string
      - description: 页码(从1开始)
        in: query
        name: page
        required: true
        type: integer
      - description: 每页数量(默认20)
        in: query
        name: limit
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取指定 Node 上的 Pod 列表
      tags:
      - K8s Node 管理
  /api/v1/k8s/node/taints:
    patch:
      consumes:
      - application/json
      description: 添加或删除节点污点
      parameters:
      - description: 污点修改参数
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeNodeTaintPatchRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 修改节点污点
      tags:
      - K8s Node 管理
  /api/v1/k8s/node/yaml:
    get:
      description: 获取指定 Node 的 YAML 配置
      parameters:
      - description: Node 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 Node YAML 配置
      tags:
      - K8s Node 管理
  /api/v1/k8s/pod:
    post:
      consumes:
      - application/json
      description: 创建一个 Kubernetes Pod（用于调试或临时用途）
      parameters:
      - description: 创建 Pod 请求参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubePodCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 创建 Pod 失败
          schema:
            additionalProperties: true
            type: object
      summary: 创建 K8s Pod
      tags:
      - K8s Pod管理
  /api/v1/k8s/pod/apply_yaml:
    put:
      consumes:
      - application/json
      description: 应用修改后的 YAML 配置到 Pod
      parameters:
      - description: YAML内容
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubePodApplyYamlRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 应用 Pod YAML 配置
      tags:
      - K8s Pod管理
  /api/v1/k8s/pod/container_image:
    get:
      description: 获取Pod的容器镜像
      parameters:
      - description: Pod名
        in: query
        maxLength: 100
        name: name
        type: string
      - description: 命名空间
        in: query
        maxLength: 100
        name: namespace
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取Pod的容器镜像
      tags:
      - K8s Pod管理
  /api/v1/k8s/pod/container_log:
    get:
      description: 由全局开关 PodLog.EnableStreaming 控制：true=实时流式(text/plain)，false=一次性(JSON)
      parameters:
      - description: Pod名
        in: query
        maxLength: 100
        name: name
        required: true
        type: string
      - description: 命名空间
        in: query
        maxLength: 100
        name: namespace
        required: true
        type: string
      - description: 容器名(可选; 多容器建议指定)
        in: query
        maxLength: 100
        name: container
        type: string
      - description: 仅返回最后N行(默认见配置)
        in: query
        name: tail
        type: integer
      produces:
      - application/json
      - text/plain
      responses:
        "200":
          description: 流式文本(EnableStreaming=true)
          schema:
            type: string
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 实时跟随 Pod 容器日志
      tags:
      - K8s Pod管理
  /api/v1/k8s/pod/container_logs:
    get:
      description: 获取Pod的容器日志
      parameters:
      - description: Pod名
        in: query
        maxLength: 100
        name: name
        type: string
      - description: 命名空间
        in: query
        maxLength: 100
        name: namespace
        type: string
      - description: 容器
        in: query
        maxLength: 100
        name: container
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取Pod的容器日志
      tags:
      - K8s Pod管理
  /api/v1/k8s/pod/container_name:
    get:
      description: 获取Pod的容器名
      parameters:
      - description: Pod名
        in: query
        maxLength: 100
        name: name
        type: string
      - description: 命名空间
        in: query
        maxLength: 100
        name: namespace
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取Pod的容器名
      tags:
      - K8s Pod管理
  /api/v1/k8s/pod/create-from-yaml:
    post:
      consumes:
      - application/json
      parameters:
      - description: YAML 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.YamlCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 从 YAML 创建 Pod
      tags:
      - K8s Pod管理
  /api/v1/k8s/pod/detail:
    get:
      description: 获取Pod的详情
      parameters:
      - description: Pod名
        in: query
        maxLength: 100
        name: name
        type: string
      - description: 命名空间
        in: query
        maxLength: 100
        name: namespace
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取Pod的详情
      tags:
      - K8s Pod管理
  /api/v1/k8s/pod/events:
    post:
      consumes:
      - application/json
      parameters:
      - description: 查询参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeEventListRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 获取 Pod 事件列表
      tags:
      - K8s Pod 管理
  /api/v1/k8s/pod/evict:
    post:
      consumes:
      - application/json
      description: 通过 Eviction API 驱逐某个 Pod（受 PDB 约束）
      parameters:
      - description: Pod 驱逐参数
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubePodEvictRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "404":
          description: Pod 不存在
          schema:
            additionalProperties: true
            type: object
        "429":
          description: 被 PDB 限制
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 驱逐指定 Pod
      tags:
      - K8s Pod管理
  /api/v1/k8s/pod/grace_delete_pod:
    delete:
      description: 删除指定命名空间下的 Pod（支持优雅终止/强制删除）
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Pod 名称
        in: query
        name: name
        required: true
        type: string
      - description: 优雅终止秒数（默认30）
        in: query
        name: grace_seconds
        type: integer
      - description: 是否强制删除（默认false）
        in: query
        name: force
        type: boolean
      produces:
      - application/json
      responses:
        "200":
          description: 删除请求已提交
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 删除 Pod
      tags:
      - K8s Pod管理
  /api/v1/k8s/pod/init_container_image:
    get:
      description: 获取Pod的Init容器镜像
      parameters:
      - description: Pod名
        in: query
        maxLength: 100
        name: name
        type: string
      - description: 命名空间
        in: query
        maxLength: 100
        name: namespace
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取Pod的Init容器镜像
      tags:
      - K8s Pod管理
  /api/v1/k8s/pod/init_container_name:
    get:
      description: 获取Pod的Init容器名
      parameters:
      - description: Pod名
        in: query
        maxLength: 100
        name: name
        type: string
      - description: 命名空间
        in: query
        maxLength: 100
        name: namespace
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取Pod的Init容器名
      tags:
      - K8s Pod管理
  /api/v1/k8s/pod/labels:
    patch:
      consumes:
      - application/json
      description: 添加或删除 Pod 标签
      parameters:
      - description: 标签修改参数
        in: body
        name: data
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubePodLabelPatchRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 修改 Pod 标签
      tags:
      - K8s Pod管理
  /api/v1/k8s/pod/list:
    get:
      description: 列出K8s Pod
      parameters:
      - description: Pod名
        in: query
        maxLength: 100
        name: name
        type: string
      - description: 命名空间
        in: query
        maxLength: 100
        name: namespace
        type: string
      - description: 页码
        in: query
        name: page
        required: true
        type: integer
      - description: 每页数量
        in: query
        name: limit
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 列出K8s Pod
      tags:
      - K8s Pod管理
  /api/v1/k8s/pod/metrics:
    get:
      description: 获取 Pod 的 CPU 和内存使用情况（需要 metrics-server）
      parameters:
      - description: 命名空间
        in: query
        maxLength: 100
        name: namespace
        required: true
        type: string
      - description: Pod名称
        in: query
        maxLength: 100
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
        "503":
          description: metrics-server 不可用
          schema:
            additionalProperties: true
            type: object
      summary: 获取单个 Pod 的资源使用情况
      tags:
      - K8s Pod管理
  /api/v1/k8s/pod/metrics/list:
    get:
      description: 获取指定命名空间下所有 Pod 的 CPU 和内存使用情况（需要 metrics-server）
      parameters:
      - description: 命名空间
        in: query
        maxLength: 100
        name: namespace
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
        "503":
          description: metrics-server 不可用
          schema:
            additionalProperties: true
            type: object
      summary: 批量获取命名空间下所有 Pod 的资源使用情况
      tags:
      - K8s Pod管理
  /api/v1/k8s/pod/patch_image:
    put:
      consumes:
      - application/json
      description: 基于 mergeKey=name 的 StrategicMergePatch 方式更新指定容器的镜像
      parameters:
      - description: body
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.PatchPodImageRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 更新 Pod 容器镜像
      tags:
      - K8s Pod管理
  /api/v1/k8s/pod/terminal:
    get:
      description: 通过 WebSocket 连接到 Pod 容器的交互式终端（类似 kubectl exec -it）
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Pod 名称
        in: query
        name: name
        required: true
        type: string
      - description: 容器名称（多容器 Pod 建议指定）
        in: query
        name: container
        type: string
      - description: Shell 类型（bash/sh/zsh，默认自动检测）
        in: query
        name: shell
        type: string
      responses:
        "101":
          description: Switching Protocols - WebSocket 握手成功
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 连接失败
          schema:
            additionalProperties: true
            type: object
      summary: 容器终端（WebSocket）
      tags:
      - K8s Pod管理
  /api/v1/k8s/pod/update:
    post:
      description: 更新Pod
      parameters:
      - description: body
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubePodUpdateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 更新Pod
      tags:
      - K8s Pod管理
  /api/v1/k8s/pod/yaml:
    get:
      description: 获取指定 Pod 的 YAML 格式配置
      parameters:
      - description: 命名空间
        in: query
        maxLength: 100
        name: namespace
        required: true
        type: string
      - description: Pod名称
        in: query
        maxLength: 100
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 Pod 的 YAML 配置
      tags:
      - K8s Pod管理
  /api/v1/k8s/pv/apply-yaml:
    post:
      consumes:
      - application/json
      parameters:
      - description: PV 名称
        in: query
        name: name
        required: true
        type: string
      - description: YAML 内容
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubePVApplyYamlRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 应用 PersistentVolume YAML
      tags:
      - K8s PV 管理
  /api/v1/k8s/pv/create:
    post:
      consumes:
      - application/json
      description: 创建 PV（支持 HostPath / NFS）
      parameters:
      - description: PV 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubePVCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 创建 PersistentVolume
      tags:
      - K8s PV 管理
  /api/v1/k8s/pv/create-from-yaml:
    post:
      consumes:
      - application/json
      parameters:
      - description: YAML 内容
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubePVCreateFromYamlRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 从 YAML 创建 PersistentVolume
      tags:
      - K8s PV 管理
  /api/v1/k8s/pv/delete:
    delete:
      parameters:
      - description: PersistentVolume 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 删除 PersistentVolume
      tags:
      - K8s PV 管理
  /api/v1/k8s/pv/detail:
    get:
      parameters:
      - description: PersistentVolume 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 PersistentVolume 详情
      tags:
      - K8s PV 管理
  /api/v1/k8s/pv/detail-enhanced:
    get:
      description: 获取 PV 的完整详情，包括关联的 PVC 信息、存储后端信息、事件等
      parameters:
      - description: PV 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "404":
          description: 资源不存在
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 PV 增强详情
      tags:
      - K8s PV 管理
  /api/v1/k8s/pv/expand:
    post:
      consumes:
      - application/json
      description: 扩大 PersistentVolume 容量（只能扩大不能缩小）
      parameters:
      - description: 扩容参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubePVExpandRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: PV 扩容
      tags:
      - K8s PV 管理
  /api/v1/k8s/pv/list:
    get:
      description: 支持分页、名称模糊查询（PV 为集群级资源，无 namespace 参数）
      parameters:
      - description: PV 名称(模糊匹配)
        in: query
        maxLength: 100
        name: name
        type: string
      - description: 页码(从1开始)
        in: query
        name: page
        required: true
        type: integer
      - description: 每页数量(默认20)
        in: query
        name: limit
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 PV 列表
      tags:
      - K8s PV 管理
  /api/v1/k8s/pv/reclaim:
    patch:
      parameters:
      - description: PV 名称
        in: query
        name: name
        required: true
        type: string
      - description: 回收策略 (Delete / Retain)
        in: body
        name: reclaimPolicy
        required: true
        schema:
          type: string
      produces:
      - application/json
      responses:
        "200":
          description: 修改成功
          schema:
            type: string
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 修改 PersistentVolume 回收策略
      tags:
      - K8s PV 管理
  /api/v1/k8s/pv/yaml:
    get:
      parameters:
      - description: PV 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 PersistentVolume YAML
      tags:
      - K8s PV 管理
  /api/v1/k8s/pvc/apply-yaml:
    put:
      consumes:
      - application/json
      description: 应用修改后的 YAML 配置到 PVC
      parameters:
      - description: YAML内容
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeApplyYamlRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 应用 PVC YAML 配置
      tags:
      - K8s PVC 管理
  /api/v1/k8s/pvc/create:
    post:
      consumes:
      - application/json
      description: 创建 PVC（支持指定 StorageClass / AccessModes / 容量等）
      parameters:
      - description: PVC 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubePVCCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 创建 PersistentVolumeClaim
      tags:
      - K8s PVC 管理
  /api/v1/k8s/pvc/create-from-yaml:
    post:
      consumes:
      - application/json
      parameters:
      - description: YAML 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.YamlCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 从 YAML 创建 PVC
      tags:
      - K8s PVC 管理
  /api/v1/k8s/pvc/delete:
    delete:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: PVC 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 删除 PersistentVolumeClaim
      tags:
      - K8s PVC 管理
  /api/v1/k8s/pvc/detail:
    get:
      description: 查询指定命名空间下的 PVC 详情（包括 YAML 内容）
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: PersistentVolumeClaim 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "404":
          description: 资源不存在
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 PersistentVolumeClaim 详情
      tags:
      - K8s PVC 管理
  /api/v1/k8s/pvc/detail-enhanced:
    get:
      description: 获取 PVC 的完整详情，包括绑定的 PV 信息、状态、事件等
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: PVC 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "404":
          description: 资源不存在
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 PVC 增强详情
      tags:
      - K8s PVC 管理
  /api/v1/k8s/pvc/list:
    get:
      description: 支持分页、名称模糊查询（PVC 属于命名空间级资源，需要 namespace 参数）
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: PVC 名称(模糊匹配)
        in: query
        maxLength: 100
        name: name
        type: string
      - description: 页码(从1开始)
        in: query
        name: page
        required: true
        type: integer
      - description: 每页数量(默认20)
        in: query
        name: limit
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 PVC 列表
      tags:
      - K8s PVC 管理
  /api/v1/k8s/pvc/resize:
    patch:
      consumes:
      - application/json
      description: 将 PVC 的 spec.resources.requests.storage 扩大为指定值（需 StorageClass 允许扩容）
      parameters:
      - description: 扩容参数：namespace/name/storage(如 10Gi)
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubePVCResizeRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "403":
          description: StorageClass 不允许扩容
          schema:
            additionalProperties: true
            type: object
        "404":
          description: 资源不存在
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 扩容 PersistentVolumeClaim（仅支持增大 storage）
      tags:
      - K8s PVC 管理
  /api/v1/k8s/rbac/check:
    post:
      consumes:
      - application/json
      description: 检查用户或 ServiceAccount 对指定资源的访问权限
      parameters:
      - description: 检查参数
        in: body
        name: body
        required: true
        schema:
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 检查主体权限
      tags:
      - K8s RBAC
  /api/v1/k8s/rbac/check/batch:
    post:
      consumes:
      - application/json
      description: 批量检查用户或 ServiceAccount 对多个资源的访问权限
      parameters:
      - description: 检查参数数组
        in: body
        name: body
        required: true
        schema:
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 批量检查主体权限
      tags:
      - K8s RBAC
  /api/v1/k8s/rbac/role:
    delete:
      description: 删除 K8s Role 或 ClusterRole
      parameters:
      - description: 类型（Role/ClusterRole）
        in: query
        name: type
        required: true
        type: string
      - description: 命名空间（Role 必填）
        in: query
        name: namespace
        type: string
      - description: 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 删除 Role
      tags:
      - K8s RBAC
    post:
      consumes:
      - application/json
      description: 创建 K8s Role 或 ClusterRole
      parameters:
      - description: 创建参数
        in: body
        name: body
        required: true
        schema:
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 创建 Role
      tags:
      - K8s RBAC
  /api/v1/k8s/rbac/rolebinding:
    delete:
      description: 删除 K8s RoleBinding 或 ClusterRoleBinding
      parameters:
      - description: 类型（RoleBinding/ClusterRoleBinding）
        in: query
        name: type
        required: true
        type: string
      - description: 命名空间（RoleBinding 必填）
        in: query
        name: namespace
        type: string
      - description: 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 删除 RoleBinding
      tags:
      - K8s RBAC
    post:
      consumes:
      - application/json
      description: 创建 K8s RoleBinding 或 ClusterRoleBinding
      parameters:
      - description: 创建参数
        in: body
        name: body
        required: true
        schema:
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 创建 RoleBinding
      tags:
      - K8s RBAC
  /api/v1/k8s/rbac/rolebindings:
    get:
      description: 获取 K8s RoleBinding 和 ClusterRoleBinding 列表
      parameters:
      - description: 命名空间（空表示所有）
        in: query
        name: namespace
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 获取 RoleBinding 列表
      tags:
      - K8s RBAC
  /api/v1/k8s/rbac/roles:
    get:
      description: 获取 K8s Role 和 ClusterRole 列表
      parameters:
      - description: 命名空间（空表示所有）
        in: query
        name: namespace
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 获取 Role 列表
      tags:
      - K8s RBAC
  /api/v1/k8s/rbac/serviceaccount:
    delete:
      description: 删除 K8s ServiceAccount
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 删除 ServiceAccount
      tags:
      - K8s RBAC
    get:
      description: 获取指定 ServiceAccount 详情
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 获取 ServiceAccount 详情
      tags:
      - K8s RBAC
    post:
      consumes:
      - application/json
      description: 创建 K8s ServiceAccount
      parameters:
      - description: 创建参数
        in: body
        name: body
        required: true
        schema:
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 创建 ServiceAccount
      tags:
      - K8s RBAC
    put:
      consumes:
      - application/json
      description: 更新 K8s ServiceAccount Labels / Annotations / AutomountToken
      parameters:
      - description: 更新参数
        in: body
        name: body
        required: true
        schema:
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 更新 ServiceAccount
      tags:
      - K8s RBAC
  /api/v1/k8s/rbac/serviceaccount/events:
    get:
      description: 获取指定 ServiceAccount 的 K8s 事件
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 获取 ServiceAccount 事件
      tags:
      - K8s RBAC
  /api/v1/k8s/rbac/serviceaccount/yaml:
    get:
      description: 获取指定 ServiceAccount 的 YAML 表示
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 获取 ServiceAccount YAML
      tags:
      - K8s RBAC
    put:
      consumes:
      - application/json
      description: 通过 YAML 更新 ServiceAccount
      parameters:
      - description: YAML 内容
        in: body
        name: body
        required: true
        schema:
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 应用 ServiceAccount YAML
      tags:
      - K8s RBAC
  /api/v1/k8s/rbac/serviceaccounts:
    get:
      description: 获取 K8s ServiceAccount 列表
      parameters:
      - description: 命名空间（空表示所有）
        in: query
        name: namespace
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 获取 ServiceAccount 列表
      tags:
      - K8s RBAC
  /api/v1/k8s/secret/apply-yaml:
    put:
      consumes:
      - application/json
      parameters:
      - description: YAML 内容
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeConfigMapApplyYamlRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 从 YAML 创建或更新 Secret
      tags:
      - K8s Secret 管理
  /api/v1/k8s/secret/create:
    post:
      consumes:
      - application/json
      description: 创建 Kubernetes Secret（支持 Opaque / TLS / DockerConfigJson / BasicAuth
        / SSHAuth）
      parameters:
      - description: Secret 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeSecretCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 创建 Secret
      tags:
      - K8s Secret 管理
  /api/v1/k8s/secret/create-from-yaml:
    post:
      consumes:
      - application/json
      parameters:
      - description: YAML 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.YamlCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 从 YAML 创建 Secret
      tags:
      - K8s Secret 管理
  /api/v1/k8s/secret/decode:
    post:
      consumes:
      - application/json
      description: 接收 Base64 字符串或整个 Secret.data 对象，返回明文
      parameters:
      - description: 要解码的数据
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeSecretDecodeRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: Base64 解码 Secret 数据
      tags:
      - K8s Secret 管理
  /api/v1/k8s/secret/delete:
    delete:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Secret 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 删除 Secret
      tags:
      - K8s Secret 管理
  /api/v1/k8s/secret/detail:
    get:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Secret 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 Secret 详情
      tags:
      - K8s Secret 管理
  /api/v1/k8s/secret/list:
    get:
      description: 支持分页、命名空间过滤、名称模糊查询
      parameters:
      - description: 命名空间
        in: query
        maxLength: 100
        name: namespace
        type: string
      - description: Secret 名称(模糊匹配)
        in: query
        maxLength: 100
        name: name
        type: string
      - description: 页码 (从1开始)
        in: query
        name: page
        required: true
        type: integer
      - description: 每页数量 (默认20)
        in: query
        name: limit
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 K8s Secret 列表
      tags:
      - K8s Secret 管理
  /api/v1/k8s/secret/patch:
    patch:
      consumes:
      - application/strategic-merge-patch+json
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Secret 名称
        in: query
        name: name
        required: true
        type: string
      - description: Patch Body(JSON字符串)
        in: body
        name: content
        required: true
        schema:
          type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: Patch Secret（StrategicMergePatch）
      tags:
      - K8s Secret 管理
  /api/v1/k8s/secret/patch-json:
    post:
      consumes:
      - application/merge-patch+json
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Secret 名称
        in: query
        name: name
        required: true
        type: string
      - description: Patch Body(JSON字符串)
        in: body
        name: content
        required: true
        schema:
          type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: Patch Secret（JSON Merge Patch – 覆盖式）
      tags:
      - K8s Secret 管理
  /api/v1/k8s/secret/yaml:
    get:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Secret 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 Secret YAML
      tags:
      - K8s Secret 管理
  /api/v1/k8s/service/apply-yaml:
    put:
      consumes:
      - application/json
      parameters:
      - description: 'YAML 内容 {yaml: string}'
        in: body
        name: body
        required: true
        schema:
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 应用 Service YAML（创建或更新）
      tags:
      - K8s Service 管理
  /api/v1/k8s/service/create:
    post:
      consumes:
      - application/json
      description: 独立创建 Kubernetes Service（支持 ClusterIP / NodePort / LoadBalancer
        / Headless）
      parameters:
      - description: Service 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeServiceCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 创建 Service
      tags:
      - K8s Service 管理
  /api/v1/k8s/service/create-from-yaml:
    post:
      consumes:
      - application/json
      parameters:
      - description: 'YAML 内容 {yaml: string}'
        in: body
        name: body
        required: true
        schema:
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 从 YAML 创建 Service
      tags:
      - K8s Service 管理
  /api/v1/k8s/service/delete:
    delete:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Service 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 删除 Service
      tags:
      - K8s Service 管理
  /api/v1/k8s/service/detail:
    get:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Service 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 获取 Service 详情
      tags:
      - K8s Service 管理
  /api/v1/k8s/service/endpoints:
    get:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Service 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
      summary: 获取 Service Endpoints（core/v1）
      tags:
      - K8s Service 管理
  /api/v1/k8s/service/list:
    get:
      description: 支持分页、命名空间过滤、名称模糊查询
      parameters:
      - description: 命名空间
        in: query
        maxLength: 100
        name: namespace
        type: string
      - description: Service 名称(模糊匹配)
        in: query
        maxLength: 100
        name: name
        type: string
      - description: 页码 (从1开始)
        in: query
        name: page
        required: true
        type: integer
      - description: 每页数量 (默认20)
        in: query
        name: limit
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 K8s Service 列表
      tags:
      - K8s Service 管理
  /api/v1/k8s/service/patch:
    patch:
      consumes:
      - application/strategic-merge-patch+json
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Service 名称
        in: query
        name: name
        required: true
        type: string
      - description: Patch Body(JSON字符串)
        in: body
        name: content
        required: true
        schema:
          type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Patch Service（StrategicMergePatch）
      tags:
      - K8s Service 管理
  /api/v1/k8s/service/patch-json:
    post:
      consumes:
      - application/merge-patch+json
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Service 名称
        in: query
        name: name
        required: true
        type: string
      - description: Patch Body(JSON字符串)
        in: body
        name: content
        required: true
        schema:
          type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: Patch Service（JSON Merge Patch – 覆盖式）
      tags:
      - K8s Service 管理
  /api/v1/k8s/service/yaml:
    get:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: Service 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 Service YAML
      tags:
      - K8s Service 管理
  /api/v1/k8s/statefulset/apply_yaml:
    put:
      consumes:
      - application/json
      description: 应用修改后的 YAML 配置到 StatefulSet
      parameters:
      - description: YAML内容
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeApplyYamlRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 应用 StatefulSet YAML 配置
      tags:
      - K8s StatefulSet 管理
  /api/v1/k8s/statefulset/create:
    post:
      consumes:
      - application/json
      parameters:
      - description: 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeStatefulSetCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 创建 StatefulSet（可选创建 Service）
      tags:
      - K8s StatefulSet 管理
  /api/v1/k8s/statefulset/create-from-yaml:
    post:
      consumes:
      - application/json
      parameters:
      - description: YAML 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.YamlCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 从 YAML 创建 StatefulSet
      tags:
      - K8s StatefulSet 管理
  /api/v1/k8s/statefulset/delete:
    delete:
      consumes:
      - application/json
      description: 前台级联删除 StatefulSet（先删 Pod/ControllerRevision，再删 StatefulSet 本体）；成功返回命名空间、名称与状态信息
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: StatefulSet 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: '示例: {\"namespace\":\"default\",\"name\":\"web\",\"status\":\"StatefulSet
            删除成功\"}'
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "404":
          description: StatefulSet 未找到
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      security:
      - BearerAuth: []
      summary: 删除 StatefulSet
      tags:
      - K8s StatefulSet 管理
  /api/v1/k8s/statefulset/delete_svc:
    delete:
      consumes:
      - application/json
      description: 根据命名空间与名称删除 Service；成功仅表示删除请求已受理。
      parameters:
      - description: 删除参数（namespace、name）
        in: body
        name: body
        required: true
        schema:
          additionalProperties:
            type: string
          type: object
      produces:
      - application/json
      responses:
        "200":
          description: 删除成功
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "404":
          description: Service 未找到
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 删除 Service
      tags:
      - K8s StatefulSet 管理
  /api/v1/k8s/statefulset/detail:
    get:
      consumes:
      - application/json
      description: 根据命名空间和名称获取单个 StatefulSet 的详细信息
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: StatefulSet 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 获取详情成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 获取 StatefulSet 详情
      tags:
      - K8s StatefulSet 管理
  /api/v1/k8s/statefulset/events:
    post:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        type: string
      - description: 资源类型
        in: query
        name: kind
        type: string
      - description: 资源名称
        in: query
        name: name
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 事件列表加载完成
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 获取事件列表（支持全局或按对象筛选）
      tags:
      - K8s StatefulSet 管理
  /api/v1/k8s/statefulset/history:
    get:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: StatefulSet 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 获取 StatefulSet 历史版本（ControllerRevision 列表）
      tags:
      - K8s StatefulSet 管理
  /api/v1/k8s/statefulset/list:
    get:
      consumes:
      - application/json
      description: 分页、模糊查询 StatefulSet 列表
      parameters:
      - description: StatefulSet 名称关键字（模糊匹配）
        in: query
        name: name
        type: string
      - description: 页码（默认 1）
        in: query
        name: page
        type: integer
      - description: 每页数量（默认 10）
        in: query
        name: limit
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 查询成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 获取 StatefulSet 列表
      tags:
      - K8s StatefulSet 管理
  /api/v1/k8s/statefulset/restart:
    post:
      consumes:
      - application/json
      description: 在 .spec.template.metadata.annotations 写入 `kubectl.kubernetes.io/restartedAt`
        时间戳，从而触发 StatefulSet 的滚动更新；等价于 `kubectl rollout restart sts <name>`。
      parameters:
      - description: 重启参数（namespace、name）
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeStatefulSetRestartRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 重启成功，返回命名空间、名称与触发时间等信息
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 参数错误
          schema:
            additionalProperties: true
            type: object
        "404":
          description: StatefulSet 未找到
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 重启 StatefulSet（触发滚动重启）
      tags:
      - K8s StatefulSet 管理
  /api/v1/k8s/statefulset/rollback:
    post:
      consumes:
      - application/json
      description: 将 StatefulSet 回滚到指定的历史版本（ControllerRevision）
      parameters:
      - description: 回滚参数（namespace、name、revision_name）
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeStatefulSetRollbackRequest'
      produces:
      - application/json
      responses:
        "200":
          description: StatefulSet 回滚成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 回滚 StatefulSet
      tags:
      - K8s StatefulSet 管理
  /api/v1/k8s/statefulset/scale:
    put:
      consumes:
      - application/json
      description: 通过 Patch 局部更新 .spec.replicas，K8s 将按策略有序创建/删除 Pod
      parameters:
      - description: 扩缩容参数（namespace、name、scale_num）
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeStatefulSetScaleRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 扩缩容成功，返回修改前后及当前副本信息
          schema:
            additionalProperties: true
            type: object
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 扩缩容 StatefulSet（修改副本数）
      tags:
      - K8s StatefulSet 管理
  /api/v1/k8s/statefulset/sts_pods:
    get:
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: StatefulSet 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 获取 StatefulSet 对应的 Pod 列表
      tags:
      - K8s StatefulSet 管理
  /api/v1/k8s/statefulset/update-resources:
    post:
      consumes:
      - application/json
      description: 直接修改 StatefulSet 容器的 CPU/Memory requests/limits
      parameters:
      - description: 资源配置
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeDeploymentUpdateResourcesRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 快速修改容器资源限制
      tags:
      - K8s StatefulSet 管理
  /api/v1/k8s/statefulset/update_image:
    put:
      consumes:
      - application/json
      description: 仅修改 .spec.template.spec.containers[*].image，不影响其它字段；根据 UpdateStrategy
        触发滚动更新
      parameters:
      - description: 更新镜像参数（namespace、name、container、image）
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeStatefulSetUpdateImageRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 更新成功，返回资源版本与副本进度
          schema:
            additionalProperties: true
            type: object
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties: true
            type: object
        "409":
          description: Conflict
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 更新 StatefulSet 容器镜像（Patch 局部更新）
      tags:
      - K8s StatefulSet 管理
  /api/v1/k8s/statefulset/yaml:
    get:
      description: 获取指定 StatefulSet 的 YAML 格式配置
      parameters:
      - description: 命名空间
        in: query
        name: namespace
        required: true
        type: string
      - description: StatefulSet 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            additionalProperties: true
            type: object
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 StatefulSet 的 YAML 配置
      tags:
      - K8s StatefulSet 管理
  /api/v1/k8s/storageclass/apply-yaml:
    post:
      consumes:
      - application/json
      parameters:
      - description: YAML 内容
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.YamlCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 应用 StorageClass YAML（创建/更新）
      tags:
      - K8s StorageClass 管理
  /api/v1/k8s/storageclass/create:
    post:
      consumes:
      - application/json
      description: 创建 Kubernetes StorageClass（支持指定 Provisioner、ReclaimPolicy、VolumeBindingMode
        等参数）
      parameters:
      - description: StorageClass 创建参数
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.KubeStorageClassCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 创建 StorageClass
      tags:
      - K8s StorageClass 管理
  /api/v1/k8s/storageclass/create-from-yaml:
    post:
      consumes:
      - application/json
      parameters:
      - description: YAML 内容
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.YamlCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 从 YAML 创建 StorageClass
      tags:
      - K8s StorageClass 管理
  /api/v1/k8s/storageclass/delete:
    delete:
      parameters:
      - description: StorageClass 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 删除 StorageClass
      tags:
      - K8s StorageClass 管理
  /api/v1/k8s/storageclass/detail:
    get:
      parameters:
      - description: StorageClass 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 获取 StorageClass 详情
      tags:
      - K8s StorageClass 管理
  /api/v1/k8s/storageclass/list:
    get:
      description: 支持分页、名称模糊查询
      parameters:
      - description: StorageClass 名称(模糊匹配)
        in: query
        maxLength: 100
        name: name
        type: string
      - description: 页码 (从1开始)
        in: query
        name: page
        required: true
        type: integer
      - description: 每页数量 (默认20)
        in: query
        name: limit
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 K8s StorageClass 列表
      tags:
      - K8s StorageClass 管理
  /api/v1/k8s/storageclass/yaml:
    get:
      parameters:
      - description: StorageClass 名称
        in: query
        name: name
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.Response'
        "400":
          description: 请求参数错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 获取 StorageClass YAML
      tags:
      - K8s StorageClass 管理
  /api/v1/k8s/vpa/available:
    get:
      responses: {}
      summary: 检测 VPA Operator 是否在集群中安装
      tags:
      - K8s VPA 垂直扩缩容
  /api/v1/k8s/vpa/create:
    post:
      responses: {}
      summary: 创建 VPA
      tags:
      - K8s VPA 垂直扩缩容
  /api/v1/k8s/vpa/create-from-yaml:
    post:
      consumes:
      - application/json
      parameters:
      - description: YAML 内容
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.YamlCreateRequest'
      responses: {}
      summary: 通过 YAML 创建 VPA
      tags:
      - K8s VPA 垂直扩缩容
  /api/v1/k8s/vpa/delete:
    delete:
      responses: {}
      summary: 删除 VPA
      tags:
      - K8s VPA 垂直扩缩容
  /api/v1/k8s/vpa/detail:
    get:
      responses: {}
      summary: 获取 VPA 详情
      tags:
      - K8s VPA 垂直扩缩容
  /api/v1/k8s/vpa/list:
    get:
      responses: {}
      summary: 获取 VPA 列表
      tags:
      - K8s VPA 垂直扩缩容
  /api/v1/k8s/vpa/update:
    post:
      responses: {}
      summary: 更新 VPA
      tags:
      - K8s VPA 垂直扩缩容
  /api/v1/ldap/config:
    get:
      description: 获取当前 LDAP 认证配置（脱敏）
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_internal_app_services.LDAPConfigResponse'
      security:
      - ApiKeyAuth: []
      summary: 获取 LDAP 配置
      tags:
      - LDAP管理
  /api/v1/ldap/status:
    get:
      description: 获取 LDAP 启用状态和连接状态
      produces:
      - application/json
      responses: {}
      summary: 获取 LDAP 状态
      tags:
      - LDAP管理
  /api/v1/ldap/sync:
    post:
      description: 从 LDAP 同步所有用户到平台
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_internal_app_services.LDAPSyncResult'
      security:
      - ApiKeyAuth: []
      summary: 全量同步 LDAP 用户
      tags:
      - LDAP管理
  /api/v1/ldap/test:
    post:
      description: 测试 LDAP 服务器是否可连接
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      security:
      - ApiKeyAuth: []
      summary: 测试 LDAP 连接
      tags:
      - LDAP管理
  /api/v1/platform/appstore/categories:
    get:
      description: 获取所有分类及其应用计数
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
      summary: 获取分类列表
      tags:
      - 应用商城
  /api/v1/platform/appstore/create:
    post:
      consumes:
      - application/json
      description: 管理员创建新应用
      parameters:
      - description: 应用信息
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_models.AppStoreCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
      summary: 创建应用
      tags:
      - 应用商城
  /api/v1/platform/appstore/delete/{id}:
    delete:
      description: 管理员删除应用（软删除）
      parameters:
      - description: 应用ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
      summary: 删除应用
      tags:
      - 应用商城
  /api/v1/platform/appstore/detail/{id}:
    get:
      description: 根据 ID 获取应用详情
      parameters:
      - description: 应用ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
      summary: 获取应用详情
      tags:
      - 应用商城
  /api/v1/platform/appstore/install:
    post:
      consumes:
      - application/json
      description: 将应用安装到指定集群的命名空间
      parameters:
      - description: 安装信息
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_models.AppStoreInstallRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
      summary: 安装应用
      tags:
      - 应用商城
  /api/v1/platform/appstore/installs:
    get:
      description: 分页获取应用安装记录
      parameters:
      - description: 应用ID
        in: query
        name: app_id
        type: integer
      - description: 集群ID
        in: query
        name: cluster_id
        type: integer
      - description: 状态
        in: query
        name: status
        type: integer
      - description: 页码
        in: query
        name: page
        type: integer
      - description: 每页数量
        in: query
        name: page_size
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
      summary: 获取安装记录列表
      tags:
      - 应用商城
  /api/v1/platform/appstore/installs/{id}:
    get:
      parameters:
      - description: 安装记录ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
      summary: 获取安装记录详情
      tags:
      - 应用商城
  /api/v1/platform/appstore/installs/{id}/status:
    get:
      description: 实时查询已安装应用在 K8s 集群中的 Deployment/Pod/Service 状态
      parameters:
      - description: 安装记录ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
      summary: 获取安装实时状态
      tags:
      - 应用商城
  /api/v1/platform/appstore/list:
    get:
      description: 分页获取应用列表，支持分类筛选和关键词搜索
      parameters:
      - description: 分类
        in: query
        name: category
        type: string
      - description: 搜索关键词
        in: query
        name: keyword
        type: string
      - description: 状态
        in: query
        name: status
        type: integer
      - description: 推荐
        in: query
        name: featured
        type: integer
      - description: 页码
        in: query
        name: page
        type: integer
      - description: 每页数量
        in: query
        name: page_size
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
      summary: 获取应用商城列表
      tags:
      - 应用商城
  /api/v1/platform/appstore/uninstall/{id}:
    post:
      description: 从集群卸载已安装的应用
      parameters:
      - description: 安装记录ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
      summary: 卸载应用
      tags:
      - 应用商城
  /api/v1/platform/appstore/update:
    put:
      consumes:
      - application/json
      description: 管理员更新应用信息
      parameters:
      - description: 应用信息
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_models.AppStoreUpdateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
      summary: 更新应用
      tags:
      - 应用商城
  /api/v1/platform/audit/cleanup:
    post:
      description: 根据当前保留策略清理过期数据
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties: true
            type: object
      summary: 手动清理过期审计日志
      tags:
      - 审计日志
  /api/v1/platform/audit/export:
    get:
      description: 按筛选条件导出审计日志（最多10000条）
      parameters:
      - description: 用户名
        in: query
        name: username
        type: string
      - description: 操作类型
        in: query
        name: action
        type: string
      - description: 模块
        in: query
        name: module
        type: string
      - description: 状态
        in: query
        name: status
        type: string
      - description: 开始时间
        in: query
        name: start_time
        type: integer
      - description: 结束时间
        in: query
        name: end_time
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/k8soperation_internal_app_models.AuditLog'
            type: array
      summary: 导出审计日志
      tags:
      - 审计日志
  /api/v1/platform/audit/logs:
    get:
      description: 支持多维度筛选、分页、排序
      parameters:
      - default: 1
        description: 页码
        in: query
        name: page
        type: integer
      - default: 20
        description: 每页条数
        in: query
        name: page_size
        type: integer
      - description: 用户名
        in: query
        name: username
        type: string
      - description: 操作类型
        in: query
        name: action
        type: string
      - description: 模块
        in: query
        name: module
        type: string
      - description: '状态: success/failed'
        in: query
        name: status
        type: string
      - description: 开始时间(unix)
        in: query
        name: start_time
        type: integer
      - description: 结束时间(unix)
        in: query
        name: end_time
        type: integer
      - description: 关键词搜索
        in: query
        name: keyword
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_internal_domain_audit.AuditLogListResponse'
      summary: 获取审计日志列表
      tags:
      - 审计日志
  /api/v1/platform/audit/logs/{id}:
    get:
      description: 根据ID获取单条审计日志完整信息
      parameters:
      - description: 日志ID
        in: path
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_internal_domain_audit.AuditLog'
      summary: 获取审计日志详情
      tags:
      - 审计日志
  /api/v1/platform/audit/retention:
    get:
      description: 返回当前保留天数和是否永久保留
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_internal_domain_audit.AuditRetentionPolicy'
      summary: 获取审计日志保留策略
      tags:
      - 审计日志
    put:
      consumes:
      - application/json
      description: 设置保留天数(0=永久)或切换永久保留
      parameters:
      - description: 保留策略
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_models.AuditRetentionUpdateReq'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
      summary: 更新审计日志保留策略
      tags:
      - 审计日志
  /api/v1/platform/audit/statistics:
    get:
      description: 返回今日/本周操作量、成功率、用户排行、模块排行等
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_internal_domain_audit.AuditStatistics'
      summary: 获取审计统计数据
      tags:
      - 审计日志
  /api/v1/platform/license/activate:
    post:
      consumes:
      - application/json
      description: 校验 License（签名/机器码/有效期）并持久化，激活后立即生效
      parameters:
      - description: License 文本
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/internal_app_controllers_api_v1.activateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_license.Status'
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
      summary: 激活 License
      tags:
      - License授权
  /api/v1/platform/license/status:
    get:
      description: 返回授权状态、被授权方、到期时间与本机机器码（机器码用于向供应商申请 License）
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_license.Status'
      summary: 查询 License 授权状态
      tags:
      - License授权
  /api/v1/platform/settings:
    get:
      description: 获取所有平台配置项
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_internal_domain_settings.PlatformSettingsResponse'
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 获取平台设置
      tags:
      - 平台管理
    put:
      consumes:
      - application/json
      description: 批量更新平台配置项
      parameters:
      - description: 设置内容
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_models.PlatformSettingsResponse'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_pkg_app_response.SuccessResponse'
        "400":
          description: Bad Request
          schema:
            additionalProperties: true
            type: object
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 更新平台设置
      tags:
      - 平台管理
  /api/v1/platform/settings/reset:
    post:
      description: 将所有设置恢复为默认值
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/k8soperation_internal_domain_settings.PlatformSettingsResponse'
        "500":
          description: Internal Server Error
          schema:
            additionalProperties: true
            type: object
      summary: 重置平台设置
      tags:
      - 平台管理
  /api/v1/rbac/check:
    get:
      description: 检查用户是否有指定集群的操作权限
      parameters:
      - description: 集群ID
        in: query
        name: cluster_id
        required: true
        type: integer
      - description: 操作类型(view/create/update/delete/exec)
        in: query
        name: action
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 检查权限
      tags:
      - RBAC权限管理
  /api/v1/rbac/cluster-permission/batch:
    post:
      consumes:
      - application/json
      description: 为用户批量分配多个集群的权限
      parameters:
      - description: 权限信息
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.BatchClusterPermissionRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 批量分配集群权限
      tags:
      - RBAC权限管理
  /api/v1/rbac/cluster-permission/create:
    post:
      consumes:
      - application/json
      description: 为用户创建集群访问权限
      parameters:
      - description: 权限信息
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.ClusterPermissionCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 创建集群权限
      tags:
      - RBAC权限管理
  /api/v1/rbac/cluster-permission/delete:
    post:
      description: 删除用户的集群访问权限
      parameters:
      - description: 权限ID
        in: query
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 删除集群权限
      tags:
      - RBAC权限管理
  /api/v1/rbac/cluster-permission/list:
    get:
      description: 获取集群权限列表（支持按用户或集群筛选）
      parameters:
      - description: 用户ID
        in: query
        name: user_id
        type: integer
      - description: 集群ID
        in: query
        name: cluster_id
        type: integer
      - description: 页码
        in: query
        name: page
        required: true
        type: integer
      - description: 每页数量
        in: query
        name: limit
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 获取集群权限列表
      tags:
      - RBAC权限管理
  /api/v1/rbac/cluster-permission/update:
    post:
      consumes:
      - application/json
      description: 更新用户的集群访问权限
      parameters:
      - description: 权限信息
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.ClusterPermissionUpdateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 更新集群权限
      tags:
      - RBAC权限管理
  /api/v1/rbac/permission/list:
    get:
      description: 获取所有权限定义
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 获取权限列表
      tags:
      - RBAC权限管理
  /api/v1/rbac/role/all:
    get:
      description: 获取所有角色（不分页，用于下拉选择）
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 获取所有角色
      tags:
      - RBAC权限管理
  /api/v1/rbac/role/create:
    post:
      consumes:
      - application/json
      description: 创建新角色
      parameters:
      - description: 角色信息
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.RoleCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 创建角色
      tags:
      - RBAC权限管理
  /api/v1/rbac/role/delete:
    post:
      description: 删除角色（系统内置角色不可删除）
      parameters:
      - description: 角色ID
        in: query
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 删除角色
      tags:
      - RBAC权限管理
  /api/v1/rbac/role/detail:
    get:
      description: 获取角色详情（包含权限列表）
      parameters:
      - description: 角色ID
        in: query
        name: id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 获取角色详情
      tags:
      - RBAC权限管理
  /api/v1/rbac/role/list:
    get:
      description: 获取角色列表（分页）
      parameters:
      - description: 角色名称
        in: query
        name: name
        type: string
      - description: 角色类型
        in: query
        name: role_type
        type: string
      - description: 页码
        in: query
        name: page
        required: true
        type: integer
      - description: 每页数量
        in: query
        name: limit
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 获取角色列表
      tags:
      - RBAC权限管理
  /api/v1/rbac/role/permissions:
    get:
      description: 获取角色关联的权限列表
      parameters:
      - description: 角色ID
        in: query
        name: role_id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 获取角色权限
      tags:
      - RBAC权限管理
  /api/v1/rbac/role/permissions/update:
    post:
      consumes:
      - application/json
      description: 更新角色关联的权限（覆盖原有权限）
      parameters:
      - description: 角色权限信息
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.RolePermissionsUpdateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 更新角色权限
      tags:
      - RBAC权限管理
  /api/v1/rbac/role/update:
    post:
      consumes:
      - application/json
      description: 更新角色信息
      parameters:
      - description: 角色信息
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.RoleUpdateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 更新角色
      tags:
      - RBAC权限管理
  /api/v1/rbac/role/users:
    get:
      description: 获取角色绑定的用户列表
      parameters:
      - description: 角色ID
        in: query
        name: role_id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 获取角色绑定用户
      tags:
      - RBAC权限管理
  /api/v1/rbac/user-role/assign:
    post:
      consumes:
      - application/json
      description: 为用户分配角色（覆盖原有角色）
      parameters:
      - description: 分配信息
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.UserRoleAssignRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 分配用户角色
      tags:
      - RBAC权限管理
  /api/v1/rbac/user-role/list:
    get:
      description: 获取用户的角色列表
      parameters:
      - description: 用户ID
        in: query
        name: user_id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 获取用户角色
      tags:
      - RBAC权限管理
  /api/v1/rbac/user/clusters:
    get:
      description: 获取当前用户可访问的集群列表
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 获取用户可访问的集群
      tags:
      - RBAC权限管理
  /api/v1/rbac/user/info:
    get:
      description: 获取用户完整的RBAC信息（角色+集群权限）
      parameters:
      - description: 用户ID
        in: query
        name: user_id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 获取用户RBAC信息
      tags:
      - RBAC权限管理
  /api/v1/rbac/user/namespaces:
    get:
      description: 获取当前用户在指定集群可访问的命名空间列表
      parameters:
      - description: 集群ID
        in: query
        name: cluster_id
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 获取用户可访问的命名空间
      tags:
      - RBAC权限管理
  /api/v1/rbac/user/permissions:
    get:
      description: 获取当前用户的完整权限信息（权限隔离用）
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
      security:
      - ApiKeyAuth: []
      summary: 获取当前用户完整权限
      tags:
      - RBAC权限管理
  /api/v1/user/create:
    post:
      description: 创建用户
      parameters:
      - description: body
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.UserCreateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 创建用户
      tags:
      - 用户管理
  /api/v1/user/delete:
    post:
      description: 删除用户
      parameters:
      - description: body
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.CommonIdRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 删除用户
      tags:
      - 用户管理
  /api/v1/user/list:
    get:
      description: 列出用户
      parameters:
      - description: 用户名
        in: query
        maxLength: 100
        name: username
        type: string
      - description: 角色筛选
        in: query
        name: role
        type: string
      - description: 状态筛选
        in: query
        name: status
        type: string
      - description: 页码
        in: query
        name: page
        required: true
        type: integer
      - description: 每页数量
        in: query
        name: limit
        required: true
        type: integer
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      security:
      - ApiKeyAuth: []
      summary: 列出用户
      tags:
      - 用户管理
  /api/v1/user/update:
    post:
      description: 更新用户
      parameters:
      - description: body
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/k8soperation_internal_app_requests.UserUpdateRequest'
      produces:
      - application/json
      responses:
        "200":
          description: 成功
          schema:
            type: string
        "400":
          description: 请求错误
          schema:
            additionalProperties: true
            type: object
        "500":
          description: 内部错误
          schema:
            additionalProperties: true
            type: object
      summary: 更新用户
      tags:
      - 用户管理
swagger: "2.0"
