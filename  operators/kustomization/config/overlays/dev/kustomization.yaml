apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: nginx-dev

resources:
  - ../../base

patches:
  - path: patch-nginx-dev.yaml

images:
  - name: nginx
    newName: nginx
    newTag: "1.27"
