apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: nginx-prod

resources:
  - ../../base

patches:
  - path: patch-nginx-prod.yaml

images:
  - name: nginx
    newName: nginx
    newTag: "1.27-alpine"
