# ============================================================
# K8sOperation 敏感配置 Secret
# 使用前请 base64 编码实际值替换下方占位符
# 生成命令：echo -n "your-value" | base64
# ============================================================
apiVersion: v1
kind: Secret
metadata:
  name: k8soperation-secret
  namespace: k8soperation
  labels:
    app.kubernetes.io/name: k8soperation
type: Opaque
stringData:
  # 数据库密码
  DB_PASSWORD: "123456"
  # Redis 密码
  REDIS_PASSWORD: "123456"
  # JWT 签名密钥
  JWT_SIGNING_KEY: "eoNB0%bv5M7995F1"
  # Jenkins 配置
  JENKINS_URL: "http://1.117.227.207:8080/"
  JENKINS_USERNAME: "admin"
  JENKINS_API_TOKEN: "1140e22ae3b1db1af5242b72213a530acb"
  # HMAC 签名密钥（Jenkins 回调验证）
  HMAC_SECRET: "changeme"
  # KubeConfig 加密密钥（AES-256，至少 32 位）
  KUBECONFIG_ENCRYPT_KEY: "changemp"
  # Cache 加密密钥
  CACHE_SECRET: "k8smana"
  # 钉钉 Webhook（可选）
  DINGTALK_WEBHOOK: ""
  # 前端公网地址（钉钉通知链接用）
  PLATFORM_FRONTEND_URL: "http://localhost:5173"
