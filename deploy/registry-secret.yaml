# ============================================================
# Harbor 镜像拉取凭证
# 首次部署前执行：
#   kubectl create secret docker-registry harbor-registry \
#     --docker-server=harbor.maitian-yun.com \
#     --docker-username='robot$foxesscloud' \
#     --docker-password='sOyDlotNV9rpmCs1thhUd0zl7sIKZY5o' \
#     -n k8soperation
# （此文件仅作记录，不在 kustomization 中引用以免泄露密码）
# ============================================================
